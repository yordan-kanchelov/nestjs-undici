# Plan: nestjs-axios-undici 1.0.0

This file records the plan so the work can pick up from any machine or session. Update it with every step: PR opened, reviewed, merged, or a decision made.

## Goal

Release 1.0.0 as a drop-in replacement for `@nestjs/axios`:

- Behave the way axios / `@nestjs/axios` does, as closely as possible, so that most projects can switch by changing one import.
- Keep the performance advantage over `@nestjs/axios` (currently about 2x throughput and about half the latency).
- Test automatically against the latest NestJS (12) and every supported version.
- Enforce package quality in CI: declared dependencies, types, and a frozen public API.
- Make the docs present the package on its own terms. The original fork is credited once, in the README Credits.

## How we work

- **Master branch:** `claude/v1.0.0`, merged to `main` through the master PR. This file lives there. Every feature PR targets `claude/v1.0.0`, not `main`.
- **For each step:**
  1. The worker commits and pushes to a branch named `claude/<topic>`.
  2. A PR into `claude/v1.0.0` is opened before the review starts.
  3. A reviewer reviews the PR.
  4. The worker pushes fix commits to the same PR.
  5. Once CI is green and the review is clean, the PR is merged into `claude/v1.0.0`.
  6. This file is updated.
- **Changesets:**
  - A change to the published package gets a changeset (`npx changeset`).
  - A change that affects only development or CI gets `npx changeset --empty`.
  - 1.0.0 is the major changeset added in the final step.
- **Exploration reports** go in `plan/reports/`. Reusable prototypes (tests, scripts) from the explorations go in `plan/prototypes/`. Delete `plan/prototypes/` before the master PR is merged, once everything useful has been ported.
- **Continuing locally:**
  ```bash
  git fetch origin && git checkout claude/v1.0.0
  # pick the next unchecked item below, then:
  git checkout -b claude/<topic> origin/claude/v1.0.0
  # develop on Node 24+ (Jest needs --experimental-vm-modules for ESM-only Nest 12; the npm scripts pass it)
  npm ci && npm run lint && npm run typecheck && npm test
  # open a PR into claude/v1.0.0
  ```

## Status

Master PR: #8 (`claude/v1.0.0` → `main`, draft).

Released so far:

- 0.6.0 was published by hand on 2026-09-25 (git tag / GitHub release still missing).
- 0.6.1 was published by CI through npm trusted publishing. It adds `@nestjs/core` as a peer dependency.
- Merged to main: #3 (release pipeline, Node 22/24/26), #4 (toolchain, cleanup, fixes), #5 (OIDC-only release), #6 (@nestjs/core peer), #7 (version packages 0.6.1).

## Decisions (made by the owner on 2026-09-25)

- [x] **`withCredentials`: yes.** It becomes a no-op, as in axios on Node. Cookie handling becomes opt-in through an explicit `cookieJar` option, and `http-cookie-agent` / `tough-cookie` become optional, lazily loaded peers. This is breaking and needs a changeset note.
- [x] **Redirects: do it as axios does.** Follow up to 21 redirects by default, and respect `maxRedirects` (0 disables). Use manual 3xx handling so non-redirect responses cost nothing extra, and add `ERR_FR_TOO_MANY_REDIRECTS` and `beforeRedirect`.
- [x] **Public API: trim it where nothing is lost.** Remove the legacy and internal exports listed in `plan/reports/package-quality.md`, keeping everything with a real use. For each removal, check that a supported alternative exists, and list it in the 1.0 migration notes.
- [x] **Benchmark apps: both Express and Fastify.** Use one shared app source with `CLIENT=axios|undici` and `PLATFORM=express|fastify`, and the same axiosRef interceptor in both. Raw undici is the floor.
- [x] **Minimum Node version:** `>=22.17.0` (PR #10, decided from the consumer-matrix results).
- [x] **Node 24+ / undici 8 only: offered by the owner, not taken for now** (2026-09-25). The owner said we may drop Node 22 and undici 7 if that makes things easier. It doesn't yet. Only one comment in `src/` is version-specific (setting `allowH2` explicitly). The Node 22 bundled-undici case gets fixed on every version by the planned per-service `Agent`. The CI cost is three short rows. Node 22 is LTS until April 2027 and common among Nest 10/11 users. Revisit only if an item needs a Node 24 / undici 8 feature, and bring the concrete trade-off to the owner.
- [x] **Breaking changes are fine before 1.0.0: do it the right way** (owner, 2026-09-25). Don't add compatibility shims, deprecation cycles or loose types just to avoid a break. Every break gets a changeset with a **BREAKING** note (`minor` until the final 1.0.0 `major`) and a line in the 1.0 migration notes. What this means for the remaining items:
  - **Types:** `HttpModuleOptions` becomes strictly typed (no `& any` / `Partial<any>`). Typos and unsupported keys become compile errors. `register()` takes a real type.
  - **API trim:** remove the legacy and internal exports outright (no 0.7 `@deprecated` step): the typed module, the no-op interceptors, internal error helpers and unused types.
  - **HttpService members:** rename `setGlobalDispatcher` to `setDispatcher` without an alias, make `setInterceptors` internal, return a real `interceptorCount` and a read-only `undiciRef`.
  - **Default dispatcher:** each HttpService owns an `Agent` built from this package's undici copy, even with no transport options (`allowH2: false` unless `httpVersion: 2`, created at setup, no per-request cost). This fixes several things at once:
    - the duplicate-undici case, where Node's bundled undici owns the global dispatcher;
    - undici 8 negotiating HTTP/2 by default on the no-options path;
    - `OnModuleDestroy`, which can then close everything the module created.

    `setGlobalDispatcher` from undici no longer affects services that don't configure a dispatcher. That is a break; document it.

## Roadmap

Legend: `[ ]` todo, `[~]` in progress (a PR is open), `[x]` merged into `claude/v1.0.0`.

### Phase 1: automation and compatibility foundation (first)

- [x] **A. ci: package integrity checks and consumer smoke matrix** (`claude/package-checks`, PR #10, merged; engines >=22.17.0)
  - The packed tarball is checked for declared dependencies, and passed through publint and attw.
  - A consumer matrix installs it with only its peers: Nest 10/11/12 × undici 7/8, on Node 22.x-min/22/24/26, loaded as CJS and as ESM, and type-checked.
  - It runs weekly, so new upstream releases within the peer ranges get tested.
  - `engines` changes, and the README gets a supported-versions table.
  - Prototypes: `plan/prototypes/automation/consumer/`, `plan/prototypes/quality/scripts/check-deps.js`.
- [x] **B. test: suite on NestJS 12 / @nestjs/axios 12, Nest 10/11 on Node 22** (`claude/nest12-test-matrix`, PR #9, merged)
  - devDeps move to Nest 12, and Jest runs with `--experimental-vm-modules`.
  - The CI matrix is: Node 24 Nest 12 (full checks + coverage), Node 26 Nest 12, Node 24 undici 8, Node 22 Nest 11, Node 22 Nest 10.
  - Coverage uses the v8 provider (fixes the Codecov paths), `forceExit` is removed, and `form-data` becomes a devDependency.
  - Note: the required status check names change.
- [x] **C. test: API-surface parity and type-level drop-in checks** (`claude/api-parity-checks`, PR open)
  - Compare the members of `@nestjs/axios` HttpService, axiosRef and AxiosHeaders against ours, with an allowlist.
  - Compile-only drop-in tests: `AxiosRequestConfig` / `AxiosResponse` / `AxiosInstance` assignability, plus the 16 typecompat usage cases.
  - Prototypes: `plan/prototypes/automation/api-surface-parity.cjs`, `.../differential/drop-in.types.ts`, `plan/prototypes/quality/typecompat/`.
  - `tests/compat/api-surface.spec.ts` (Jest, runs in `test:jest`): fails on any un-allowlisted missing member, and on any allowlisted member that starts to exist (stale allowlist). Allowlisted gaps, all tracked to phase 2 items above:
    - `HttpService`: `instance`, `makeObservable` (protected @nestjs/axios internals, still enumerable at runtime); `query` (new in @nestjs/axios 12, not implemented).
    - `axiosRef`: not callable, and missing `getUri`, `create`, `postForm`, `putForm`, `patchForm`, `query` (needs "make axiosRef a real axios instance"); the allowlist also has the function artifacts `length`, `name`, `prototype`.
    - `axiosRef.defaults`: `transitional`, `adapter`, `transformRequest`, `transformResponse`, `timeout`, `xsrfCookieName`, `xsrfHeaderName`, `maxContentLength`, `maxBodyLength`, `env`, `validateStatus` not populated (same item); `hasOwnProperty` is allowlisted too.
    - `axiosRef.interceptors.request`: `handlers`, `forEach` internals (same item).
  - `tests/types/` (compile-only, `npm run typecheck:compat` = `tsc -p tests/types/tsconfig.json`, `--strict`, added to the Node 24 full-checks CI row): `drop-in.ts` (HttpService/axiosRef/HttpModuleOptions assignability against `@nestjs/axios`) and `usage.ts` (the 16 typecompat cases). Both compile against the **built** package (`../../lib`, so `typecheck:compat` runs `npm run build` first) - kept as a separate build/check rather than folded into `tsconfig.json` so `tests/types` isn't loosely re-typechecked there and isn't matched by Jest's `testRegex`. Remaining known gaps, each pinned with `// @ts-expect-error` (fixed by `claude/types-axios-interop`: typed `HttpModuleOptions` catching typos, the merged `AxiosLikeRequestConfig`/`post<T,D>`, `registerAsync(HttpModuleAsyncOptions)`, `config.headers` bracket/`.set()` in interceptors; what's left needs a fuller `AxiosHeaders` class - tracked in "feat(axiosRef): make it a real axios instance"):
    - `HttpService` isn't fully assignable to `@nestjs/axios`' (`response.config.headers`, a real `AxiosHeaders` instance, isn't assignable to axios' own `AxiosHeaders` class: our class doesn't mirror every one of axios' overloaded `set`/`get`/`toJSON` call shapes, only the ones this library itself needs).
    - `axiosRef` isn't assignable to `AxiosInstance` (not callable, missing `getUri`/`create`/`*Form`/`query`).
    - A callback typed with axios' own `InternalAxiosRequestConfig` doesn't fit `axiosRef.interceptors.request.use()` (same `AxiosHeaders`-class root cause as above).
- [x] **D. test: table-driven differential harness** (`claude/differential-harness`, PR #12, merged)
  - Each scenario runs through `@nestjs/axios` and this package against one local server, and the results are compared.
  - `knownDifference` cases are cross-checked against the docs.
  - Prototypes: `plan/prototypes/automation/differential/`, plus the compat report's probe tests.
  - `tests/compat/differential/` (Jest, runs in `test:jest`): `harness.ts` plus `response.diff.spec.ts`, `request.diff.spec.ts`, `errors.diff.spec.ts`, `interceptors.diff.spec.ts`, `module.diff.spec.ts`, `retry-once.diff.spec.ts` — 207 scenarios (`claude/fix-errors` added 5: a stream-body `maxBodyLength`, a header value with an embedded newline, module-level `maxBodyLength`/`timeoutErrorMessage`, and `config.auth` winning over URL credentials), 9 with `knownDifference`, each pointing at one phase 2 item below. Runs in about 5s. Known-difference count per item (a case can only carry one item, so an item that touches several code paths, like the errors item, collects more):
    - fix(response): decode bodies like axios: 0 (fixed by `claude/fix-response-decoding`)
    - feat: axios default headers: 0 (was 9, plus 2 in `response.diff.spec.ts`; fixed by `claude/feat-default-headers`)
    - fix(observable): abort on unsubscribe and run request interceptors per subscription: 0 (was 4; fixed)
    - refactor(axiosRef): one config object from interceptors to response.config / error.config: 0 (was 11; fixed by `claude/refactor-axiosref-config`, plus the 4 new `retry-once.diff.spec.ts` cases, all passing)
    - fix: follow redirects by default (21): 0 (was 1; fixed by `claude/fix-redirects`)
    - fix(config): transport options: 0 (was 1; fixed by `claude/fix-transport-options`)
    - breaking: withCredentials becomes a no-op; add cookieJar: 0 (was 1; fixed by `claude/cookie-jar`)
    - types: axios interop: 3
    - feat(axiosRef): make it a real axios instance: 0 (was 3; fixed by `claude/axiosref-instance`)
    - fix(errors): match axios errors: 6 (was 19; fixed by `claude/fix-errors`: the total-deadline timeout, `timeoutErrorMessage`/`clarifyTimeoutError`, size-limit codes/precedence, `validateStatus: null`, URL credentials, `allowAbsoluteUrls: false`, unsupported-protocol/undici-argument-error wrapping, and `error.request`/`response.request`. Left: a query-string apostrophe always comes out `%27` (undici's own WHATWG URL parsing, not fixable without bypassing it), a header value with an embedded `\n` (undici rejects it, Node's own `http` - what axios uses - silently strips it instead; this library's rejection is at least a well-formed `AxiosError` now), a timeout landing after the response has already started streaming (axios' own internal race can give `ERR_BAD_RESPONSE`/"stream has been aborted" instead of `ECONNABORTED`, depending on whether axios' response object happens to exist yet), and a non-string/non-object request body (`5`, `true`) sent through instead of rejected (out of this item's original scope, not attempted)
    - Progress callbacks / formSerializer: 0 (added `tests/compat/differential/progress-form.diff.spec.ts`, 6 scenarios comparing the stable parts of progress events plus exact `formSerializer`/`formDataToJSON` output against real axios/`@nestjs/axios`; fixed by `claude/progress-form`)
  - Follow-ups from the PR #12 review (not blocking):
    - Our side reuses the global undici Agent across scenarios; give each scenario a fresh dispatcher, the way the axios side gets its own `register({})`.
    - `knownDifference` only asserts that *something* differs; pin the expected differing field per case.
    - Replace the 200 ms sleep in interceptors.diff.spec.ts with polling for the close event.
    - The generic fallback normalizer in harness.ts is unused.
- [x] **E. perf: rebuild the PR regression check.** (`claude/perf-check`, PR #13, merged) It isn't reliable today: 2 of 4 runs of identical code failed at the 10% threshold.
  - Switched `benchmarks/micro/compare.js` to client CPU time per request (`process.cpuUsage()`), paired against raw undici measured in the same round (cancels out runner speed), alternating base/head rounds, gated on the median paired ratio. Scenarios: `get`, `post` with a JSON body, `get` with params and headers, the 404 error path, and axiosRef request/response interceptors. On a regression it re-runs the failing scenarios once (fresh rounds) before failing the job. `benchmarks/micro/client.js` replaces `http-service.bench.js`; `server.js` gained `/echo` and `/404`.
  - **Noise measured:** 3 head-vs-head runs (same build both sides) at the CI settings (5 rounds × 2s, threshold 10%), on a heavily shared/contended sandbox (noisier than a dedicated CI runner). Worst paired CPU/req drift per scenario across the 3 runs: get 3.0%, post 4.7%, config 5.9%, error 4.1%, interceptors 9.5% (one outlier, close to but under the 10% threshold; the other two runs were ≤6% on every scenario). All 3 runs passed with no retry needed. `interceptors` is the noisiest scenario (highest baseline CPU/req and the most sources of variance); worth revisiting (more rounds, or a slightly higher per-scenario threshold) if it flakes in real CI.
  - **Sensitivity measured:** an artificial ~5µs busy-loop injected into `executeRequest` (on a scratch copy of the built lib, not committed) reliably failed the check — 3 of 5 scenarios (`get`, `post`, `error`) still regressed after the automatic retry, at +12–14% paired CPU/req. `config` and `interceptors` (higher baseline CPU/req, so 5µs is a smaller fraction) passed in this run, so the check's sensitivity floor is roughly a 5µs regression on the lighter scenarios; a real regression concentrated in `executeRequest` and touching `config`/`interceptors` only would need to be a bit larger to reliably trip.
  - **CI runtime:** the comparison step alone measured ~2m50s–2m57s on a passing run (5 rounds × 2s, `--base`/`--head` as separate built directories) in this sandbox; add checkout/install/build for both directories (~1–1.5 min with `cache: npm`) for a total job estimate of about 4 minutes. A run that hits a real regression and retries takes roughly double (~5–6 min), since the retry re-measures the failing scenarios in full.
  - Note: making this check a **required** status check is a GitHub branch-protection setting; the repo owner has to turn it on (Settings → Branches → branch protection rule for `claude/v1.0.0` → Require status checks to pass, add "HttpService regression check" / the `regression` job).
  - Prototypes: `plan/prototypes/perf/compare2.js`, `client.js`, `server.js`.
- [x] upstream conformance suites (see `plan/reports/upstream-test-suites.md`). Merged in PR #28.
  - Promoted into maintained CI jobs at `tests/upstream/` (`.github/workflows/upstream.yml`: weekly, `workflow_dispatch`, and PRs touching `src/**`/`tests/upstream/**`), each with an `expected-failures*.json` (like `knownDifference`) the runner diffs against, printing passed/expected-fail/new-fail/fixed and appending to `$GITHUB_STEP_SUMMARY`. `plan/prototypes/upstream-tests/` deleted.
    - @nestjs/axios' own specs (jest `moduleNameMapper` shims): 20/23 pass (up from 16/23 - PR #23/#25 fixed `postForm`/`query`/`axiosRef.defaults.timeout`); the 3 left are the stream-abort deliberate difference below and two harness/shim limits (`HTTP_MODULE_ID` shape, `AXIOS_INSTANCE_TOKEN` having no real equivalent).
    - axios' `tests/unit/adapters/http.test.js`, strategy (a) axiosRef as the instance: 130/241 pass, 111 expected failures, 1 environment-dependent (IPv6), 0 not evaluated. Strategy (b) our transport as an axios adapter: 111/241 pass, 130 expected failures, 1 environment-dependent, 0 not evaluated (4 tests hard-excluded from both: legacy `CancelToken`, a `HangingConnectAgent`/fake-hostname pair, plus FormData multipart for (b) only - see `run.mjs`'s `HARD_EXCLUDES`). Most of the remainder is the documented scope boundary (strategy (b) bypasses axiosRef's config normalization: auth/params/proxy/socketPath/default headers) or Node-http-specific/HTTP2-and-proxy-are-module-level-only differences, not new findings.
    - The full 246-test run's early hang was bisected to two causes, both fixed: (1) the upstream fixture's own `server.listen(port, cb)` never surfaces a bind failure to `cb` (a real bug in `tests/setup/server.js`, not modified), so one lingering socket on its fixed test port could hang every later test on it forever - fixed by `adapters/ephemeral-ports.cjs`; (2) an undrained response body on a `buildRedirectHop` security-check failure leaked a connection - fixed in `undici-adapter.cjs`. A first real-CI run still hit its per-strategy deadline (a `progress`/`Rate limit` describe block is genuinely, deliberately slow by test design - one test `await`s ten real `setTimeout(1100ms)`s - not hung; several such tests clustered past a too-short per-attempt timeout), silently folding ~70 never-run tests into "expected failures". Fixed properly: a test whose chunk never produces a report is now reported **NOT EVALUATED** in its own bucket and always fails the run (never silently expected/passed); `CHUNK_SIZE`/`CHUNK_TIMEOUT_MS`/`STRATEGY_DEADLINE_MS` retuned with headroom for the slow cluster; the workflow runs the 3 suites as parallel matrix jobs (not one serial job) so a slow strategy's deadline doesn't starve its siblings or need a longer `timeout-minutes`. `should support IPv6 literal strings` (fails with no IPv6, passes on a real runner) is now `environmentDependent` in `expected-failures*.json` (allowed either outcome, tracked separately) instead of a fixed guess. Recalibrated both axios expected-failures lists from fully-evaluated runs (strategy (b) had 11 stale "expected failure" entries that were actually never-run tests which do pass; removed).
    - Real, new finding: this package has no `data:` URL support at all (axios decodes one locally; `unsupportedProtocol` here rejects it) - added as its own Phase 2 item below.
  - The error-related findings (timeout message, `timeoutErrorMessage`, `error.request`, `maxBodyLength`, unsupported protocol) were handed to fix(errors).
  - **Deliberate difference, not fixed:** @nestjs/axios never aborts a `responseType: 'stream'` request on unsubscribe. We abort it only before the headers arrive, when no one can hold the stream yet, which frees the connection. Once the stream is emitted we never abort. Documented in `tests/upstream/nestjs-axios/expected-failures.json`.

### Phase 2: compatibility fixes (one small PR each; each flips differential cases and passes the perf check)

Details and repro tests: `plan/reports/axios-compat.md` and `plan/prototypes/compat/`. ★ = must-fix for 1.0.

- [x] ★ **fix(response): decode bodies like axios.** `+json` types, strings for non-binary responses, gzip/br/deflate with `decompress`, `blob`, and `statusText` taken from the server's reason phrase. (PR #14, merged; also rejects on corrupt compressed bodies)
- [x] ★ **feat: axios default headers.** `Accept`, `User-Agent`, `Accept-Encoding`; flatten `headers.common` / `headers.post` in module options. PR open (`claude/feat-default-headers`).
- [x] ★ **fix(observable): abort on unsubscribe and run request interceptors per subscription.** Use `defer()` so `retry()` re-runs interceptors. PR #15, merged.
  - Follow-ups from the PR #15 review (not blocking):
    - [x] `resolveSignal` in axios-request.adapter.ts leaves a listener on `signal` when combined with a legacy `cancelToken`. Fixed by `claude/fix-errors`: the combined signal carries a `SIGNAL_CLEANUP` symbol that `executeRequest`'s teardown calls, so the listener is removed whether or not the request ever aborts.
    - [x] `toAxiosError` checks `options.signal` (the user's) instead of the per-request signal; it works only via the AbortError/UND_ERR_ABORTED fallback. Fixed by `claude/fix-errors`: `toAxiosError` takes the actual per-request `RequestAbortSignal` `executeRequest` dispatches with.
    - Performance: cache the interceptor handler chain until interceptors change; skip the `defer()` config re-normalisation when no interceptors are registered; merge the two `.then()` calls.
- [x] ★ **refactor(axiosRef): one config object from interceptors to `response.config` / `error.config`.** PR #18, merged.
  - Fixes retry-once loops, empty POST replays, axios-retry and axios-auth-refresh.
  - Covers axios interceptor order, `runWhen` / `synchronous`, and per-request transforms.
- [x] ★ **fix: follow redirects by default (21)** using manual 3xx handling with no cost on other responses. Also `ERR_FR_TOO_MANY_REDIRECTS`, dropping body headers after 301/302, and `beforeRedirect`. (Owner decision.) PR #19, merged.
- [x] ★ **fix(config): transport options.** `httpsAgent` TLS (`ca` / `cert` / `rejectUnauthorized`), `socketPath`, proxy env vars, HTTP/2 opt-in. PR #20, merged.
- [x] ★ **breaking: `withCredentials` becomes a no-op; add an explicit `cookieJar` option.** (Owner decision.) PR #21, merged.
- [x] ★ **types: axios interop.** PR #22, merged.
  - `AxiosRequestConfig` / `AxiosResponse` / `AxiosInstance` assignability and `post<T, D>`. Done except full `AxiosInstance`/`AxiosHeaders`-class parity (left for the item below).
  - Typed `HttpModuleOptions`. Done.
  - Optional `axios` peer so `instanceof axios.AxiosError` works. Done (`instanceof axios.CanceledError` specifically doesn't - a prototype chain is linear; documented in `linkOptionalAxiosPeer`'s doc comment in `axios-error.ts`, use `isCancel()`).
  - Full `AxiosHeaders`, with `response.headers` as `AxiosHeaders`. Partly done: `AxiosHeaders` gained `concat`/`toString`/`normalize`/`getSetCookie`/the `get/set/has*` shorthands. `response.headers` stays a plain object at runtime - measured (this library's `AxiosHeaders`, a typical response, 200k iterations) at about 955ns more per response just to construct (before the Proxy-trap cost on every later read), not worth it against the CI regression check's `+10%` budget, paid on every response unconditionally. Typed `Record<string, any>` (assignable to/from axios' own response header types) regardless.
- [x] **feat(axiosRef): make it a real axios instance.** PR #23, merged.
  - Full `AxiosHeaders` parity: keep the header name casing as given, as axios does. Today names are stored lower-cased, so `normalize(true)` can't title-case and `toJSON()` returns lower-case names (PR #22 review). The remaining 3 `@ts-expect-error` markers in `tests/types` (axios' overloaded `AxiosHeaders` signatures) also belong here.
  - Callable, `getUri`, `create`, `*Form`, `query`.
  - A function `adapter` (so axios-mock-adapter works) and the full `defaults`.
  - `HttpService.query()`.
- [x] **fix(errors): match axios errors.** PR #25, merged.
  - [x] Wrap synchronous undici errors and set `request`.
  - [x] Total (deadline) timeout, `timeoutErrorMessage`, `clarifyTimeoutError`.
  - [x] Size-limit codes and precedence.
  - [x] `validateStatus: null`, URL credentials, `allowAbsoluteUrls`.
- [x] Progress callbacks (`onUploadProgress` / `onDownloadProgress`), `maxRate`, `formSerializer`. Merged in PR #30.
- [x] fix: support `data:` URLs (found by upstream conformance). PR #31, merged. `HttpService.executeRequest`'s `unsupportedProtocol` check only allows `http:`/`https:`, so `axiosRef.get('data:text/plain;base64,...')` rejects with `Unsupported protocol data:`; real axios decodes a `data:` URL locally, no network request. Repro: `tests/upstream/axios/expected-failures.strategy-b.json`'s "Data URL" entries (axios' own `tests/unit/adapters/http.test.js`, run against this package's undici-backed transport).
- [x] fix: enforce `maxContentLength` for `responseType: 'stream'` (found by upstream conformance). PR #31, merged. The stream branch in `axios-response-type.adapter.ts` returns the body untouched, while axios destroys the stream with an error once the limit is exceeded. Unbounded streamed downloads despite a cap make this DoS-relevant.
- [x] fix: sanitize CRLF / non-Latin1 header values like axios (found by upstream conformance). PR #31, merged. axios strips them before handing headers to Node (in two passes at two different times - see the doc comment on `sanitizeHeaderValue` in `axios-headers.ts`); undici threw `InvalidArgumentError`, which we wrapped as an AxiosError. Decided: strip, matching axios exactly, rather than reject.
- [x] fix: remaining error-shape gaps (found by upstream conformance). PR #31, merged.
  - an unparsable `timeout` should give `ERR_BAD_OPTION_VALUE`, not `ERR_BAD_REQUEST` - done.
  - a synchronous config-normalization error (e.g. throwing inside `params`) should be wrapped as an AxiosError - done (also sets `error.url`/`error.exists`, axios' own `AxiosError.from` `customProps` for this case).
  - HTTP and interceptor errors should keep the call-site stack, as axios does - **not implemented as asked**: a literal port of axios' mechanism (a fresh `Error.captureStackTrace` per error, appended in `HttpService.dispatch`'s `catchError`) measured 40%+ CPU/req on an all-error workload even capped at `stackTraceLimit: 1`, for materially less benefit than in axios (this library's request path is an RxJS `Observable`; confirmed with a minimal repro, and with the actual axiosRef pipeline, that neither a real HTTP error nor a *synchronously*-thrown interceptor error keeps a link back to the caller of `axiosRef.get()`/`firstValueFrom()` - subscribing an Observable through a Promise-returning wrapper doesn't preserve V8's async-stack-trace chain the way a plain `await`/`.then()` chain does, regardless of where in the pipeline the error itself originates). Reverted; documented in the doc comment on `HttpService.dispatch`. This library's existing errors already keep a meaningful stack at no extra cost (an interceptor's own thrown error is never wrapped, so it keeps the stack from wherever it was thrown; an HTTP-originated error's stack shows its own `AxiosError`/`AxiosError.from` construction site). axios' own "should wrap HTTP errors and keep stack" / "should wrap interceptor errors and keep stack" tests (both assert the stack contains the caller's own function name) are expected failures here - see `tests/upstream/axios/expected-failures.strategy-a.json`.
- [x] investigate: reading a large response body slowly fails with `UND_ERR_SOCKET` "other side closed" when the server has a short `keepAliveTimeout` (Node's 5s default included) (`claude/slow-consumer`, PR #33 open). Root-caused: a real undici h1-client bug, not a test-harness artifact - `onHttpSocketEnd` (`lib/dispatcher/client-h1.js`) treats the socket's `'end'` event as a premature close whenever `parser.statusCode` is still set, but a slow consumer leaves the parser *paused on backpressure* (so `onMessageComplete` hasn't reset it yet) for exactly long enough that a short `keepAliveTimeout` server's FIN can race it - confirmed directly with instrumented undici internals (`parser.bytesRead` already matched `Content-Length`; `onMessageComplete` simply hadn't run yet). Not filed upstream (the owner will, using `plan/reports/undici-slow-consumer.md`'s repro/analysis). Mitigated in this package (`meterDownloadBody`, `src/modules/http/adapters/axios-progress.adapter.ts`) for a **buffered** download (every `responseType` except `'stream'`) with a known `Content-Length` within `maxContentLength`, metered by `maxRate`/`onDownloadProgress`: the raw body is drained as fast as undici delivers it (bounded by `Content-Length`, no worse than what that response already holds in memory) instead of left backpressured, so the race is avoided at the source, plus a belt-and-suspenders check (every `responseType`, `'stream'` included) for the rare case it still occurs. `responseType: 'stream'` deliberately keeps today's backpressured pipe unconditionally - eager-draining it would buffer a potentially huge streamed download in memory, defeating the point of streaming (coordinator review fix: the first version of this drained eagerly for every `responseType`, flagged and corrected before merge). Also fixed, found by the same review: an abort/`timeout` on a metered, buffered download used to have nothing left to interrupt once the eager drain had already finished with the raw transport, so it could silently resolve instead of reject - `RequestAbortSignal` (`http.service.ts`) now supports more than one listener, and `meterDownloadBody` registers its own alongside undici's. Confirmed directly against axios' own "should support download rate limit" upstream test (the real trigger: its fixture defaults `keepAliveTimeout` to 1s) - failed before this fix, passes reliably after it (repeated isolated runs); `tests/upstream/axios/expected-failures.strategy-a.json`'s entry removed. A chunked/unknown-length response, a response whose `Content-Length` already exceeds `maxContentLength`, and `responseType: 'stream'` (with or without `maxRate`/`onDownloadProgress`), remain exposed - documented as a known limitation with a workaround in `docs/axios-supported-options.md` and `docs/migration-guide.md`.
- [x] fix: join duplicate response headers like Node/axios. PR #34. undici's own header parser (`parseHeaders` in `core/util.js`) accumulates every repeated response header name into an array uniformly; axios, running on Node's `http`, instead gets `IncomingMessage.headers`'s per-name rules (ported from Node's `_http_incoming.js`): `set-cookie` is always an array, `cookie` joins repeats with `'; '`, a fixed "no duplicates" set (`content-type`, `content-length`, `user-agent`, `referer`, `host`, `authorization`, `proxy-authorization`, `if-modified-since`, `if-unmodified-since`, `from`, `location`, `max-forwards`, `retry-after`, `etag`, `last-modified`, `server`, `age`, `expires`) keeps only the first value, and everything else joins with `', '`. New `joinDuplicateHeaders()` in `axios-response.adapter.ts`, applied once in `toAxiosLikeResponse` before `content-type`/`content-encoding`/`content-length` are read, before `transformResponse` sees the headers, and before they reach the caller - also fixes a latent crash where a duplicated `Content-Type` reached `.trim()` as an array. Flips the "headers: casing, multi-value set-cookie, duplicates" known difference in `response.diff.spec.ts` to an equality case.
- [x] fix: zstd decompression. PR #35 (open). axios feature-detects `zlib.createZstdDecompress` and decodes a `Content-Encoding: zstd` response when the Node.js runtime exposes it (added in Node 22.15.0/23.8.0 - every Node this package's `engines.node` allows already has it, feature-detected the same way axios does rather than assumed); wired into `decompressBuffer`/`decompressStream` (`axios-response-type.adapter.ts`), covering both the buffered and `responseType: 'stream'` paths, `decompress: false`, and `maxContentLength` against the decompressed size (reusing `decompressStream`'s existing bidirectional destroy propagation and `guardStreamMaxContentLength`, unchanged). **Decided:** the default `Accept-Encoding` still doesn't advertise `zstd` - axios itself only does when `transitional.advertiseZstdAcceptEncoding === true` is explicitly set (default `false`), so this matches axios' own out-of-the-box behaviour; a caller who wants to advertise it can already override the header the normal way. Found via the axios upstream conformance suite (PR #32 docs pass); `docs/axios-supported-options.md` updated (no longer a gap).
- [x] fix: JSON reviver (`parseReviver`). PR #35 (open). axios' default `transformResponse` calls `JSON.parse(data, config.parseReviver)`; wired the same reviver through this library's own default JSON parsing (`parseJsonOrText`/`parseTextMaybeJson`, `axios-response-type.adapter.ts`) for both the no-`responseType` and `responseType: 'json'` paths - only reaches default parsing, same as axios (a custom `transformResponse` replaces it entirely). Supported on per-request config, `axiosRef.defaults` (added to `DEFAULTS_PASSTHROUGH_KEYS`) and module options (`register({ parseReviver })`), same request > defaults > module precedence as every other passthrough default; no cost when unset. Found via the axios upstream conformance suite (PR #32 docs pass); documented in `docs/axios-supported-options.md`.
- [x] fix: redirect `sensitiveHeaders` option. PR #35 (open). axios (`lib/adapters/http.js`) lets `config.sensitiveHeaders` (an array of strings, validated - `ERR_BAD_OPTION_VALUE` otherwise) extend the built-in `Authorization`/`Cookie`/`Proxy-Authorization` list, via **two** separate mechanisms: follow-redirects' own lenient (subdomain-exempt, downgrade-only) rule now also covers the extra names (concatenated into its header filter), *and* axios wires its own stricter, purely origin-based rule (`isSameOriginRedirect`) that strips only the extra names on *any* origin change, including a subdomain redirect or an http→https upgrade. Both ported into `redirect.adapter.ts` (`shouldStripSensitiveHeaders` unchanged for the built-in 3; new `isSameOrigin` for the stricter rule) and wired into `HttpService.executeRequest`, request > `axiosRef.defaults`/`axiosRef.create()` > module precedence (added to `DEFAULTS_PASSTHROUGH_KEYS`, matching axios' own `axios.create({ sensitiveHeaders })`), validated eagerly - before ever dispatching - whenever redirects are actually followed, matching axios' own timing. Found via the axios upstream conformance suite (PR #32 docs pass); documented in `docs/axios-supported-options.md`.
- [x] fix: parse a numeric-string `timeout` like axios. PR #37, merged. `timeout: '250'` (a string, `parseInt`-able) rejects with `ERR_BAD_REQUEST`/"invalid headersTimeout" (undici's raw argument-validation error, since the string reaches undici's own `headersTimeout`/`bodyTimeout` unparsed) instead of being enforced as 250ms and giving `ECONNABORTED`/"timeout of 250ms exceeded" like axios. Found triaging `tests/upstream/axios/expected-failures.strategy-a.json`'s "not yet root-caused" entries (PR #32 docs pass), confirmed with a repro against the built package.
- [x] fix: wrap a throwing `beforeRedirect` like axios. PR #37, merged. A `beforeRedirect` callback that throws propagates the raw, unwrapped `Error` (`code: undefined`) instead of axios' (`follow-redirects`') "Redirected request failed: \<message\>" wrapping. Found triaging the same expected-failures file, confirmed with a repro against the built package.
- [x] fix: decode `Content-Encoding: compress`. PR #38, open. axios' Node `http` transport aliases `compress`/`x-compress` to its gzip decoder (`zlib.createUnzip`); this library now recognizes both, decoding via the same path as `gzip`, for buffered and `responseType: 'stream'`, honouring `decompress`/`decompress: false` and the streamed `maxContentLength` check. Default `Accept-Encoding` now reads `gzip, compress, deflate, br`, matching axios exactly. Found triaging the same expected-failures file; documented in `docs/axios-supported-options.md`.
- [x] fix: cancel a `responseType: 'stream'` response when its request stream is destroyed. PR #38, open. Destroying the request-body stream mid-upload does destroy the response stream here too, but with undici's raw `AbortError`/`UND_ERR_ABORTED` (not wrapped as an `AxiosError`); axios expects `CanceledError`/`ERR_CANCELED`. Fixed: `wrapStreamCancellation` (`axios-response.adapter.ts`) translates the abort-shaped error undici *already* raises on the response body (confirmed directly against a real undici `request()` call, no code of this library's own involved in producing it) into a real `CanceledError`. Found triaging the same expected-failures file, confirmed with a repro against the built package.
- [x] fix: reject a malformed URL like axios instead of silently dispatching it. PR #37, merged. A URL with an embedded null byte or a bare `\n` (e.g. `'\u0000https:example.com/users'`) isn't rejected synchronously - the characters are silently stripped and the request is actually sent over the network, instead of rejecting with `ERR_INVALID_URL` before ever touching the network, as axios does. Found triaging the same expected-failures file, confirmed with a repro against the built package (the malformed URL turned into a real outbound request in the repro).
- [x] fix: abort a `responseType: 'stream'` response when the caller's `AbortSignal` fires after emission. PR #38, open. Once the stream has been handed back, calling `.abort()` on the signal passed as `config.signal` has no effect any more - the stream keeps flowing indefinitely instead of being destroyed with `ERR_CANCELED`, as axios does. Different from the already-documented RxJS-unsubscribe-after-emission difference (that one is about `HttpService`'s Observable, not the caller's own signal). Fixed: `HttpService.executeRequest` now keeps reacting to the user's signal for as long as the stream stays open (mirroring axios' own `config.signal.addEventListener('abort', ...)`, removed only once the stream itself finishes), and `wrapStreamCancellation` translates the resulting abort into a real `CanceledError`. Zero-cost when no signal is passed. Found triaging the same expected-failures file, confirmed with a repro against the built package (the stream never closed after `.abort()`).
- [x] fix: corrupt/truncated compressed body gives a raw, unwrapped zlib error. PR #38, open. `axios-response.adapter.ts`'s catch block (`if (isMetered || body.bodyUsed) throw error;`) let a genuine decode failure (e.g. `Z_DATA_ERROR`) through unwrapped, with no `isAxiosError`/`.config`/`.request`, for a buffered response - and a `responseType: 'stream'` consumer got it raw too, with no shaping at all. Fixed to match axios' own `AxiosError.from(err, null, config, lastRequest, response)` (buffered path, checked against real axios 1.20's `handleStreamError`) for both cases - the stream case is an improvement over axios itself, which leaves it unwrapped there. Scoped to an actual zlib error code (`Z_*`) so a genuine network error (e.g. a dropped socket) that happens to occur while decompression is configured still gets `fail()`/`toAxiosError`'s own, more specific shaping, unchanged. Along the way, found and fixed a real regression this same PR's own corrupt-body wrapping would otherwise have introduced: axios' decompressors are flush-tolerant (`finishFlush: Z_SYNC_FLUSH` etc., `lib/adapters/http.js`'s `zlibOptions`/`brotliOptions`/`zstdOptions`), so an empty or merely-truncated (but structurally valid) compressed body never throws at all in axios - resolving with `''`/partial bytes instead. This library's decoders (`gunzipSync`/`createGunzip`/etc.) now use the same flush options, caught by the axios upstream conformance suite's "should not fail with an empty response (with|without) content-length header (Z_BUF_ERROR)" during this PR's own development (a genuine regression against a first, flush-less version of the corrupt-body wrap, fixed before merge).
- [x] fix: `transitional.silentJSONParsing: false`. PR #38, open. Not previously honoured (the type accepted it, but the doc comment said outright it described the default parsing this library already did unconditionally). Now, combined with `responseType: 'json'`, a `JSON.parse` failure throws `ERR_BAD_RESPONSE` with `response.data` the raw text, matching axios' exact condition (`strictJSONParsing = !silentJSONParsing && responseType === 'json'` - every other `responseType`, including the plain default, stays silent and zero-cost regardless of this flag). Added `transitional` to `AxiosRefDefaults`/`DEFAULTS_PASSTHROUGH_KEYS` (an intentional, additive API change - `npm run api:update` run) so it now also works on `axiosRef.defaults.transitional` and at module level, request > `axiosRef.defaults` > module, the same precedence as every other passthrough default (previously only request > module).
- [x] decide: `HttpService.onModuleDestroy` waits forever for an unconsumed `responseType: 'stream'` response (`dispatcher.close()` is graceful). PR #41. **Owner decision: `close()` with a grace period, then force-abort.** `closeDispatcher` now races `Dispatcher#close()` against an internal, non-public `DEFAULT_SHUTDOWN_GRACE_MS` (5s - long enough for a normal slow response to finish gracefully, short enough not to stall shutdown; overridable only for this package's own tests via an undocumented env var, never a public option). Past the grace period, everything still outstanding is force-aborted. **Real finding during implementation**: the naive "then call `dispatcher.destroy()`" reading of the decision doesn't actually work - confirmed directly against undici's own `lib/dispatcher/agent.js`: `Agent#close()` (and `ProxyAgent`/`EnvHttpProxyAgent`, both built on the same `Agent`) clears its per-origin client registry *synchronously*, the instant `close()` is called, before the returned promise ever settles; a `destroy()` call on that same instance afterward therefore has nothing left to reach and silently no-ops (verified with a real server: the socket stayed open indefinitely with no fix beyond a bare `destroy()` fallback). The actual fix: `HttpService` now tracks each still-open `responseType: 'stream'` body's own per-request abort signal (`trackOpenStream`, keyed by dispatcher, populated from `executeRequest`'s existing `RequestAbortSignal`/undici dispatch-`signal` wiring - the same mechanism the "abort after emission" item above already uses), and the grace-period fallback aborts those directly - which *does* force-close the connection (confirmed empirically: a zero-listener body reacts to it with no unhandled `'error'`, matching undici's `BodyReadable` behaviour) - plus still calls `dispatcher.destroy()` too, harmlessly, for whatever it can still reach. Scoped to `responseType: 'stream'` only (zero cost otherwise): every other response shape is always fully drained by this library itself, so it can never be genuinely "abandoned" the way a stream can - `trackOpenStream` is a no-op call outside that one path. Every owned dispatcher (default `Agent`, module-built dispatcher, every cached `socketPath` `Agent`) closes concurrently (`Promise.all`, unchanged), so N cached `socketPath` Agents don't add up to N times the grace period. The timer is `unref()`d and cleared the instant `close()` wins, so a clean shutdown with nothing in flight isn't delayed at all. A user-supplied `dispatcher` is still never touched (unchanged). No public API change - `npm run api:check` passes with the report untouched. Tests: 4 new e2e cases with real servers/sockets (`tests/shutdown-grace.e2e.spec.ts`) - an abandoned stream bounded within grace + margin with the server observing the socket close, a normal in-flight request still completing successfully during shutdown, a clean shutdown resolving immediately, and a user-supplied dispatcher never closed/destroyed even with an abandoned stream left on it; every one hangs (or times out) against the pre-fix code. Full `test:jest` (1025/1025, Node 24), lint/format/typecheck/typecheck:compat/build/api:check all green. Perf (`benchmarks/micro/compare.js`, Node 24, 5 rounds × 2s, paired against a fresh `claude/v1.0.0` worktree build - none of the benchmarked scenarios use `responseType: 'stream'`, so no change was expected): get -4.9%, post -2.6%, config -1.3%, error -16.1%, interceptors +9.5% - all within budget (10%/15%). Docs: `docs/axios-supported-options.md` (the `signal` row's "delays `onModuleDestroy`" note, and the dispatcher-closing note in "Module-level axios options"), `docs/http/http.service.md` (`OnModuleDestroy` section), `docs/guides/configuration.md`, `docs/http/http.module.md`. Changeset: `minor` (`.changeset/shutdown-grace.md`).
- [x] docs: update the compatibility page and the migration guide to match whatever differences remain. (PR #32)

- [x] investigate: axios' "should able to cancel multiple requests with CancelToken" upstream test fails on every Actions run but passes locally. PR #37, merged. **Root-caused, not the premise as stated**: the test fails deterministically in *every* environment, not just CI - confirmed with an isolated repro (no server, no timing) against the built package, and against the real GitHub Actions job log for this exact test (2026-09-26, run #16 "axios (axiosRef as the instance)"), both giving the identical `TypeError: Cannot read properties of undefined (reading 'source')` at `axios.CancelToken.source()`. Cause: the upstream harness's strategy (a) (`tests/upstream/axios/adapters/axiosref-instance.mjs`) substitutes this package's own `axiosRef` for axios' *entire* default export (so the unmodified upstream spec exercises axiosRef's real interceptor/config-normalization pipeline) - and `axiosRef` has no `.CancelToken` static, since that legacy top-level class was deliberately dropped in an earlier API-trim decision (axios itself deprecates it in favour of `AbortSignal`; this package's *shape*-level `config.cancelToken` support - `AxiosCancelTokenLike`, `resolveSignal` in `axios-request.adapter.ts` - was never touched and still works). Confirmed there's no real cancellation bug: strategy (b) (the real `axios` package, only its transport adapter swapped) has no expected-failure entry for this test at all, since `axios.CancelToken` there is real axios' own class; a 20-iteration stress repro of the exact "5 concurrent requests, one shared `cancelToken`, cancel synchronously right after they start" scenario (via this package's own `AxiosCancelTokenLike` shape, bypassing the missing class) passed every time. Pinned as a permanent differential test against `@nestjs/axios` (`tests/axios-compatibility-matrix.e2e.spec.ts`, "cancels multiple in-flight requests sharing one cancelToken"). `tests/upstream/axios/expected-failures.strategy-a.json`'s entry rewritten from "cause not isolated" to this root-caused harness-limitation explanation; kept as an expected failure (nothing to fix in `src/` - this package doesn't reproduce the legacy `CancelToken` *class*, only its instance shape), matching the same-suite hard-exclude precedent for "should support cancel". Also (same PR): `tests/upstream/lib/conformance.mjs` now prints the first line of a new failure's message into the job summary, so a future case like this is diagnosable without pulling the full job log.
### Phase 3: package quality and API (see `plan/reports/package-quality.md`)

- [x] `src/version.ts`: replace the runtime `require('../package.json')` with a build-time constant, so bundlers that prune non-JS files don't break it (PR #16 review). PR #27, merged.
- [x] Resource cleanup: `OnModuleDestroy` closes the dispatchers the module created. The static module's default options become a factory. PR #26, merged.
- [x] Option mapping: PR #26 merged the two sub-items below; the `__`-casts sub-item is done, PR #27, merged.
  - ~~replace the `__` casts with a typed resolved config~~ - done by `claude/api-trim`: one internal `ResolvedModuleConfig` (`src/modules/http/internal/resolved-config.ts`), produced once by `mapAxiosConfigToUndici` and consumed by `HttpService`, replaces the `__agentOptions`/`__proxyAgent` `as any` reads.
  - ~~strip axios-only keys (such as `auth`) before calling undici~~ - done by `claude/dispatcher-lifecycle`: `auth`/`httpAgent`/`httpsAgent`/`proxy`/`httpVersion`/`cookieJar`/`withCredentials`/`xsrfCookieName`/`xsrfHeaderName` and the internal `__` keys are stripped once, at setup, into a `dispatchBaseOptions` object, instead of leaking into every request's undici dispatch options.
  - ~~fix `socketPath`~~, ~~the dead `__axiosCompat.baseURL` branch~~, ~~`keepAlive`~~ and ~~the ignored `httpsAgent` TLS options~~ - done by `claude/fix-transport-options`. Still open: a `baseURL` combined with a `UrlObject` URL (not a string) silently ignores `baseURL` instead of crashing (`normalizeAxiosRequest` only combines a string URL) - better than before, not fully correct.
  - ~~use Nest `Logger` instead of console~~ - done by `claude/dispatcher-lifecycle`: `http.module.ts`'s axios-compatibility warnings now go through a `Logger` with context `HttpModule`.
- [x] Trim the public API (decided: remove what loses nothing), then commit an api-extractor report and check it in CI. PR #27, merged.
- [x] HttpService members: `setDispatcher` (rename), internal `setInterceptors`, a real `interceptorCount`, a read-only `undiciRef`. PR #26, merged.
- [x] `strict` TypeScript in `tsconfig.build.json` (7 errors) - done by `claude/types-axios-interop` (only 4 remained by then; the main `tsconfig.json`, editor/ts-jest/tests, stays loose).
- [x] Duplicate undici copy: plain requests should also use an Agent from this package's undici copy. Per the "breaking changes are fine" decision, this is a per-service default `Agent`, done together with `OnModuleDestroy` resource cleanup. PR #26, merged.

### Phase 4: performance (see `plan/reports/performance.md`)

Measured: library overhead is small. Per-request client CPU is 41 µs, vs 35 µs for raw undici and 510–560 µs for @nestjs/axios. No memory leak, and keep-alive reuse works.

- [x] ★ **perf: request-path pass (next after the default-headers PR).** PR #17, merged. `get` is about 8-11% cheaper than before, but it is not yet within +10% of 0.6.1 (local measurements +10% to +32%, noisy).
  - Follow-up (before 1.0): reduce allocations per request and add a cheaper decode path. Profile shows the gap comes from GC, undici validating the 3 default headers, axios decoding and abort wiring, not from header merging.
  - Why: each Phase 2 fix passes the CPU-per-request check on its own, but the costs add up. `get` went +7.6% in #15, then about +8% in the default-headers PR.
  - Record the CPU per request against 0.6.1 as the baseline, and keep `get` within +10% of it for 1.0.
  - Options:
    - cache the interceptor handler chain until interceptors change
    - pre-merge default, module and method headers once, and invalidate that when `axiosRef.defaults` changes
    - skip config re-normalisation in `defer()` when no interceptors are registered
    - merge the promise `.then()` calls
    - avoid `AxiosHeaders` Proxy access on the hot path
- [x] ★ **Abort the request on unsubscribe.** Done in PR #15. This is the same item as in phase 2. It costs 1–3 µs per request.
- [x] ★ **bench: a fair app set.** PR #29, merged.
  - Configurations: `@nestjs/axios` and nestjs-axios-undici in identical apps, each with and without the same axiosRef interceptor (no stdout logging), plus raw undici as the floor.
  - The mock backend runs with `logger: false`.
  - Drop `nestjs-fastify-undici`, the upstream `nestjs-undici` dependency, and the 3 separate compose files.
  - Decided: both Express and Fastify.
  - Update the k6 script and the report generator to match.
- [x] ★ **A light end-to-end A/B check without Docker or k6** that runs on PRs and releases and publishes the throughput ratio. PR #29, merged. Prototype: `plan/prototypes/perf/e2e/`, not run yet; it needs `@nestjs/platform-fastify` and `autocannon`.
- [ ] Later: cheaper request-adapter paths (saves 2–8 µs), a streaming `maxContentLength` check, instruction-count benchmarks, and the full k6 run on the version PR before publish.

### Phase 5: docs (see `plan/reports/docs-critic.md`)

- [x] bench: rewrite the report generator. The README and docs headline are generated from the same numbers, as ratios, with no caveats. PR #29, merged.
- [x] docs: present the package on its own terms. Delete `docs/features.md`, trim the migration guide, keep the fork mention only in the README Credits. (PR #32)
- [x] docs: fix inaccurate or stale statements, keep the dispatcher snippet in one place, and use consistent terminology. (PR #32)
- [x] bench: turn `benchmarks/README.md` into a short how-to-run page. PR #29, merged.

### Phase 6: 1.0.0 release

- [ ] Every must-have item above is merged into `claude/v1.0.0`.
- [x] Delete `plan/prototypes/`. Add a major changeset with migration notes from 0.6. (`claude/release-prep`: `.changeset/v1-stable.md` links the migration guide's "Upgrading from 0.6.x"; three stale migration-guide links in older changesets repointed at the docs site.)
- [ ] **CodeRabbit review of the master PR (#8)**, once everything else in this phase is done.
  - Trigger it with a PR comment: `@coderabbitai full review`. Automatic reviews are off because the repo has fewer than 10 stars and the base branch is non-default.
  - Handle every finding. Fix it, or reply with the reason it stays as is, then resolve the thread.
  - Fixes go in small PRs into `claude/v1.0.0`, or straight onto it for trivial ones. Re-run the review until it is clean.
  - `claude/coderabbit-fixes` (PR #43): 4 runtime-code findings from a CodeRabbit review, verified against `claude/v1.0.0` with a repro/failing test each before fixing (item 5, the `index.html` throughput wording, is out of scope - PR #42 rewrites that copy):
    - Security: `toAxiosLikeResponse`'s `transformResponse` path (text/json/default `responseType`) called `readText` without `maxContentLength`, so a custom `transformResponse` bypassed the size limit entirely (buffering, and fully decompressing, the whole body first). Fixed by threading `maxContentLength` through; every other `readText`/`readBuffer`/`readBodyAsResponseType` call site was already correct.
    - `readText`'s unlimited-decompression branch called `body.arrayBuffer()` directly, so a metered body (`onDownloadProgress`/download `maxRate` - a plain `Transform`, no `.arrayBuffer()`) rejected a compressed response with a `TypeError` instead of resolving. Fixed via `bodyArrayBuffer(body)`, matching `readBuffer`.
    - `isAxiosRequestConfig` required a `url` key to detect the `request(config)`/`axiosRef(config)` form, so a config with no `url` (`baseURL` alone, or one an interceptor fills in) was misdetected as a raw URL and stringified to `"[object Object]"`. Fixed: a missing `url` key alone no longer means "this is a URL" - only an object structurally shaped like a `UrlObject` does, matching axios' own `typeof configOrUrl === 'string'` check while keeping this library's own `request(url, options)`/`get(url, config)` `UrlObject` support.
    - `axiosRef.create(config)` (and a runtime `axiosRef.defaults.X = ...` assignment) silently dropped `auth`/`maxContentLength`/`maxBodyLength`/`timeoutErrorMessage`/`decompress`/`socketPath`/`allowAbsoluteUrls`/`beforeRedirect` - read only from module/instance options, never `defaults`. All 8 added to `DEFAULTS_PASSTHROUGH_KEYS`/`AxiosRefDefaults`, resolved request > `axiosRef.defaults` > module, same as `timeout`/`maxRedirects`. Audited the rest of axios' `mergeConfig` surface: `httpAgent`/`httpsAgent`/`proxy`/`httpVersion`/`cookieJar` stay module(`HttpService`)-level only by design (`create()` shares the host's dispatcher, built once at construction); `signal` isn't mergeable in axios either; `xsrfCookieName`/`xsrfHeaderName` are a no-op at every level; `insecureHTTPParser`/`lookup`/`family` aren't implemented at all.
    - Perf (`compare.js`, base = `origin/claude/v1.0.0`, 5 rounds x 3s, Node 24): get +1.9%, post +9.2%, config +9.3%, error -8.6%, interceptors +2.5% - all within budget (10%/15%).
  - `claude/coderabbit-tests-docs` (PR #47): 18 findings from a CodeRabbit review of the tests/, docs/, docsify/ and examples/ slice of the 1.0 diff (CodeRabbit reviewed without `src/`), each verified against `src/` on `claude/v1.0.0` before deciding:
    - Valid, fixed: the axios `QUERY` version threshold (own CHANGELOG confirms v1.16.0, not v1.13, in `docs/axios-supported-options.md`/`docs/http/http.service.md`); the bare-`HttpModule` singleton-isolation claim in `docs/http/http.module.md` (Nest shares one `HttpService` across every feature module that imports the bare class; only `.register()`/`.registerAsync()` calls each get their own); two stale differential-test comments left over from fixed gaps (`module.diff.spec.ts`'s `transformResponse` comment, `request.diff.spec.ts`'s undefined-body/`Blob` `Content-Type` comment); the apostrophe case's name typo and `knownDifference` label in `request.diff.spec.ts`; the stale "axiosRef isn't (yet) callable" docstring in `retry-once.diff.spec.ts` (it's a real callable function now); the `undiciRef` "__ keys" assertion in `dispatcher-lifecycle.e2e.spec.ts` (now checks every `__`-prefixed key, not just `__resolvedConfig`); the unclosed `TestingModule` in `observable-abort.e2e.spec.ts` (added `afterEach`); the vacuous same-host-redirect header assertions in `redirects.e2e.spec.ts` (now point at `/post-redirect`→`/echo` and assert the header value); the vacuous `rejects.toBeDefined()` in `transport-tls.e2e.spec.ts` (now matches the same cert-error codes as the sibling test); and, in the upstream axios conformance harness's bare adapter (`tests/upstream/axios/adapters/undici-adapter.cjs`, not `src/`), a real `maxBodyLength` operator-precedence bug (ternary binds looser than `&&`, so every `Buffer` body failed the default `maxBodyLength: -1`) and a premature `destroyDispatcher()` for `responseType: 'stream'` (deferred to the stream's `close` event). Re-ran `node tests/upstream/axios/run.mjs --strategy b`: 0 new failures, 6 fixed, removed from `expected-failures.strategy-b.json` (`should support buffers`, `should support streams`, `should destroy the response stream with an error on request stream destroying`, `should be able to abort the response stream`, and both `Rate limit` cases). Every strengthened test was checked to fail against a temporarily reverted/broken `src`, then restored.
    - Not valid, left as is (reason recorded in each reply): the benchmarks.md regression-metric wording (already describes client CPU/req, matching `benchmarks/micro/compare.js`); the three timeout docs (`error-handling.md`/`configuration.md`/`migration-guide.md` already describe the real deadline timer `src/modules/http/services/http.service.ts` implements, not the old idle-timeout-only behaviour); `axios-mock-adapter`/`axios-retry`/`axios-auth-refresh` as devDependencies (already declared and installed; typecheck and the tests pass); the `axios-auth-refresh` default-vs-named-export claim (the pinned v5.0.2 exports both); the zstd Node-version floor in `response.diff.spec.ts` and `corrupt-compressed-body.e2e.spec.ts` (both findings assumed `engines.node` is `>=22.12.0`; it is actually `>=22.17.0`, already above the 22.15.0 zstd floor).
    - Full `test:jest` 1043/1043 on Node 24; lint/format:check/typecheck/build/`test:examples` all green.
- [ ] Owner sign-off. Merge the master PR into `main`. Merge the "version packages" PR. Publish 1.0.0 through trusted publishing.
- [ ] Add git tags / GitHub releases for 0.6.0 (and check 0.6.1).

## Log

- 2026-09-25: Explorations done for automation, package quality, axios compatibility and docs (reports in `plan/reports/`). Performance exploration done too. Workers A and B started. Created `claude/v1.0.0` and this plan.
- 2026-09-25: PR #9 merged. PR #10 approved; fixed the CI pack step and formatting, waiting for green.
- 2026-09-25: PR #10 merged (consumer matrix green on Node 22.17.0/22.19.0/22/24/26). Worker C started (`claude/api-parity-checks`).
- 2026-09-25: PR #11 (C) merged. PR #12 (D, 183 differential scenarios, 77 known differences) open, in review.
- 2026-09-25: PR #12 (D) merged; the differential tests pass on Nest 10/11/12. Worker E (perf check) next.
- 2026-09-25: PR #13 (E, `claude/perf-check`) merged: CPU-per-request regression check, 5 scenarios, pooled re-measure, 1.5x threshold for interceptors. Noise and sensitivity measured, see item E above.
- 2026-09-25: Owner decisions recorded: withCredentials becomes a no-op with an explicit cookieJar; redirects follow axios (21 by default); trim the API where nothing is lost; benchmarks cover Express and Fastify.
- 2026-09-25: `claude/fix-response-decoding` (phase 2 fix(response)) open: `+json` content types, JSON-looking text/no-content-type/octet-stream/javascript/x-www-form-urlencoded/svg decode like axios, gzip/br/deflate decompression honouring `decompress`, `blob` as a string, `statusText` from the real reason phrase. Fixes all 19 tracked `knownDifference` cases in `response.diff.spec.ts`.
- 2026-09-25: PR #14 merged: response decoding matches axios (19 known differences fixed, 58 left; corrupt gzip now rejects). The abort-on-unsubscribe worker is running.
- 2026-09-25: PR open (`claude/fix-observable-abort`): `fix(observable): abort on unsubscribe and run request interceptors per subscription`. `HttpService.request()` now wraps its body in `defer()`, so it stays cold (no work before subscribe) and re-runs config normalization plus the axiosRef request interceptors on every subscription. `executeRequest` creates a per-subscription `AbortController`, combined with any user `signal`/`cancelToken` via a plain listener (no `AbortSignal.any`), and aborts it on unsubscribe unless the response (or, for `responseType: 'stream'`, the headers) has already been emitted. 3 of the 4 `fix(observable)` known-difference cases now pass; the 4th was a mistag (undici's idle-timer timeout vs. axios' deadline timeout) and was retagged to `fix(errors)`. Perf check and full Node 24 verification green.
- 2026-09-25: PR #15 merged: unsubscribing aborts the request, and interceptors run per subscription (55 known differences left). CI CPU/req vs base: get +7.6%, post -1.6%, config +5.3%, error +3.0%, interceptors +2.7%. Still 8.4x the rps of @nestjs/axios on get.
- 2026-09-25: PR open (`claude/feat-default-headers`): `feat: axios default headers`. Requests now send `Accept`, `User-Agent` (`nestjs-axios-undici/<version>`, read from `package.json` once at load) and `Accept-Encoding` (`gzip, deflate, br`, no `compress`, only when decompression is on), seeded into `axiosRef.defaults.headers.common`; module and per-request headers override them, and `undefined`/`null` removes one. `register({ headers: { common, post, ... } })` is now flattened per method at setup (item 11), for both module headers and `axiosRef.defaults.headers`. A POST/PUT/PATCH with no body now gets the default `Content-Type: application/x-www-form-urlencoded` (previously skipped when `data` was `undefined`), and a `Blob` body's own `type` is used instead of that default. Fixes all 12 tracked `knownDifference` cases (9 in `request.diff.spec.ts`, 2 in `response.diff.spec.ts`, 1 mistagged under `fix(config)` in `module.diff.spec.ts`). The perf check initially failed on `interceptors` (+15.7%, over the 15% threshold): every request now carries 3 default headers, and the axios-interceptor adapter was rebuilding its `AxiosHeaders` wrapper one `.set()` call at a time *after* it was already Proxy-wrapped (each call paying Proxy-trap + rebind cost); populating it through the constructor instead (before the Proxy wrap) fixed it (interceptors +5.1% on the next run). CI CPU/req vs base: get +8.0%, post +5.4%, config -8.7%, error +8.1%, interceptors +5.1%.
- 2026-09-25: PR #16 merged: axios default headers, method-keyed module headers and Content-Type defaults (43 known differences left). Module headers now take precedence over axiosRef.defaults (documented). Perf-pass worker started.
- 2026-09-25: PR #17 open (`claude/perf-request-path`): `perf: cache merged headers and the interceptor chain`. The default + module headers for each method are merged once and cached per `defaults` object. Each request re-validates the cache against its sources: the same objects, holding the same keys in the same order, with identical primitive values. Any other source shape skips the cache. The first version tracked writes through Proxies on `axiosRef.defaults.headers`, but review found holes: wholesale `defaults.headers` reassignment, module headers changed through `undiciRef`, `Object.defineProperty`, and held references to a replaced bucket. That version was dropped, and all four cases now have tests. With no per-request headers, the cached base is shallow-copied instead of re-merged. The interceptor handler chain is cached (rebuilt on add/replace), and `executeRequest` registers one `.then(onFulfilled, onRejected)`. `normalizeAxiosRequest` for a plain get: 1.8-2.4 µs → ~0.95 µs (0.6.1: ~0.8 µs). End-to-end CPU/req vs claude/v1.0.0: get about -8 to -11%. vs 0.6.1, get still measures +10% to +32% locally (this sandbox is very noisy). A profile puts the rest of the gap in features added since 0.6.1, not in the header merge: more GC; undici validating the 3 default headers; axios decoding; abort wiring. Follow-up: a cheaper decode path and fewer allocations per request (Phase 4).
- 2026-09-25: PR #17 merged: request-path perf pass (header cache that checks its sources on each request, cached interceptor chain, single `.then()`). CI all green, including the HttpService regression check. Next: refactor(axiosRef) one config pipeline.
- 2026-09-25: PR #18 open (`claude/refactor-axiosref-config`): one axios-shaped config object runs through the axiosRef interceptors, dispatch, `response.config` and `error.config`. Interceptors now run in axios order, `runWhen`/`synchronous` are honoured, and request/response transforms work. Known differences: 42 → 28 (all 11 REFACTOR cases, plus 3 cases that differed only in `method` casing). Review fixes:
  - `transformResponse` now skips streams and passes raw bytes for `arraybuffer`/`blob`; before this fix it decoded to text, corrupting binary data and breaking streams.
  - `config` is a plain own property on responses and errors, so it survives spread, `JSON.stringify` and `structuredClone`. Building it eagerly costs about 0.44 µs per request.
  - Header lookups on `AxiosHeaders` use its own `get`/`has`/`set` instead of scanning keys through the Proxy. This removed about 9 µs per request on the interceptor path (interceptors had been +27% vs base).

  Local CPU/req vs claude/v1.0.0: get +1% to +7%, post −1% to −3%, config −2% to +2%, error +2% to +10%, interceptors −5% to −7% (noisy sandbox; all within thresholds).
- 2026-09-25: PR #18 merged: one axios config pipeline, axios interceptor order, `runWhen`/`synchronous`, transforms, and `config` as an own property (28 known differences left). CI all green, including the HttpService regression check. Next: follow redirects by default (21).
- 2026-09-25: PR open (`claude/fix-redirects`): `fix: follow redirects by default (21)`. `executeRequest` handles 3xx manually on the response (a `statusCode`/`Location` check, cheap on every other response) instead of composing undici's own redirect interceptor onto every request. New `adapters/redirect.adapter.ts` ports follow-redirects' exact rules: default `maxRedirects` 21, `maxRedirects: 0` returns the 3xx as-is through `validateStatus`; 301/302 turn `POST` into `GET`, 303 turns anything but `HEAD` into `GET` (both drop the body and `Content-*` headers), 307/308 keep method and body; `Authorization`/`Cookie`/`Proxy-Authorization` are dropped across a protocol downgrade or a host (including port) change; exceeding the limit rejects with `ERR_FR_TOO_MANY_REDIRECTS` ("Maximum number of redirects exceeded", no `response`); `beforeRedirect(options, responseDetails, requestDetails)` is supported at request and module level; a streamed body that a kept redirect would need to resend (307/308, or a non-POST 301/302) now rejects clearly with `ERR_FR_REDIRECTION_FAILURE` instead of sending a broken request. `response.request.res.responseUrl` is set to the final hop's URL once a redirect was followed. This also removes the `compatibleGlobalDispatcher`/dispatcher-compose machinery entirely: manual redirects re-issue `request()` per hop against whatever dispatcher is active, so they work on Node 22's bundled-undici-as-global-dispatcher case without needing same-copy `.compose()` (verified with a real Node 22.22.2 binary, both the normal path and a foreign dispatcher whose `.compose()` throws).
  - Differential harness: 187 → 192 scenarios (5 new redirect cases: the default limit 21 vs 22, `maxRedirects: 0`, a relative `Location` without a leading slash, a cross-host `Authorization`/`Cookie` drop, `beforeRedirect`), known differences 29 → 27 (`fix: follow redirects by default (21)`: 1 → 0; `fix(errors): match axios errors`: 20 → 19, the redirect-loop-exceeds-maxRedirects case matched as a side effect of the new error shape). `tests/redirects.e2e.spec.ts` and `tests/axios-compatibility-matrix.e2e.spec.ts` updated for the new default (both previously asserted the old "not followed by default" behaviour).
  - Benchmark (Node 24, 5 rounds × 2s, this sandbox): get -1.2%, post +2.2%, config -1.0%, error -0.5%, interceptors +8.5% (threshold 15%) - all pass, no re-run needed.
- 2026-09-25: PR #19 review fixes. `timeout` is now one budget for the whole redirect chain, as in axios; before, each hop got a fresh `timeout`, so a 5-hop chain of 80 ms hops passed a 150 ms timeout. `axiosRef.defaults.maxRedirects` is now honoured (request > defaults > module). A test was added for each, and each fails without its fix.
- 2026-09-25: PR #19 merged: requests follow redirects by default (21), `timeout` covers the whole redirect chain, and `axiosRef.defaults.maxRedirects` is honoured (27 known differences left). CI all green, including the Node 22 rows and the HttpService regression check. Next: fix(config) transport options.
- 2026-09-25: PR #20 open (`claude/fix-transport-options`): `fix(config): transport options`. `httpsAgent`'s TLS options (`ca`/`cert`/`key`/`pfx`/`passphrase`/`rejectUnauthorized`/`servername`/`ciphers`/`minVersion`/`maxVersion`, read off `agent.options`, matching what axios' own http adapter benefits from - it never reads these off `config` directly) now map onto undici's `Agent({ connect: {...} })`; `keepAlive`/`maxSockets` are fixed too (`keepAlive` used to be assigned to a `pipelining` field that was never actually used to construct an `Agent`). `socketPath` now works, at module and request level, via `Agent({ connect: { socketPath } })` cached per path (a `Map` on `HttpService`) - confirmed the request URL's host still becomes the `Host` header, and a bad path fails exactly like axios (`ENOENT`, same message). The dead `__axiosCompat.baseURL` branch and the `unix:`-URL-rewrite `__socketPath` mechanism are gone. `HTTP_PROXY`/`HTTPS_PROXY`/`NO_PROXY` (case-insensitive) are now read once at setup via undici's `EnvHttpProxyAgent`, only when no `dispatcher`/`proxy`/`httpAgent`/`httpsAgent`/`socketPath` is configured; `proxy: false` opts out. An explicit `proxy: {...}` and the env-var path both construct their `ProxyAgent`/`EnvHttpProxyAgent` with `proxyTunnel: false`, matching axios: a plain HTTP target is forwarded to the proxy in absolute form, not CONNECT-tunnelled (undici's own default tunnels everything, which a plain-http forward-proxy test server doesn't speak). `httpVersion: 2` maps to `Agent({ allowH2: true })`. Precedence fix: `setupDispatcher` now returns immediately when a `dispatcher` was passed directly in module options, so it can never be silently overridden by an auto-built one (the pre-existing bug); `executeRequest`'s dispatcher resolution was reordered so a per-request `dispatcher` also wins over the module's auto-built one.
  - Differential harness: 193 scenarios unchanged, known differences 27 → 26 (`fix(config): transport options`: 1 → 0, the `socketPath` case). Counted with `jest --json` + a script (the differential file's dynamic `.get(name)`-generated cases meant a plain source-line grep under/over-counts).
  - New tests: `src/modules/http/adapters/__tests__/axios-config.adapter.spec.ts` (13, unit, `resolveAgentOptions`/mapping), `tests/transport-tls.e2e.spec.ts` (5, a real `https.Server` with a checked-in self-signed fixture cert/key under `tests/fixtures/tls/` - default rejects, `rejectUnauthorized:false` and `ca` both work, an explicit `dispatcher` wins over `httpsAgent`), `tests/transport-socket-path.e2e.spec.ts` (4, a real unix-socket `http.Server` in the OS temp dir), `tests/transport-proxy.e2e.spec.ts` (9, a real forwarding-proxy `http.Server`; saves/restores `HTTP_PROXY`/`HTTPS_PROXY`/`NO_PROXY`/lower-case around every test so this sandbox's own proxy env vars can't leak in or out), `tests/transport-http2.e2e.spec.ts` (2, `http2.createSecureServer({ allowHTTP1: true })` with the same fixture cert). Verified against the pre-fix worktree: the TLS and socketPath suites don't just fail there, they crash the Jest worker outright (an unrelated pre-existing `AxiosHeaders.toJSON` bug chokes trying to serialise the resulting `DEPTH_ZERO_SELF_SIGNED_CERT`/`ENOENT` errors back from the worker); the proxy suite's explicit-proxy/env-proxy cases hang until timeout without `proxyTunnel: false` (confirmed directly, see the log entry's proxy paragraph below), and the http2 cases fail outright since `httpVersion` was never read before.
  - Risk/decision: reading `HTTP_PROXY`/`HTTPS_PROXY` by default is a behaviour change (documented in the changeset and `docs/axios-supported-options.md`) - it matches axios, but a caller whose environment sets these (this sandbox does) now has plain `HttpModule.register({})` calls proxied unless `NO_PROXY` covers the target or `proxy: false` is passed. Verified directly: with `HTTP_PROXY` set and none of `proxy`/`dispatcher`/`httpAgent`/`httpsAgent`/`socketPath` configured, a request to `127.0.0.1` *is* proxied by default (see `transport-proxy.e2e.spec.ts`, "with no HTTP_PROXY/HTTPS_PROXY set" case for the converse). The benchmark run explicitly unset `HTTP_PROXY`/`HTTPS_PROXY`/lower-case first, since this sandbox has them set and the default behaviour would otherwise route the benchmark's own 127.0.0.1 traffic through it.
  - Left undone (out of scope for this item, tracked in phase 3 "Option mapping"/"Resource cleanup"): axios-only keys (`auth`, `httpAgent`, `httpsAgent`, `proxy`, `httpVersion`) still leak, harmlessly, into `instanceOptions`/per-request dispatch options instead of being stripped; the `__`-prefixed casts are only partially typed; dispatchers this module builds (TLS/socketPath/proxy/env-proxy/http2 `Agent`s) are still never closed on `app.close()` (no `OnModuleDestroy` exists yet to hook into - already tracked as its own phase 3 item); a `baseURL` combined with a `UrlObject` (not string) URL still silently ignores `baseURL` (no longer crashes with `/[object%20Object]`, since the dead branch that caused it is gone, but `normalizeAxiosRequest`'s live baseURL-combining path only handles a string URL).
  - Benchmark (Node 24, 5 rounds × 2s, this sandbox, `HTTP_PROXY`/`HTTPS_PROXY` explicitly unset): get +1.5%, post -4.3%, config +3.6%, error +1.7%, interceptors -3.5% (threshold 15%) - all pass, no re-run needed.
- 2026-09-25: PR #20 review fixes:
  - A per-request `socketPath` is honoured when the module builds its own dispatcher. Before, the fast path copied the module dispatcher into every request, so the request silently went to the wrong destination.
  - `httpsAgent` TLS options apply through an explicit proxy (`requestTls`), as axios forwards them.
  - A custom `httpAgent`/`httpsAgent` no longer turns off the env proxy (axios reads it anyway).
  - The per-path `socketPath` Agent cache is capped at 32, and the oldest is closed.
  - `proxy.protocol` without the trailing colon (`'http'`) works, as in axios.
  - A Jest setup file clears the proxy env vars, so the suite no longer depends on the machine's proxy settings.
- 2026-09-25: PR #20 merged: transport options (`httpsAgent` TLS, `socketPath`, explicit and env proxies, `httpVersion: 2`), 26 known differences left. CI initially failed on the HTTP/2 test: undici 8 negotiates h2 by default (built dispatchers now set `allowH2` explicitly), and on Node 22 open h2 sessions hung `server.close()` in the test. Note: CodeRabbit skips PRs into `claude/v1.0.0` ("reviews are disabled for this base branch"); the Phase 6 review has to run on master PR #8 into main. Next: withCredentials no-op + cookieJar.
- 2026-09-25: PR #21 open (`claude/cookie-jar`): `breaking: withCredentials becomes a no-op; add cookieJar`. `withCredentials` no longer creates any dispatcher or cookie jar (accepted, kept in the types, otherwise ignored - matches axios on Node.js). Cookie handling is opt-in through a new module-level `cookieJar` option: only a `tough-cookie` `CookieJar` *instance* is accepted (never `true` - a per-service shorthand would just reintroduce the shared-jar leak with a different trigger, so the caller owns the jar's scope explicitly), and it wraps whatever dispatcher `setupDispatcher` already built (proxy/TLS/socketPath), same as the old `withCredentials` did. No per-request `cookieJar` (documented as unsupported; skipped rather than cached in a `WeakMap`, since building a `CookieAgent` per jar is only worth doing once per `HttpService`). `http-cookie-agent`/`tough-cookie` moved from `dependencies` to optional `peerDependencies` (`peerDependenciesMeta.optional`), `require()`d lazily from inside `HttpService.setupDispatcher`, only when `cookieJar` is set; a missing peer throws a clear error instead of `Cannot find module`. `scripts/consumer/check-declared-deps.cjs` now parses runtime `.js` files with the full TypeScript AST (not just the lightweight preprocessor) to tell a lazy, function-nested `require()` of an optional peer (allowed) from a top-level one (still flagged, since that would crash `require()`ing the package for anyone without the peer installed); `scripts/consumer/shared.mjs`'s `packageWithPeers()` now skips optional peers when building each matrix combo's dependencies, so the consumer matrix continues to prove the package works *without* them installed - `fixtures/scenario.cjs`'s `withCredentials: true` case was updated to assert cookies are NOT persisted, needing no cookie deps in the fixture project at all. Both `http-cookie-agent` and `tough-cookie` are genuinely CJS-requirable on every supported Node version (no `require(esm)` needed): `http-cookie-agent`'s package has no `"type": "module"`, and `tough-cookie` ships dual CJS/ESM exports with `require` resolving to `dist/index.cjs`.
  - Differential harness: 193 scenarios unchanged, known differences 26 → 25 (`breaking: withCredentials becomes a no-op; add cookieJar`: 1 → 0, `module.diff.spec.ts`'s `withCredentials: cookies are NOT persisted by axios in Node` case).
  - Tests: updated the existing cookie test (`tests/axios-options-compatibility.e2e.spec.ts`) to assert the `withCredentials` no-op instead of cookie persistence; added `tests/transport-cookie-jar.e2e.spec.ts` (cookie storage/replay through a caller-supplied jar, two services with separate jars not sharing cookies, a jar shared on purpose across two services *does* share, `cookieJar` combined with `httpAgent`, a per-request `cookieJar` being silently ignored, and the missing-peer error via `jest.doMock('http-cookie-agent/undici', ...)` inside `jest.isolateModules` - remembering to `jest.dontMock` afterwards, since `isolateModules` doesn't undo a `doMock` registration on its own). All test servers here bind to `127.0.0.1`, including the pre-existing ones in `axios-options-compatibility.e2e.spec.ts` (previously `server.listen(0, ...)` + `localhost:` URLs).
  - Startup: `require('./lib')`, median of 15 alternating rounds against `claude/v1.0.0`: base 317.9ms → head 294.3ms (-23.6ms, -7.4%; this sandbox is noisy, per plan.md's own noise notes elsewhere). Directly timing just `require('http-cookie-agent/undici'); require('tough-cookie')` (isolated from `@nestjs/common`/rxjs/etc., which dominate and add noise to the `require('./lib')` figure): about 100ms - roughly matches the ~150ms estimate in `plan/reports/package-quality.md` item 7, now paid only by services that opt into `cookieJar`.
  - Perf check (`benchmarks/micro/compare.js`, base = `claude/v1.0.0`, 5 rounds × 2s): get -0.8%, post -3.0%, config +1.6%, error -4.4%, interceptors -3.9% - all well inside the 10%/15% thresholds.
  - Docs: README, `docs/axios-supported-options.md` (new "Cookies: `cookieJar`" section replacing "Cookies: `withCredentials`", plus table rows), `docs/migration-guide.md` (new "Cookies (`cookieJar`)" subsection under "Handling Special Cases", called out as a breaking change), `docs/guides/configuration.md`, `docs/features.md`, `docs/http/http.module.md`, `docs/http/http.service.md` (fixed a stale claim that `withCredentials` makes the module build its own dispatcher - `cookieJar` does that now), and `examples/axios-compatibility-features.ts` (now demonstrates `cookieJar` instead of `withCredentials`). Changeset: `minor` with a **BREAKING** note (`.changeset/cookie-jar-opt-in.md`) - consistent with this plan's stated policy ("1.0.0 is the major changeset added in the final step") and the precedent set by the proxy-env and redirects-by-default changes, both breaking and both shipped as `minor` pre-1.0.
  - Left undone: nothing required by this item. Not done (out of scope, tracked elsewhere): dispatchers this module creates, including a `CookieAgent`, still aren't closed on `app.close()` (phase 3, `OnModuleDestroy`); `docs/features.md` itself is still slated for deletion in phase 5.
- 2026-09-25: PR #21 review fixes:
  - `check-declared-deps.cjs` now also catches `import('x')` and `require.resolve('x')`. The AST rewrite had missed both, verified with an injected undeclared import.
  - A `cookieJar` that isn't a CookieJar now fails at setup with a clear error, instead of an opaque TypeError on the first request.
  - The docs note that a `cookieJar` next to an explicit `dispatcher` is ignored.
  - New test: `registerAsync` with a jar from a factory.
- 2026-09-25: Owner decision: breaking changes are fine before 1.0.0, so do things the right way (see Decisions). This affects the remaining types, API trim, HttpService members and default-dispatcher items.
- 2026-09-25: Owner offered to drop Node 22 / undici 7. Kept them, because nothing is simplified by dropping them now; recorded in Decisions.
- 2026-09-25: PR #21 merged: `withCredentials` is a no-op; cookies are opt-in via `cookieJar`; the cookie libs are lazy optional peers (25 known differences left). CI all green. Next: types: axios interop (strict types, per the breaking-changes-are-fine decision).
- 2026-09-25: PR #22 open (`claude/types-axios-interop`): `types: axios interop`. `HttpModuleOptions` is a real, strictly-typed interface (no `& any`/`Partial<any>`) - every mapped axios option and undici pass-through option spelled out; a typo (`{ timeuot: 5 }`) is now a compile error, the same as against `@nestjs/axios`'s own types. The four overlapping request-config types (`AxiosLikeRequestConfig`, `AxiosCompatibleRequestOptions`, `AxiosCompatibleRequestConfig`, `HttpRequestOptions`) are merged into one `AxiosLikeRequestConfig<D = any>`; added `InternalAxiosLikeRequestConfig<D>` (headers non-optional) for axiosRef interceptor configs/`response.config`/`error.config`. `get`/`delete`/`head`/`options`/`post`/`put`/`patch`/`request` all take a `D` generic; `post<T, D>` has a real second (body) type parameter. `AxiosHeaders` gained an index signature plus `concat`/`toString`/`normalize`/`getSetCookie` and the `get/set/has*` shorthand accessors. `HttpModule.registerAsync({})` (none of `useFactory`/`useClass`/`useExisting`) now throws a clear error instead of registering a broken provider (`provide: undefined`). `axios` is now an optional peerDependency; when installed, `AxiosError` links its prototype onto axios' own at module load (lazily, in a try/catch - measured ~95µs for the failed `require('axios')` when absent, no noticeable startup cost), so `instanceof axios.AxiosError` holds (not `instanceof axios.CanceledError` specifically - a prototype chain is linear, see `linkOptionalAxiosPeer`'s doc comment in `axios-error.ts`; use `isCancel()`). `strict` TypeScript is now on in `tsconfig.build.json` (only 4 of the reported 7 errors remained by now; fixed - `registerAsync`'s `useClass` narrowed before use, `validateStatus`/`socketPath` widened to accept `null`; the main `tsconfig.json` for editor/ts-jest/tests stays loose). `tests/types/`: removed the `@ts-expect-error` markers this item fixes (`post<T,D>`, `registerAsync(HttpModuleAsyncOptions)`, `config.headers` bracket/`.set()` in interceptors) and added the typo case (`case16`, now genuinely caught); the ones left (`HttpService`/`request()`, an `InternalAxiosRequestConfig`-typed callback, `axiosRef` as `AxiosInstance`) need a fuller `AxiosHeaders` class matching axios' own overloaded `set`/`get`/`toJSON`, left for "feat(axiosRef): make it a real axios instance". `response.headers` stays a plain object at runtime, not `AxiosHeaders` - measured (this library's `AxiosHeaders`, a typical response, 200k iterations) at ~955ns more per response just to construct, before the Proxy-trap cost on every later read, not worth it unconditionally against the CI regression check's budget; typed `Record<string, any>` regardless, structurally assignable to/from axios' own response header types (a test mock `of({...} as AxiosResponse)` now compiles against `HttpService['get']`'s return type). Differential-harness known differences unchanged at 25 (the 3 tagged `types: axios interop` are runtime-shape gaps - header duplicate-joining, `axiosRef.interceptors.request.handlers`/`.forEach`, `response.headers` lacking the `AxiosHeaders` API - not type gaps, so out of this item's scope; `tests/compat/api-surface.spec.ts`'s "AxiosHeaders instance" allowlist entry is gone, all 19 members now implemented). New tests: `instanceof axios.AxiosError` with axios installed and with `require('axios')` mocked to fail (module still loads, own classes unaffected); `registerAsync({})` throws. Benchmark (Node 24, 5 rounds × 2s, vs `claude/v1.0.0`): get -2.0%, post +4.1%, config +0.5%, error -5.7%, interceptors +5.9% - all pass, no re-run needed. `npm pack` + `check-declared-deps.cjs`/publint/attw/consumer type check (4 combos × 2 compilers)/consumer smoke matrix (6 combos × 2 module formats) all green locally. Next: feat(axiosRef): make it a real axios instance.
- 2026-09-25: PR #22 review: no blocking bugs. The prototype link to `axios.AxiosError` was verified: no effect on the user's axios, load order doesn't matter, ESM-only axios is caught. Strict `HttpModuleOptions` compiled against realistic @nestjs/axios code in strict and non-strict mode. Documented `AxiosHeaders#normalize` as a no-op (casing parity is tracked under feat(axiosRef)) and the two-copies-of-axios `instanceof` caveat.
- 2026-09-25: PR #22 merged: strict `HttpModuleOptions`, one `AxiosLikeRequestConfig<D>`, `post<T, D>`, `instanceof axios.AxiosError` via an optional axios peer, `strict` build (25 known differences, 3 `@ts-expect-error` left for feat(axiosRef)). CI all green. Next: feat(axiosRef): make it a real axios instance.
- 2026-09-26: Explored running upstream `@nestjs/axios`/`axios` test suites as a conformance layer (`plan/reports/upstream-test-suites.md`). `@nestjs/axios`'s two behavioural vitest specs run unmodified against this package via jest `moduleNameMapper` shims: 23 tests, 16 pass; the 7 failures include one genuinely new finding (a `responseType: 'stream'` request aborts on unsubscribe before headers arrive, when real `@nestjs/axios` never aborts a stream request regardless) plus confirms `postForm`/`query`/`axiosRef.defaults.timeout` gaps already tracked. A prototype axios `adapter` backed by this package's own response/error/redirect code, run against axios's own unmodified `tests/unit/adapters/http.test.js` (30 of 246 tests, filtered to transport/response/error behaviour): 15/30 pass; 5 of the 15 failures are real, already-tracked gaps with new concrete repro strings (timeout message format, `response.request` placeholder, `maxBodyLength` unenforced, unsupported-protocol error text); the rest trace to one prototype wiring gap (fixed) and its `EADDRINUSE` cascade, or to sandbox DNS. Recommendation: strategy (a) - axiosRef as the instance under test - once `claude/axiosref-instance` lands; strategy (b) as a narrower, cheaper standing transport check. Prototypes in `plan/prototypes/upstream-tests/`.
- 2026-09-25: PR #23 open (`claude/axiosref-instance`): `feat(axiosRef): make it a real axios instance`. `axiosRef` is now callable (`axiosRef(config)` / `axiosRef(url, config)`), and gained `getUri`, `create()` (a fresh instance sharing the same undici transport, with its own defaults/interceptors merged from the parent, axios-style), `postForm`/`putForm`/`patchForm` (multipart by default, always, matching axios' `generateHTTPMethod(isForm)` - no url-encoded override even with an explicit Content-Type), and `query()` (`@nestjs/axios` 12 / axios >=1.13). `axiosRef.defaults` now supports `validateStatus`, `params`, `paramsSerializer`, `maxRedirects`, `responseType`, `transformRequest`/`transformResponse`, `timeout`, `baseURL`, `headers`, `adapter`, `withCredentials` (no-op, unchanged). **Breaking**: the old, inconsistent precedence (module options won for headers, `defaults` won for timeout/maxRedirects) is replaced by one rule everywhere - `HttpModule.register(moduleOptions)` seeds `axiosRef.defaults` once at setup (like `axios.create(moduleOptions)`), and from then on `axiosRef.defaults` is the sole source of truth: request config > `axiosRef.defaults` > nothing (the module options no longer apply independently after setup). A function `adapter` is now supported (bypassing undici entirely, verified end-to-end with `axios-mock-adapter`, added as a devDependency: `tests/axios-mock-adapter.e2e.spec.ts`, 7 cases x 2 libraries); a string/array adapter is ignored (undici stays in charge). `AxiosHeaders` was rewritten for casing parity with axios: first-set casing is preserved, lookups are case-insensitive, `normalize(true)` title-cases, `toJSON()`/`toString()` return the preserved casing (**breaking**: previously lower-cased); `set`/`get`/`has`/`delete`/`toJSON` collapsed to single wide signatures (sidesteps TS's unreliable overload-to-overload structural comparison) to remove 2 of the 3 remaining `tests/types` `@ts-expect-error` markers (`HttpService` assignable to `@nestjs/axios`'s `HttpService`; `axiosRef` assignable to `AxiosInstance`) - the last one (`InternalAxiosRequestConfig`-typed callback) turned out to depend on axios' own unrelated 4th `R` generic on request methods and was left as `@ts-expect-error` (documented in the changeset; not worth mirroring an unused escape-hatch generic throughout this library's types for it); `forEach`/`entries`/`keys`/`values`/`setAcceptEncoding`/`getAcceptEncoding`/`hasAcceptEncoding` removed (none exist on axios' own `.d.ts`). Storage for the new `AxiosHeaders` is a module-private `unique symbol`-keyed *optional* class field holding a `Map` (not a `WeakMap`, which cost ~200ns/op from V8's GC bookkeeping and was a measured perf regression when first tried; the field's optionality keeps `AxiosHeaders` structurally, not nominally, typed, so it stays mutually assignable with axios' own class). Differential harness: 202 scenarios, known differences 25 -> 22 (`axiosRef instance API surface` and 2 `postForm` cases resolved). Perf check (`benchmarks/micro/compare.js`, Node 24, base = `claude/v1.0.0`, 5 rounds x 2s): get +2.2%, post -1.7%, config +5.0%, error -1.7%, interceptors -3.8% - all inside the 10%/15% thresholds (first attempt regressed `get`/`error` past threshold on the `WeakMap` design; fixed and re-measured clean, faster than base on `AxiosHeaders` construction specifically). Docs: `docs/http/http.service.md`, `docs/axios-supported-options.md`, `docs/migration-guide.md` (every break listed), `docs/features.md`. Changeset: `minor` with BREAKING notes (`.changeset/axiosref-real-instance.md`). `npm run lint`/`format:check`/`typecheck`/`typecheck:compat`/`build`, `test:jest` (all suites) and `test:examples`, `npm pack` + `check-declared-deps.cjs`/publint/attw, consumer type check and matrix scripts all green on Node 24. Left undone: nothing required by this item; the 1 remaining unrelated `@ts-expect-error` is a deliberate scope boundary (documented above and in the changeset). Next: continue phase 2 per plan order.
- 2026-09-25: PR #23 review fixes:
  - `postForm`/`putForm`/`patchForm` with a `URLSearchParams` body sent an empty FormData; it's now sent as is.
  - `axiosRef.create()` copies object and array defaults (`params`, `transformRequest`, and so on), as axios' `mergeConfig` does, so a later parent mutation no longer leaks into the child.
  - `setAcceptEncoding`/`getAcceptEncoding`/`hasAcceptEncoding` are back as runtime-only methods: axios has them at runtime but not in its `.d.ts`, so removing them was a needless break.
  Each fix has a test that fails without it.
- 2026-09-26: PR #23 merged: `axiosRef` is a real axios instance (callable, `create`, `getUri`, `*Form`, `query`, runtime defaults, function `adapter` so axios-mock-adapter works, header casing kept), with one precedence rule: request > `axiosRef.defaults`, seeded from module options. 22 known differences, 1 `@ts-expect-error` left. CI all green. Next: fix(errors).
- 2026-09-26: Upstream test-suite exploration merged into the plan (report + prototypes). Recommendation: run both projects' own suites in CI against an expected-failures list; keep abort-before-headers for streams as a deliberate difference.
- 2026-09-26: PR open (`claude/fix-errors`): `fix(errors): match axios errors`. **Breaking:** `timeout` is now a total (deadline) timeout, like axios - one timer per request, from start until the body is fully read (or, for `responseType: 'stream'`, until the headers arrive), created only when `timeout > 0`; undici's `headersTimeout`/`bodyTimeout` stay a backstop. A slowly-but-steadily trickling body (undici's idle timers never reset the clock) now times out at the configured value, where it never did before. **Breaking:** size-limit codes now match axios: `maxContentLength` -> `ERR_BAD_RESPONSE` (was `ERR_FR_MAX_CONTENT_LENGTH_EXCEEDED`), enforced while streaming for an uncompressed body; `maxBodyLength` is now actually enforced (`ERR_BAD_REQUEST` for a string/Buffer body, checked synchronously before dispatch; `ERR_FR_MAX_BODY_LENGTH_EXCEEDED` for a stream body, matching axios' own default `follow-redirects` transport) - a per-request value now correctly wins over a module-level one either way. The `SizeLimitInterceptor` is no longer auto-registered (it used the old codes and buffered first); still exported, with its own codes updated. Also fixed: undici's synchronous/argument-validation errors become proper `AxiosError`s (`ERR_BAD_REQUEST`) with `config`/`request` set - an unsupported protocol now gives axios' exact `Unsupported protocol X:` message, checked via a pre-dispatch check rather than letting undici's own error through; a genuinely malformed URL still propagates raw, matching `@nestjs/axios`. `error.request`/`response.request` are built from the hop actually dispatched (`path`/`method`/`host`/`protocol`/`res.responseUrl`, the last always set, whether or not a redirect ran) instead of a useless placeholder - `res.responseUrl` on a response, and `error.request` on a network/timeout/cancellation error, except where axios itself never builds a request object either (already-aborted signal, unsupported protocol). `timeoutErrorMessage`/`transitional.clarifyTimeoutError`, `validateStatus: null` (or `undefined` as an explicit own key), URL credentials as Basic auth (`config.auth` wins), and `allowAbsoluteUrls: false` are now honoured. Both PR #15 review follow-ups are fixed: `resolveSignal`'s listener leak (a `SIGNAL_CLEANUP` symbol on the combined signal, called from `executeRequest`'s teardown on every request, not just an aborted one) and `toAxiosError` now reading the actual per-request `RequestAbortSignal` instead of the caller's pre-merge one. `AxiosError.toJSON()` now matches axios' exact key set and serialises `AxiosHeaders` as a plain object. Differential harness: 202 -> 207 scenarios (5 new: a stream-body `maxBodyLength`, a header value with an embedded `\n`, module-level `maxBodyLength`/`timeoutErrorMessage`, `config.auth` winning over URL credentials), known differences under this item 19 -> 6 (a query-string apostrophe always comes out `%27` - undici's own WHATWG URL parsing, not fixable without bypassing it entirely; a header value with an embedded `\n` - undici rejects it where Node's own `http` module, what axios uses, silently strips it, though the rejection is now at least a well-formed `AxiosError`; a timeout landing after the response has already started streaming - axios' own internal race can give `ERR_BAD_RESPONSE`/"stream has been aborted" instead of `ECONNABORTED`, depending on whether axios' response object happens to exist yet; a non-string/non-object request body (`5`, `true`) sent through instead of rejected - out of this item's original scope, not attempted). Benchmark (Node 24, 5 rounds x 2s, this sandbox, vs `claude/v1.0.0`): get +6.1%, post +6.1%, config +9.2%, error +7.0%, interceptors +7.8% (thresholds 10%/15%) - all pass; `config` (the `timeout: 5000` scenario, which now also pays for one `setTimeout`/`clearTimeout` per request) is the closest to its threshold and worth watching if it flakes in real CI. Docs: `docs/axios-supported-options.md` (Request config, Response, Errors, Size limits), `docs/guides/error-handling.md`, `docs/migration-guide.md` (new "Errors, timeouts and size limits" breaking-changes section). Changeset: `minor` with BREAKING notes (`.changeset/errors-match-axios.md`). `npm run lint`/`format:check`/`typecheck`/`typecheck:compat`/`build`, `test:jest` (664, all suites) and `test:examples`, `npm pack` + `check-declared-deps.cjs` all green on Node 24. Left undone (documented above as remaining known differences, not attempted): the `%27` encoding and header-newline gaps (both need bypassing/duplicating platform URL/header handling), the timeout-during-streaming error-shape race, and non-string/non-object request bodies. Also not attempted: `onUploadProgress`/`onDownloadProgress`/`maxRate`/`formSerializer` (tracked separately, phase 2's own next item) and module-level `timeoutErrorMessage`/`transitional` defaults beyond the fallback this PR added (per-request is fully covered; no differential case needed the module-level path beyond what's now tested).
- 2026-09-26: PR #25 review fixes: `maxContentLength` for a compressed response is now enforced against the *decompressed* size while streaming (both the compressed body stream and the decompression stream are destroyed the moment the limit is crossed), not after buffering the whole decompressed body first - a small gzip response no longer fully decompresses in memory before being rejected (`readBuffer`/`readText`, gated on `maxContentLength` actually being set; the common, unlimited case still takes the fast, fully-buffered `decompressBuffer` path, measurably faster for a normal body). New differential case (gzip + `maxContentLength`, axios vs. ours: match) and a memory/early-abort test (a 50MB-zeros gzip body over a real server, `maxContentLength: 1000` - rejects quickly, server sees the connection close early; a separate fast, deterministic unit test covers the same shape without a real server or an RSS assertion). Also fixed the 6-9% perf regression: `response.request`/`error.request` are now a small `RequestInfo` class built from raw inputs only (no `new URL()` parse, no object allocation beyond `this`) with lazy, cached prototype getters for `path`/`host`/`protocol`/`method`/`res` - `HttpService.executeRequest` no longer calls `buildRequestInfo`/`urlToString` eagerly on every response/error. `unsupportedProtocol` gained a `charCode`-based fast path for a plain `http(s)://` URL (case-insensitive), skipping the regex for the overwhelming majority of requests. Isolated JS-only benchmark (`isolated-bench.js`, 200k iterations, stubbed undici): before this fix ~4.06-4.44µs/req (cpu ~4.6-4.9µs) vs. base `claude/v1.0.0` ~3.34-3.37µs/req (cpu ~3.72-3.82µs) - a ~25% regression; after this fix, ~3.39-3.57µs/req (cpu ~3.79-4.04µs), within ~1-6% of base and mostly noise-level. Full benchmark (`benchmarks/micro/compare.js`, Node 24, 5 rounds x 2s, vs. `claude/v1.0.0`): get -0.8%, post +3.8%, config +4.7%, error +3.4%, interceptors +3.6% (thresholds 10%/15%) - all pass, get comfortably inside the ~3% target. Docs: `docs/axios-supported-options.md`'s `maxContentLength` row and the `errors-match-axios` changeset no longer admit the gzip-buffered-first gap. `npm run lint`/`format:check`/`typecheck`/`typecheck:compat`/`build`, `test:jest` (692, all suites) and `test:examples` all green on Node 24.
- 2026-09-26: PR #25 merged: errors match axios (deadline `timeout`, `timeoutErrorMessage`/`clarifyTimeoutError`, real `error.request`, `maxContentLength` streamed including gzip, `maxBodyLength`, URL credentials, `allowAbsoluteUrls`, `validateStatus: null`); the PR #15 follow-ups are fixed. Review fixes: a gzip bomb now aborts early (0.8 MB RSS vs 195 MB), and a lazy `RequestInfo` brought per-request CPU back to within about 5% of base. CI all green. Next: Phase 3 dispatcher ownership (per-service Agent, OnModuleDestroy, setDispatcher) and upstream conformance CI jobs, run in parallel.
- 2026-09-26: PR #28 open (`claude/upstream-conformance`): upstream conformance suites promoted from prototypes to maintained CI (`tests/upstream/`, `.github/workflows/upstream.yml`), `plan/prototypes/upstream-tests/` deleted. Layout: `tests/upstream/` (not `scripts/`), since these are Jest/vitest-adjacent test harnesses, not build tooling, and must stay outside Jest's own `testRegex`/`roots` (confirmed `test:jest` still 692/692, no upstream specs picked up). @nestjs/axios suite (jest `moduleNameMapper` shims): 20/23 pass, 3 expected failures (1 deliberate stream-abort difference, 2 shim/harness limits), 0 new, 0 fixed - clean, deterministic. axios suite (`tests/unit/adapters/http.test.js`), both strategies now possible since PR #23: strategy (a) axiosRef as the instance 130/242 pass (112 expected failures); strategy (b) our transport as an adapter 100/242 pass (142 expected failures) - 3-4 tests hard-excluded (legacy `CancelToken`, a `HangingConnectAgent`/fake-hostname pair, FormData multipart for (b) only). Bisected and fixed the report's "full run hangs early": (1) upstream's own fixture (`tests/setup/server.js`, not modified) never surfaces a `listen()` bind failure to its callback, so one lingering socket on its shared fixed port could hang every later test on it forever - `tests/upstream/axios/adapters/ephemeral-ports.cjs` patches `net.Server.prototype.listen` to substitute an OS-assigned port for the fixture's 3 fixed ones (one test that hardcodes the literal port number in a redirect `Location` header is now an expected failure instead); (2) an undrained response body on a `buildRedirectHop` security-check failure leaked a connection, fixed in `undici-adapter.cjs`. The suite still occasionally needs a retry in this (heavily shared, contended) sandbox; the runner chunks the file across several vitest processes (a fresh process reclaims the OS port instantly, unlike in-process cleanup) and recursively splits/retries a chunk that hangs, down to one test at a time, bounded by a per-strategy wall-clock deadline so a bad run degrades to "some tests reported failed, not evaluated" rather than hanging the job - real GitHub Actions runners, being dedicated rather than shared, are expected to need this far less. Real, new finding: no `data:` URL support at all (own Phase 2 item added). Expected-failures reasons reference either a plan.md item, a documented deliberate/module-level-only difference (proxy, HTTP/2, `allowedSocketPaths`), or "harness limitation" (config-normalization bypassed by strategy (b), Node-http-internals strategy (a)/(b) have no equivalent for, sandbox network specifics) - none left as an unexplained guess. `npm run lint`/`format:check` clean over `tests/upstream/**` and `.github/**`; `.github/workflows/upstream.yml` validated with actionlint (weekly cron, `workflow_dispatch`, PRs into `main`/`claude/v1.0.0` touching `src/**`/`tests/upstream/**`; clones cached by tag via `actions/cache`, keyed on the two `run.mjs` files so a pin bump busts it). Changeset: empty (dev/CI-only). Docs: a short "Upstream conformance suites" section in `docs/guides/testing.md`.
- 2026-09-26: PR #26 open (`claude/dispatcher-lifecycle`): dispatcher ownership and `HttpService` member cleanup, per the "breaking changes are fine" decision's "Default dispatcher" bullet. **Breaking:** every `HttpService` now builds its own undici `Agent` once, at construction (`allowH2: false` unless `httpVersion: 2`, which already wins via its own module-built dispatcher), instead of falling back to `undici.getGlobalDispatcher()` - `undici.setGlobalDispatcher()` elsewhere in the process no longer affects requests made through `HttpService` at all; fixes the two-copies-of-undici case and undici 8's default HTTP/2 negotiation on the no-options path. Full precedence: per-request `dispatcher` > per-request `socketPath` > module `dispatcher`/module-built dispatcher > this per-service default. `axiosRef.create()`-derived instances already shared the parent `HttpService`'s transport (same instance, `dispatchAxiosConfig`), confirmed with a test. **Breaking:** `HttpService` implements `OnModuleDestroy`, closing (gracefully, `close()`) every dispatcher it created - the per-service default, the module-built one (`Agent`/`ProxyAgent`/`EnvHttpProxyAgent`/`CookieAgent`), and cached `socketPath` `Agent`s - never a user-supplied `dispatcher`. Reproduced the package-quality report's probe (a lingering server-side connection after `module.close()`) as a real test; fixed. **Breaking:** the static `HttpModule` import's default options (`useValue: {}` for both `UNDICI_INSTANCE_TOKEN` and `HTTP_MODULE_OPTIONS`) are now `useFactory: () => ({})`, so apps that import the bare module no longer share one options object (confirmed leaking via `setDispatcher()` before the fix). **Breaking, `HttpService` members** (plan.md phase 3 "HttpService members", decided inline): `setGlobalDispatcher` renamed to `setDispatcher`, no alias - if the dispatcher it replaces is one this service created, it's closed (decided: yes, tested); `setInterceptors` is now `private` (the module passes the resolved interceptor list through a new constructor parameter instead of calling a public method after construction); `interceptorCount` is now the real, plain length of the module-registered (`addInterceptor`/module `interceptors`) chain - the old formula added a phantom "+1" for a response-adapter interceptor removed by the axiosRef refactor and separately counted `axiosRef`'s own interceptors (a different chain with no analogous count on real axios either); kept (not removed) because several existing tests use it to assert registration actually happened. `undiciRef` now returns a frozen, `__`-key-stripped snapshot, rebuilt fresh on every read (so a later `setDispatcher()` is reflected, but mutating one snapshot can't affect the service or a subsequent read). **Fix:** axios-only keys (`auth`/`httpAgent`/`httpsAgent`/`proxy`/`httpVersion`/`cookieJar`/`withCredentials`/`xsrfCookieName`/`xsrfHeaderName`/the internal `__` keys) are stripped once, at setup, into a `dispatchBaseOptions` object, instead of leaking into every request's undici dispatch options (confirmed via a dispatcher `dispatch` spy). **Fix:** `http.module.ts`'s axios-compatibility warnings go through Nest's `Logger` (context `HttpModule`) instead of `console.warn`. Left undone (out of this item's scope, still open in plan.md): the `__` casts aren't yet a typed resolved-config object (only the key-stripping and Logger sub-items were in scope here); the public-API trim/api-extractor report item. Tests: new `tests/dispatcher-lifecycle.e2e.spec.ts` (10 cases: default-Agent-used-even-with-global-dispatcher-swapped, `onModuleDestroy` closing the default/module-built dispatcher with no lingering server-side connection, never closing a user-supplied one, `setDispatcher` precedence/closing-what-it-replaces/never-closing-a-user-supplied-one, frozen `undiciRef` without `__` keys, axios-only-key stripping via a `dispatch` spy, `axiosRef.create()` sharing the parent's dispatcher, the static-`HttpModule` options-isolation fix); strengthened the existing `tests/redirects.e2e.spec.ts` foreign-global-dispatcher case to assert the foreign dispatcher's `dispatch` is never called; updated 4 existing e2e tests' `interceptorCount` expectations (`tests/services/http-interceptor.e2e.spec.ts`, `tests/interceptor-axios-comparison.e2e.spec.ts`, `tests/interceptor-opentelemetry.e2e.spec.ts`, `tests/axios-opentelemetry-integration.e2e.spec.ts` - the last one's premise no longer holds, since `interceptorCount` doesn't count `axiosRef` interceptors, so it now documents that instead) and the `console.warn` spies in `http.service.axios-compatibility.spec.ts` to spy on `Logger.prototype.warn`. `forceExit` stays off; no test needed a new `module.close()` beyond the new file's own (`--detectOpenHandles` clean throughout). Docs: `docs/http/http.service.md` (new "Dispatchers and connection lifecycle"/`OnModuleDestroy` section, `setDispatcher`, rewritten `interceptorCount`/`undiciRef`), `docs/http/http.module.md`, `docs/guides/configuration.md`, `docs/guides/interceptors.md`, `docs/axios-supported-options.md` (precedence section), `docs/migration-guide.md` (new "Dispatcher lifecycle and `HttpService` members" breaking-changes section). Changeset: `minor` with BREAKING notes (`.changeset/dispatcher-lifecycle.md`). Benchmark (Node 24, 5 rounds x 2s, this sandbox, vs `claude/v1.0.0`, proxy env vars unset): 3 runs, all scenarios pass both thresholds every time (worst per-scenario across the 3 runs: get +6.3%, post +1.8%, config +5.2%, error +9.3%, interceptors +2.6%; most runs well under half the threshold). `npm run lint`/`format:check`/`typecheck`/`typecheck:compat`/`build`, `test:jest` (702, all suites, `--detectOpenHandles` clean) and `test:examples`, `npm pack` + `check-declared-deps.cjs` all green on Node 24; a Node 22.22.2 smoke test (plain request + `app.close()`) against the built `lib` confirmed no lingering connection. Next: continue phase 3 (public API trim/api-extractor) once this merges; upstream conformance CI jobs are being done in parallel on `claude/upstream-conformance`.
- 2026-09-26: PR #26 review: no bugs; lifecycle, close semantics, key stripping and the frozen `undiciRef` were verified with runtime repros. The migration guide and changeset now name undici's `MockAgent` + `setGlobalDispatcher` test pattern explicitly (it would silently hit the real network) and show `register({ dispatcher: mockAgent })` / `setDispatcher(mockAgent)`.
- 2026-09-26: PR #26 merged: a per-service default Agent, `OnModuleDestroy` closes the dispatchers it created, `setDispatcher`, a private `setInterceptors`, a real `interceptorCount`, a frozen `undiciRef`, axios-only keys stripped from dispatch, Nest `Logger`. The MockAgent migration is documented. CI all green.
- 2026-09-26: PR #27 open (`claude/api-trim`): the three remaining phase 3 items. **Trim the public API** (**breaking**, removed outright, no `@deprecated` step): the legacy typed module (`TypedHttpModule`/`InjectTypedHttpService`/`ExtractHttpServiceType`/`HTTP_SERVICE_TYPE`/`TypedDynamicModule`), `AxiosResponseAdapterInterceptor`/`axiosResponseAdapter` (dead code), `SizeLimitInterceptor`/`createSizeLimitInterceptor`/`SizeLimitOptions` (no remaining use once `maxBodyLength`/`maxContentLength` are enforced natively), `STATUS_TEXT_MAP`, `HTTP_MODULE_ID` (registered but never injected), the internal error helpers (`toAxiosError`/`createStatusError`/`createTimeoutError`/`createUnsupportedProtocolError`/`isDeadlineTimeoutReason`/`DeadlineTimeoutReason`/`EffectiveAbortSignal`), and unused types (`HttpServiceWithAxiosRef`, `BodyMixin`, `CommonResponseHeaders`, `MethodHeaders`) - every `export *` barrel (`src/index.ts` and each `modules/http/*/index.ts`) replaced with an explicit named-export list (57 -> 42 exported symbols); added `AxiosRequestConfig`/`AxiosResponse`/`AxiosInstance` axios-named aliases for the existing `AxiosLikeRequestConfig`/`AxiosLikeResponse`/`AxiosRef`; dropped `reflect-metadata` from this package's own peerDependencies (unused by this library's code, already required transitively through `@nestjs/common`/`@nestjs/core`'s own peer on it - confirmed empirically via the consumer smoke matrix, which still resolves it); `UNDICI_INSTANCE_TOKEN`/`HTTP_MODULE_OPTIONS` kept and now documented in `docs/guides/testing.md` for test-provider overrides. **api-extractor**: `@microsoft/api-extractor` devDependency, `api-extractor.json` (run against `lib/index.d.ts`), the report committed at `etc/nestjs-axios-undici.api.md`, `npm run api:check`/`api:update`, wired into the Node 24 full-checks job in `tests.yml` (package.yml is about the packed tarball's dependency/format shape, a different concern); `ae-missing-release-tag` and `tsdoc-*` syntax messages silenced (no release-tag convention in use, and doc comments are prose, not strict TSDoc) - `ae-forgotten-export` stays at its default and caught two real gaps fixed along the way: `HttpService`'s constructor parameter types now stay the plain public `UndiciRequestOptionsType`/`HttpModuleOptions` (a constructor parameter property's declared type is part of the public `.d.ts` regardless of `protected`/`private`), with the internal `Resolved*` variant cast locally at the one point each method actually reads `__resolvedConfig`; `AxiosInstanceContext`/`AxiosInterceptorStore`/`AxiosInterceptorEntry` (needed to type the already-public `dispatchAxiosConfig`) are now exported too. **`src/version.ts`**: no more runtime `require('../package.json')` - `scripts/generate-version.js` writes a build-time `src/version.generated.ts` (gitignored), run as `prebuild`/`pretest:jest`/`pretypecheck`; a new test asserts `LIBRARY_VERSION` matches `package.json`. **Option mapping**: the `__agentOptions`/`__proxyAgent` `as any` casts in `axios-config.adapter.ts`/`http.service.ts` are replaced by one internal `ResolvedModuleConfig` (`src/modules/http/internal/resolved-config.ts`), produced once by `mapAxiosConfigToUndici` and consumed by `HttpService` - not exported. No behaviour change. Docs: `docs/features.md`, `docs/http/http.module.md`, `docs/guides/testing.md`, `docs/migration-guide.md` (new "Public API trim" section listing every removal and its replacement), README's supported-versions table. Changeset: `minor` with BREAKING notes (`.changeset/api-trim-version-config.md`). Benchmark (Node 24, 5 rounds x 2s, vs `claude/v1.0.0`): get -0.3%, post +3.4%, config +1.0%, error +0.8%, interceptors -1.8% (thresholds 10%/15%) - all pass. `npm run lint`/`format:check`/`typecheck`/`typecheck:compat`/`build`/`api:check`, `test:jest` (691, all suites) and `test:examples`, `npm pack` + `check-declared-deps.cjs`/publint/attw, consumer type check (2 combos x 2 compilers) and a 2-combo consumer smoke matrix all green on Node 24. Left undone: nothing required by these three items. Phase 3 is now fully merged into this branch (pending review/merge into `claude/v1.0.0`).
- 2026-09-26: PR #27 review fixes:
  - `axiosRef` now dispatches through a private closure instead of a public `HttpService.dispatchAxiosConfig`, so it and `AxiosInstanceContext`/`AxiosInterceptorStore`/`AxiosInterceptorEntry` are off the public API (the api report shrank by 29 lines).
  - `test:debug` generates the version file first.
  - The migration guide covers the alias/axios name clash.
  - Known follow-up: api-extractor bundles TS 5.9 while the repo uses TS 6.0. It currently only warns; upgrade api-extractor when a TS 6 build ships.
- 2026-09-26: PR #27 merged: public API trimmed from 57 to 39 exports (the axiosRef dispatch bridge and its context types are now internal), api-extractor report plus `api:check` in CI, build-time version constant, typed internal `ResolvedModuleConfig`, `reflect-metadata` peer dropped. CI all green.
- 2026-09-26: PR #28 review audited the expected-failures lists. Three real gaps were hidden in the 'not yet root-caused' groups and are now plan items and relabelled entries: stream `maxContentLength`, header sanitization, error shapes. Content-Length override is marked deliberate (undici safety), the HTTP_MODULE_ID reason is corrected, and the job timeout is raised to 12 min, since the per-strategy deadlines alone were 5 min.
- 2026-09-26: PR #28's first real-CI run (a dedicated GitHub Actions runner, not just this sandbox) hit the axios strategies' `STRATEGY_DEADLINE_MS`, silently folding ~70 never-evaluated tests into "expected failures" per strategy - it happened there too, so it wasn't only sandbox contention. Root cause: a `progress`/`Rate limit` describe block is genuinely, deliberately slow by test design (one test `await`s ten real 1100ms `setTimeout`s to produce ten progress samples - ~11s, not a hang); several such tests cluster together in file order and, bundled into one chunk, their legitimate durations add up past a too-short per-attempt timeout (`CHUNK_TIMEOUT_MS`, was 20s), which looks identical to a hang from the outside. Fixed: (1) a test whose chunk never produces a report is now reported **NOT EVALUATED** in its own bucket, separate from expected/new failures, and always fails the run - `diffResults`/`printSummary`/`isSuiteFailing` (`tests/upstream/lib/conformance.mjs`) never again silently treat "never ran" as "expected" or "passed"; (2) `CHUNK_SIZE`/`CHUNK_TIMEOUT_MS`/`STRATEGY_DEADLINE_MS` retuned (10/25s/240s) so a slow cluster gets enough headroom without a full retry storm; (3) `.github/workflows/upstream.yml` runs the 3 suites as parallel matrix jobs (`fail-fast: false`), each with its own budget, instead of one serial job - keeps `timeout-minutes` at 8 (was 12) per job rather than raising a shared deadline. `should support IPv6 literal strings` fails with no IPv6 (this sandbox) and passes on a real runner; added an `environmentDependent` expected-failures entry kind (`{reason, environmentDependent: true}`), allowed to pass or fail either way, tracked in its own bucket, never counted as new or fixed. Recalibrated both axios `expected-failures.strategy-{a,b}.json` from fully-evaluated local runs (0 not-evaluated): strategy (a) 130/241 pass (111 expected, 1 environment-dependent); strategy (b) 111/241 pass (130 expected, 1 environment-dependent) - strategy (b)'s old list had 11 stale entries (tests that only ever hit the deadline before, never actually failing) now removed. `npm run lint`/`format:check`/`test:jest` (691/691) clean; `.github/workflows/upstream.yml` validated with actionlint.
- 2026-09-26: PR open (`claude/progress-form`): `feat: progress callbacks, maxRate, formSerializer` - the last phase 2 item. `onUploadProgress`/`onDownloadProgress` (axios' own `AxiosProgressEvent` shape, throttled the way axios throttles it via ported `speedometer`/`throttle`/`progressEventReducer` logic - at most every ~333ms, plus a final flush) and `maxRate` (a number or `[upload, download]`, both directions throttled via a trimmed port of axios' `AxiosTransformStream`) are wired up only when actually set - `axios-progress.adapter.ts` (new file). `onDownloadProgress` works with every `responseType` including `'stream'`; `onUploadProgress` works for a string/Buffer/stream/`FormData` body (a raw `FormData` from the `undici` package specifically, not Node's global one, can't be metered - undici encodes it internally - documented, unchanged either way). All four (`onUploadProgress`/`onDownloadProgress`/`maxRate`/`formSerializer`) follow the same request > `axiosRef.defaults` > module rule as every other passthrough default (added to `DEFAULTS_PASSTHROUGH_KEYS`, so module options seed `axiosRef.defaults` once at setup, same as `baseURL`/`timeout`). `formSerializer` (`visitor`/`dots`/`metaTokens`/`indexes`/`maxDepth`, ported from axios' `toFormData.js`, defaults matching axios exactly) now drives `postForm`/`putForm`/`patchForm`, and a plain request whose `Content-Type` is explicitly `multipart/form-data` or `application/x-www-form-urlencoded`; the reverse (`formDataToJSON`, a real `FormData` sent with `Content-Type: application/json` converted to a plain object first) is also implemented - both ported into `axios-request.adapter.ts`, verified field-for-field against real axios' own `toFormData`/`formDataToJSON` (`node_modules/axios/lib/helpers/`). Not implemented: `formDataHeaderPolicy` (a `form-data`-package-specific option) and a pre-computed `Content-Length` for a multipart upload (axios' own `formDataToStream` sums part sizes upfront; this library's `globalFormDataToStream`, built on the Fetch `Response` encoder, doesn't expose one) - so a `postForm` upload's `onUploadProgress` gets `lengthComputable: false`/`total: undefined` even though `loaded` still tracks real bytes, both documented in `docs/axios-supported-options.md`. Types: `AxiosProgressEvent`/`FormSerializerOptions`/`SerializerVisitor`/`FormDataVisitorHelpers`/`FormDataLikeTarget` added (matching axios' own type names/shapes, including `SerializerVisitor`'s `path: Array<string|number> | null` with no `undefined`, needed for mutual assignability - `typecheck:compat` caught this), plus the four new fields on `HttpModuleOptions`/`AxiosLikeRequestConfig`/`AxiosRefDefaults`. New tests: `axios-progress.adapter.spec.ts` (19, unit), `axios-request.adapter.spec.ts` gained a `formSerializer` describe block (14 new, unit, field-for-field checked against real axios' `toFormData`/`formDataToJSON`), `tests/progress-form.e2e.spec.ts` (13, module-/defaults-/request-level precedence, `responseType: 'stream'`, functional `maxRate` throttling), `tests/compat/differential/progress-form.diff.spec.ts` (6, comparing the *stable* parts of progress events - final `loaded`/`total`, `progress` reaching 1, monotonic `loaded`, `upload`/`download` flags, not the timing-dependent raw event stream - plus exact `formSerializer`/`formDataToJSON` output, all passing, no `knownDifference`). `test:jest`: 691 → 743. Benchmark (Node 24, 5 rounds × 2s, vs `claude/v1.0.0`, proxy env vars unset): get -1.2%, post +1.9%, config +5.0%, error +6.5%, interceptors +3.0% (thresholds 10%/15%) - all pass, no re-run needed. `npm run lint`/`format:check`/`typecheck`/`typecheck:compat`/`build`/`api:check` (39 → 44 exports: the 5 new form/progress types, `npm run api:update` committed), `test:jest` and `test:examples`, `npm pack` + `check-declared-deps.cjs` all green on Node 24.
- 2026-09-26: PR #30 review fixes (`claude/progress-form`), 2 bugs found by repro scripts:
  - **CRITICAL**: a metered download (`onDownloadProgress`/download `maxRate` set) silently succeeded with an HTTP 200 and `data: ''` on a real stream error (an abort, a destroyed source, or a throwing `onDownloadProgress` callback itself) instead of rejecting. `toAxiosLikeResponse`'s catch (`axios-response.adapter.ts`) fell back to a "corrupt-gzip recovery" raw-text re-read meant for undici's own body (checked via `.bodyUsed`/`.text()`); the metered body is a plain `Transform` with neither, so it silently returned `''`. Fixed by tracking `isMetered` and always rethrowing for a metered body, same as the existing `bodyUsed` check. Also matched axios' exact handling of a *throwing progress callback*: axios decouples the raw callback via `process.nextTick` (`asyncDecorator` in `lib/adapters/http.js`) *before* it ever reaches the stream/throttle machinery - verified directly against real axios 1.20 (the response still resolves with the full, correct body; the throw itself surfaces as a separate, unrelated `uncaughtException`). Ported the same `asyncDecorator`/`scheduleProgress` into `axios-progress.adapter.ts`'s `attachReporter`, so a throwing callback here never touches the meter stream at all any more.
  - **MEDIUM**: `meterUploadBody` turned a resendable string/Buffer upload body into a one-shot stream before redirect handling ever saw it, so a 307/308 redirect rejected with `ERR_FR_REDIRECTION_FAILURE` with `onUploadProgress` set, even though the identical request without it succeeded (axios/follow-redirects don't have this problem: they buffer every byte *written* to the socket, not the original source, so a metered upload replays fine there). Fixed in `http.service.ts`'s `executeRequest`: a resendable string/Buffer body is now kept unmetered on `options.body`/`currentOptions.body` throughout the redirect chain (so `buildRedirectHop` never sees a one-shot stream for it) and re-metered fresh, via a new `dispatchBody` helper, right before each hop's actual `request()` call - one meter instance per hop, matching axios. An already-stream body (a caller's `Readable`, or `form-data` output) is still metered once, immediately, unchanged - it was never resendable on a redirect either way.
  - New/changed tests: `axios-response.adapter.spec.ts` gained a "metered body error propagation" describe block (4, including a `process.nextTick`-interception technique to observe the decoupled callback's throw safely, without tripping jest-circus's own uncaught-exception-fails-the-test behavior); `axios-progress.adapter.spec.ts` gained one test proving a throwing `onProgress` never reaches/errors the meter stream; `tests/progress-form.e2e.spec.ts` gained an abort-mid-download test and 2 redirect-with-`onUploadProgress` tests; `tests/compat/differential/progress-form.diff.spec.ts` gained a 307-redirect-with-Buffer-body-and-`onUploadProgress` case against real axios (passes, no `knownDifference`). `test:jest`: 743 → 751. Verified against all 4 reviewer repro scripts (`repro-abort.js`, `repro-full-response.js`, `repro-callback-throw.js`, `repro-redirect-meter.js`) directly - all now show the fixed behaviour. Benchmark (Node 24, 5 rounds × 2s, vs `claude/v1.0.0`, proxy env vars unset): get -1.3%, post +1.9%, config -0.3%, error -1.8%, interceptors +0.3% (thresholds 10%/15%) - all pass, confirming the plain (no-progress-options) path is unaffected. No public API change (`api:check` unchanged). `npm run lint`/`format:check`/`typecheck`/`typecheck:compat`/`build`/`api:check`, `test:jest` and `test:examples`, `npm pack` + `check-declared-deps.cjs` all green on Node 24.
- 2026-09-26: PR #30 merged (progress callbacks, `maxRate`, `formSerializer`); CI green on every row.
- 2026-09-26: PR #28 CI: both axios strategies reported 0 new failures but one 'now passing' entry, regression #10908. It passes a mock `transport` (unsupported), so the request really goes to http://example.com/: it passes on Actions (internet) and fails offline. Marked `environmentDependent` in both axios lists, not removed.
- 2026-09-26: PR open (`claude/bench-fair`, Phase 4/5 bench items): one shared `benchmarks/apps/nestjs-app` (`CLIENT=axios|undici`, `PLATFORM=express|fastify`, `INTERCEPTOR=0|1`) replaces the 7-app zoo; `apps/undici-raw` is the floor; the upstream `nestjs-undici` dependency and app are gone; mock backend runs with `logger: false`. Compose files, `k6-scripts/lib/benchmark.js` (new `SERVICES`/comparison keys), `test-node*.js` and `simple-test.sh` updated to match - the 3 compose files were updated in place, not consolidated into one `NODE_VERSION`-templated file (left for later, see performance.md). `generate-comparison-report.js` rewritten: new `CONFIGS`, a single `buildHeadline()` shared by a new `--headline <file>` flag (writes `<!-- bench-headline -->` markers) and `--docs`; no more `fastify_undici`/`INTERCEPTOR_BASES`/framework-impact code or the two-different-libraries "Interceptor Overhead" table; wording is ratio-only (`Nx throughput`, `NN% lower latency`, never "faster"/"overhead"/"fork"/"upstream" - `grep -niE "fork|upstream|faster|overhead|this repository" docs/benchmarks.md benchmarks/README.md` is clean). Added `benchmarks/e2e/` (`upstream.js`, `load.js`, `run.js`): a no-Docker/no-k6 A/B check (`npm run bench:ab`, ~20-40s locally) alternating axios/undici rounds of the shared Express app and reporting the throughput and p95-latency ratios; wired into `.github/workflows/benchmarks.yml` as an informational (`continue-on-error: true`) `ab_check` job on pull requests and on releases (`workflow_call`), timeout 5 minutes. `benchmarks/README.md` cut to ~75 lines (how-to-run only). Docker has no daemon in this sandbox, so the full Docker+k6 suite could not be run here; seeded `benchmarks/results/node24-performance-summary.json` and regenerated `docs/benchmarks.md`/the README headline from a short local run of the new apps through `benchmarks/e2e` instead (Node.js 24 only, said plainly in both places) - the next `full`/`publish` run of `benchmarks.yml` replaces these with real Docker+k6 numbers across Node 22/24/26. `benchmarks/npm run typecheck` and `npm run bench:ab` both green on Node 24; root `format:check`/`lint` unaffected (benchmarks/ is prettier-ignored, lint only covers `src`/`tests`). Left undone: single-template compose file consolidation (not required by the task, deferred); Fastify interceptor rows and a native-interceptor row (marked optional in docs-critic.md, skipped for time).
- 2026-09-26: PR #29 review:
  - The setup is fair: byte-identical app code, the same logging-free interceptor, keep-alive on both sides.
  - The reviewer's own run reproduced about 3.9x.
  - Fixes: the README's stale hand-typed "About 2x" lead-in is replaced by a number-free one; the generated headline now labels local-run numbers as preliminary until the full Docker + k6 run replaces them; the k6 harness shuffles the service order each run, instead of always running axios first.
- 2026-09-26: PR #29 merged: fair benchmarks (one shared Nest app, Express+Fastify, identical interceptor, raw-undici floor, shuffled k6 order), rewritten generator (ratio headline marked preliminary until a full Docker+k6 run), `bench:ab` informational PR check, how-to-run README. The full release benchmark run will replace the preliminary 4x local-run headline. PR #28's upstream job failed on first CI run (strategy b deadline hit and an IPv6 env-dependent test), sent back to its worker.
- 2026-09-26: PR #28 CI after merging #30: strategy (a) had 5 'now passing' tests. Four are #30's (progress x3, formSerializer) and were removed. The TCP-connect-timeout test depends on the runner's DNS and is now `environmentDependent`. One new failure, 'should support download rate limit', is a real undici slow-consumer issue (see the new phase 2 item) and is listed as a plan item.
- 2026-09-26: PR #28 merged: upstream conformance suites promoted to maintained CI jobs, both axios strategies and the @nestjs/axios suite green (see the roadmap item above for the full numbers).
- 2026-09-26: PR #31 open (`claude/conformance-fixes`): the four remaining Phase 2 items found by upstream conformance. `data:` URLs (a new `axios-data-url.adapter.ts`, ported from axios' `fromDataURI`/`estimateDataURLBufferAllocation`); `maxContentLength` now enforced for `responseType: 'stream'` too (an async-generator-wrapped `Readable`, matching axios' own streamed enforcement exactly); header values sanitized like axios in **two** passes at two different times, not one (control characters at `set()` time in `mergeHeaders`/`AxiosHeaders#set`; non-Latin1 bytes once, at actual dispatch, in `HttpService.executeRequest`'s new `sanitizeHeadersToByteString`) - the two-pass split was itself a fix during this PR, after the first, single-pass version broke axios' own "should allow request interceptors to encode Unicode header values before Node sends them" test by stripping Unicode before an axiosRef request interceptor could read/transform it; an unparsable `timeout` now gives `ERR_BAD_OPTION_VALUE`; a throwing `paramsSerializer` (or any other synchronous error building the request URL, e.g. an invalid `Date` in `params`) now rejects as a proper `AxiosError` with `url`/`exists` set, matching axios' own `AxiosError.from` `customProps` exactly (added a 6th parameter to this package's own `AxiosError.from`, an intentional, additive public-API change - `etc/nestjs-axios-undici.api.md` updated). **Decided not to implement** the call-site-stack part of "remaining error-shape gaps": a literal port of axios' mechanism (`Axios.prototype.request`'s `catch` block re-capturing and appending a fresh `Error.captureStackTrace`) measured 40%+ CPU/req on an all-error micro-benchmark scenario even capped at `stackTraceLimit: 1`, for materially less benefit than in axios - confirmed directly (a minimal repro, and this package's own axiosRef pipeline) that neither a real HTTP error nor a synchronously-thrown interceptor error keeps a link back to the caller of `axiosRef.get()`/`firstValueFrom()` the way axios' own `await`/`.then()` chain does, since this package's request path is an RxJS `Observable` (see the doc comment on `HttpService.dispatch`). This library's *existing* errors already keep a meaningful stack at zero extra cost (an interceptor's own thrown error is never wrapped; an HTTP error's stack shows its own `AxiosError`/`AxiosError.from` construction site). Upstream expected-failures: strategy (a) 11 entries now passing (removed); strategy (b) 1 entry now passing (removed, `maxContentLength`/stream, which lives in the shared `axios-response.adapter.ts` its bare adapter already reuses); strategy (b)'s `data:`/header-sanitization/params-error entries updated to a harness-limitation reason (its bare adapter bypasses `HttpService`/`buildAxiosConfig`/`normalizeAxiosRequest` entirely, so those fixes don't reach it - matching the existing, established wording for every other config-normalization-bypass entry in that file); one narrower, new finding recorded (strategy (a)'s "should display error while parsing params" fails only on an `instanceof` check against `AxiosError` imported directly from the axios clone's own file, bypassing this package's `axios`-peer-linking - a harness artifact, since `error.code`/`.message`/`.url`/`.exists` all match axios exactly, confirmed directly). Perf (`benchmarks/micro/compare.js`, paired against `claude/v1.0.0`, 5 rounds x 3s): get +2-7%, post +2-4%, config +1-9%, error +1-3%, interceptors -2 to +6% - all comfortably within the 10%/15% budget across two separate runs.
- 2026-09-26: PR #31 review fixes, on `claude/conformance-fixes`: HIGH, a socket leak on a compressed `responseType: 'stream'` response. `decompressStream` (`axios-response-type.adapter.ts`) used to `body.pipe(createGunzip())` and hand back only the decompressor; destroying *that* stream - either `guardStreamMaxContentLength` (`axios-response.adapter.ts`) throwing once the decoded byte count crosses the limit, or a plain `responseType: 'stream'` consumer just stopping reading early, no limit involved - never destroyed the raw undici body/socket behind it, since `.pipe()` only forwards data, never destruction, in either direction; confirmed live against a real server (an oversized gzip'd body left the socket open well past 2s where the uncompressed equivalent closed in under 0.5s - DoS-relevant, given the feature's whole point is capping a streamed download). Fix: `decompressStream` now wires bidirectional destroy propagation itself (`.once('error'/'close', ...)` on both streams), so both call sites are covered by one change, with no code change needed in `guardStreamMaxContentLength`. Added 4 disposal-asserting tests (limit-crossed and consumer-early-destroy, compressed and the already-working uncompressed case as a regression guard); the two compressed-case tests use a genuinely slow, chunked `Readable` rather than `Readable.from([oneBuffer])`, after confirming a single-buffered-chunk source drains into `.pipe()`'s destination immediately regardless of consumption speed and so reaches its own natural `'close'` even without the fix - the original, single-chunk version of the PR's own "enforces the limit against the DECODED byte count" test suffered exactly this blind spot and passed with or without the fix; both new gzip tests were confirmed to fail (via timeout) with the bug reintroduced, and pass with the fix restored. `docs/axios-supported-options.md`'s `maxContentLength` row and the changeset updated to describe the raw-body disposal accurately; also noted there that `beforeRedirect` header mutations aren't sanitized (matching axios, whose transport hits the same Node `http` per-hop path - no code change, per-hop redirect headers were never in scope for the sanitization added earlier in this PR). Full recheck: lint/format/typecheck/build/api:check clean, affected specs (28 tests) green, perf vs `claude/v1.0.0` all within budget (get +0.9%, post +5.6%, config +6.6% [re-measured once], error +8.9%, interceptors +5.8%).
- 2026-09-26: `claude/slow-consumer` (PR open): root-caused the `UND_ERR_SOCKET` slow-consumer phase 2 item to a real undici h1-client bug (`onHttpSocketEnd` in `lib/dispatcher/client-h1.js` treats the socket's `'end'` event as a premature close whenever `parser.statusCode` is still set, but a slow consumer leaves the parser paused on backpressure - so `onMessageComplete` hasn't reset it yet - for long enough that a short-`keepAliveTimeout` server's FIN can race it) - confirmed with instrumented undici internals (`parser.bytesRead` already matched `Content-Length`; `onMessageComplete` simply hadn't run yet) and against Node's own `http` client (unaffected, same scenario). Full analysis and the minimal repro: `plan/reports/undici-slow-consumer.md` (not filed upstream - the owner will). Mitigated in this package for a `Content-Length`-known download metered by `maxRate`/`onDownloadProgress` (`meterDownloadBody`, `axios-progress.adapter.ts`): a first attempt (swallow `UND_ERR_SOCKET` once our own forwarded-byte count matched `Content-Length`) measurably helped but didn't reliably fix the real case, since that count is itself paced by the same throttling that creates the race; fixed by attacking the trigger instead - the raw body is now drained as fast as undici delivers it (bounded by `Content-Length`, no worse than a buffered response already pays) instead of left backpressured by our own throttle, with the byte-count check kept as a belt-and-suspenders backstop. Confirmed directly against axios' own "should support download rate limit" upstream test (its fixture defaults `keepAliveTimeout` to 1s - the real-world trigger): failed before this fix, passes reliably after it (3 repeated isolated runs); `expected-failures.strategy-a.json`'s entry removed (strategy (b)'s own, unrelated entry for the same name is untouched - its bare adapter never wires up `maxRate` at all). A chunked/unknown-length response, and a plain `responseType: 'stream'` consumer with neither `maxRate` nor `onDownloadProgress` set, remain exposed - documented in `docs/axios-supported-options.md`/`docs/migration-guide.md`. Changeset: `minor`. `npm run lint`/`format:check`/`typecheck`/`build`/`api:check` and the affected specs (24 tests) green; perf vs `claude/v1.0.0` (5 rounds x 2s, this code path is gated behind `maxRate`/`onDownloadProgress` and untouched by every benchmarked scenario) all within budget: get +9.2% (re-measured), post -0.3%, config +8.0% (re-measured), error -6.0%, interceptors -4.2%.
- 2026-09-26: PR #33 review fixes (`claude/slow-consumer`). Two real problems in the first version's mitigation, both correctly flagged before merge:
  1. **Unbounded memory for `responseType: 'stream'`.** The eager-drain step ran for *every* responseType, so a `'stream'` download metered with `onDownloadProgress`/`maxRate` (a progress bar on a large file - exactly why a caller picks `'stream'` at all) could now sit fully in memory ahead of a slow consumer. Fixed: `meterDownloadBody` (`axios-progress.adapter.ts`) gained `options.buffered`, set by the call site (`axios-response.adapter.ts`) to `responseType !== 'stream'` - the eager drain now only runs for a response that's going to be fully buffered in memory regardless (`json`/`text`/`arraybuffer`/`blob`), where it costs nothing extra. Also gated on `options.maxContentLength`: even for a buffered response, skipped whenever the response's own `Content-Length` already exceeds it, so the streamed `maxContentLength` check (`readBufferWithLimit`/`readDecompressedBufferWithLimit`) still gets to reject an oversized body promptly instead of reading all of it first. `responseType: 'stream'`, a chunked/unknown-length response, and an over-`maxContentLength` response now keep today's plain, backpressured pipe unconditionally (still covered by the belt-and-suspenders `UND_ERR_SOCKET` check, kept for every responseType since it's provably safe - it only reclassifies an error that already happened, never buffers anything extra). New tests (`axios-progress.adapter.spec.ts`, "buffered vs stream backpressure"): confirmed two of the four fail against the pre-fix (unconditional eager drain) version by temporarily git-stashing the two adapter files back to that version, re-running, and restoring.
  2. **Abort/timeout no longer interrupted a still-pending buffered read.** Running this PR's own e2e specs under Node 24 (coordinator pointed out this sandbox can run them via `npx node@24` + jest's native `require(esm)`, not just Node 22's partial coverage) surfaced `tests/progress-form.e2e.spec.ts`'s "an abort mid-download still rejects with ERR_CANCELED" actually failing (resolving instead of rejecting) against the step-1-only fix: eager-draining finishes the raw undici transfer almost immediately, so a later abort has nothing left to interrupt at the transport layer, and a still-pending, `maxRate`-paced buffered read would just complete successfully, late. Fixed: `RequestAbortSignal` (`http.service.ts`) now supports more than one listener (an array, still cheaper than a real `EventTarget`); `toAxiosLikeResponse` gained an optional 4th `signal` parameter, threaded into `meterDownloadBody`'s `options.signal`, which registers its own `'abort'` listener alongside undici's and destroys the metered pipeline directly (with the signal's own `reason`, or a synthetic `AbortError` when the reason is nullish - this library's own RxJS-teardown abort path calls `abort()` with none at all) - independent of whether the raw transport already finished. Covers this library's own deadline `timeout` too (same `abortSignal`), for free.
  Re-verified after both fixes: `tests/progress-form.e2e.spec.ts` (16/16), `tests/compat/differential/` (222/222), the full `src/`+`tests/` jest suites under Node 24 (828/828 combined - this sandbox's Node 22 only misses 8 suites that need `@nestjs/testing`'s ESM, all otherwise green), axios' own "should support download rate limit" (3 more isolated runs, still passes), a full `run.mjs --strategy a` (0 new failures, twice), `lint`/`format:check`/`typecheck`/`build`/`api:check` clean. Perf (`benchmarks/micro/compare.js`, vs `claude/v1.0.0`, 5 rounds x 2s): get +9.9%, post -0.9%, config +9.0%, error +4.9% (re-measured), interceptors +5.0% - all within budget. `plan/reports/undici-slow-consumer.md`, the changeset, and `docs/axios-supported-options.md`/`docs/migration-guide.md` updated to describe the corrected, narrower scope (only a buffered, `maxContentLength`-safe download is fixed; `'stream'` remains exposed regardless of `maxRate`/`onDownloadProgress`).
- 2026-09-26: PR #32 open (`claude/docs-1.0`, the last 3 docs items): every documented behaviour re-checked against `src/`/the built `lib/` (a probe script, node 24) or a test, not against the older docs. `docs/features.md` deleted (fork-framed, stale interceptor-order claim), removed from the sidebar/README/index.html. The compatibility page (renamed in prose to "Axios Compatibility" everywhere it's linked) gets two accuracy fixes (`response.config.method` is lower-case, not upper-case; the "Performance Note" - retitled "Configuring undici directly" - implied a per-request mapping cost, it's once-at-setup) and 6 newly-documented, real differences found via the differential harness's `knownDifference` cases and the axios upstream conformance suite's `expected-failures.strategy-a.json`: a dropped connection's `error.message` (undici's "other side closed" vs axios' "socket hang up"), duplicate response headers as an array vs axios' joined string, `axiosRef.interceptors.request.handlers`/`.forEach` not existing, a bare `number`/`boolean` request body forwarded instead of rejected, a mismatched `Content-Length` header rejected instead of trusted, and no `allowedSocketPaths` allowlist - none fixed (no `src/` change), all now documented and listed in the PR as candidate future items. `docs/migration-guide.md` trimmed ~450 → ~150 lines: the 0.5.x rename moved to the end; tutorial content duplicated elsewhere removed; wrong "use interceptors for transforms" advice fixed; the unsafe `pipelining: 10` example dropped; stale hand-typed perf numbers replaced with a link; every BREAKING item from `.changeset/*.md` kept as a condensed, linked bullet. The dispatcher-configuration snippet now lives only in `docs/guides/configuration.md`; every other page links to it. `docs/guides/testing.md`'s broken example (undefined `LoggerService`, an assertion no shown interceptor satisfies) fixed; "module options"/lower-case "undici" made consistent. README: dropped the stale "65-test"/Node-26-LTS claims; fork mention already only in Credits; generated benchmark headline untouched. A one-off link-check script (not committed) confirmed all 50 relative doc links resolve. `format:check`/`lint`/`typecheck`/`build`/`test:examples` all green; `git diff --stat src/` empty.
- 2026-09-26: PR #34 open (`claude/dup-response-headers`): `fix: join duplicate response headers like Node/axios`. undici's own header parser accumulates a repeated response header name into an array uniformly; axios (Node's `http`) instead applies `IncomingMessage.headers`'s per-name rules, ported from Node 22's `_http_incoming.js` (`matchKnownFields`/`_addHeaderLine`, checked against the shipped source): `set-cookie` stays an array, `cookie` joins with `'; '`, a fixed "no duplicates" set (`content-type`, `content-length`, `user-agent`, `referer`, `host`, `authorization`, `proxy-authorization`, `if-modified-since`, `if-unmodified-since`, `from`, `location`, `max-forwards`, `retry-after`, `etag`, `last-modified`, `server`, `age`, `expires`) keeps only the first value, everything else joins with `', '`. New `joinDuplicateHeaders()` in `axios-response.adapter.ts`, applied once in `toAxiosLikeResponse` before `content-type`/`content-encoding`/`content-length` are read, before `transformResponse` sees the headers, and before they reach the caller - also fixes a latent crash where a duplicated `Content-Type` reached `.trim()` as an array. Common case (no duplicates) costs one `Array.isArray` check per header name and no allocation. Flips `response.diff.spec.ts`'s "headers: casing, multi-value set-cookie, duplicates" known difference to an equality case (verified by hand against real axios with a standalone script, since this sandbox's Node 22 can't run the `@nestjs/testing`-based Jest suites - same pre-existing limitation as every other e2e/differential spec here). New unit tests (`axios-response.adapter.spec.ts`) and an e2e spec (`tests/duplicate-response-headers.e2e.spec.ts`, a raw `net` server sending genuinely duplicated `X-Foo`/`Content-Type`/`Set-Cookie`/`Cookie` headers); each new/flipped test confirmed to fail without the fix. Perf (`benchmarks/micro/compare.js`, vs `claude/v1.0.0`, 5 rounds x 3s): get +0.5%, post +4.2%, config +4.0%, error +7.7%, interceptors +6.0% - all within the 10%/15% budget. `docs/axios-supported-options.md` doesn't mention this difference on this branch, so no doc change needed; noted in the PR that the parallel docs PR #32 (`claude/docs-1.0`) is expected to add a line about it, to be removed once both merge. Changeset: `minor`, BREAKING (a duplicated header, other than `set-cookie`, now comes back as a string instead of a `string[]`).
- 2026-09-26: PR #34 merged into `claude/v1.0.0`. `claude/docs-1.0` merged `origin/claude/v1.0.0` (this commit) and, in a follow-up commit, removed the now-fixed "duplicate response headers as an array" difference from `docs/axios-supported-options.md` and added a migration-guide bullet for it.
- 2026-09-26: PR #32 review round 2 (coordinator): re-checked every `.changeset/*.md` line by line against `docs/migration-guide.md` (not just BREAKING-labelled ones) and added what was missing (`registerAsync({})` throwing, `response.headers`'s TS type change, axiosRef interceptor order, `response.config`/`error.config` shape, unsubscribe-aborts, per-subscription interceptor re-run, response-body decoding, default headers moved out of the 0.5.x section where they didn't belong, progress/`maxRate`/`formSerializer`). Fixed two inaccuracies from round 1: the `Content-Length`-mismatch error code (`UND_ERR_REQ_CONTENT_LENGTH_MISMATCH`, not `ERR_BAD_REQUEST`) and a same-file anchor broken by the "Performance Note" rename (`#configuring-undici-directly`) - the link-checker now also validates same-file `#anchor`s. Triaged all 27 "not yet root-caused" entries in `expected-failures.strategy-a.json` with a repro against the built package for each: found 6 further real gaps beyond the 3 the coordinator named (a numeric-string `timeout`, a throwing `beforeRedirect` not wrapped, `Content-Encoding: compress` not decoded, a request-stream-destroy giving the wrong error code on the response stream, a malformed URL silently dispatched instead of rejected, and an `AbortSignal` firing after stream emission being ignored) - each documented on the compatibility page and added as its own phase 2 item above; reclassified 6 entries as already-documented/harness-limitation; removed 1 stale "now passing" entry (`CancelToken` cancelling multiple in-flight requests). Bulk-relabeled all 39 matching strategy-(b) entries as harness limitations (the bare adapter's documented config-normalization bypass).
- 2026-09-26: PR #32 CI: the CancelToken-multiple entry removed as 'now passing' still fails on Actions, so it's restored with a plan item; the TCP-connect-timeout test passed in strategy (b) this time (DNS race), so it's `environmentDependent` there too.
- 2026-09-26: PR #33 merged `claude/v1.0.0` (after PR #32): conflicts in the docs, plan.md and the strategy-(a) list resolved. #32's rewritten docs are kept, with #33's slow-consumer notes re-added in short form (`responseType` and `maxRate` rows, one migration-guide caveat). The download-rate-limit entry stays removed. The coordinator also restored `RequestAbortSignal`'s single-listener fast path; the micro benchmark is within ±3% of base.
- 2026-09-26: PR #35 open (`claude/axios-gaps`): the three remaining phase 2 items PR #32's docs pass found as known gaps. **zstd decompression**: wired into the existing `decompressBuffer`/`decompressStream` (`axios-response-type.adapter.ts`), covering buffered and `responseType: 'stream'`, `decompress`/`decompress: false`, and streamed `maxContentLength` for free (both already generic over the encoding). Feature-detected (`isZstdSupported = typeof createZstdDecompress === 'function'`), same as axios; every Node this package supports already has it (added in 22.15.0/23.8.0), so the "unsupported" fallback (raw bytes pass through, matching axios) is covered by a unit test that mocks `node:zlib` to omit it. **Decided:** the default `Accept-Encoding` still doesn't advertise `zstd` - axios itself only does when `transitional.advertiseZstdAcceptEncoding: true` is explicitly set (default `false`); this matches axios' own out-of-the-box behaviour, and a caller can already override the header the normal way (no new option added for it). **`parseReviver`**: threaded through `parseJsonOrText`/`parseTextMaybeJson` into both default-JSON-parsing call sites; request > `axiosRef.defaults` > module precedence (added to `DEFAULTS_PASSTHROUGH_KEYS`), same as every other passthrough default; no cost when unset (a plain function-reference check). **Redirect `sensitiveHeaders`**: axios' own two-part rule, checked against real axios 1.20 source - the built-in `Authorization`/`Cookie`/`Proxy-Authorization` stay subdomain-exempt/downgrade-only (`shouldStripSensitiveHeaders`, unchanged), while a header named in `config.sensitiveHeaders` gets a *stricter*, purely origin-based rule (`isSameOriginRedirect` in axios' `lib/adapters/http.js`, ported as `isSameOrigin`): dropped on any origin change, including a subdomain redirect or an http→https upgrade. Validated the same way axios validates it (`ERR_BAD_OPTION_VALUE`, "sensitiveHeaders must be an array of strings"), checked eagerly in `HttpService.executeRequest` before ever dispatching, whenever redirects are actually followed (`maxRedirects` isn't `0`) - matching axios' own timing (it validates while building the follow-redirects transport options, not lazily on the first redirect). Supported on per-request config, `axiosRef.defaults`/`axiosRef.create({ sensitiveHeaders })` and module options, same precedence as every other option (added to `DEFAULTS_PASSTHROUGH_KEYS` too, after axios' own "should strip sensitiveHeaders configured on an instance" upstream test - `axios.create({ sensitiveHeaders })` - showed the module-only precedence this PR started with wasn't enough).
  - Tests: unit (`axios-response-type.adapter.spec.ts` gained zstd + `parseReviver` describe blocks; a new `axios-response-type.adapter.zstd-unsupported.spec.ts` mocks `node:zlib` for the fallback path; a new `redirect.adapter.spec.ts` covers `normalizeSensitiveHeaders` and the lenient-vs-strict rule matrix; `http.service.spec.ts` gained the `sensitiveHeaders` validation/precedence cases), e2e with real servers (`tests/zstd-decompression.e2e.spec.ts`, `tests/parse-reviver.e2e.spec.ts`, `tests/redirects.e2e.spec.ts` gained a `sensitiveHeaders` describe block including `axiosRef.create()`), and differential cases against real axios/`@nestjs/axios` (`tests/compat/differential/response.diff.spec.ts` gained zstd + `parseReviver` cases, `errors.diff.spec.ts` gained 3 `sensitiveHeaders` redirect cases) - every new/flipped test confirmed to fail without its fix (checked directly, not assumed, including the `axiosRef.create({ sensitiveHeaders })` case). `test:jest`: 883 → 905.
  - Upstream conformance (axios' own `tests/unit/adapters/http.test.js`): strategy (a) 141 → 149 passing, 8 entries removed from `expected-failures.strategy-a.json` (both `sensitiveHeaders` redirect cases, `should reject invalid sensitiveHeaders config`, `should strip sensitiveHeaders configured on an instance`, the 3 zstd decompression cases, and the JSON reviver case); `should advertise zstd when enabled through transitional config and supported` stays an expected failure, reworded to say plainly this is a deliberate non-implementation, not an open gap. 0 new failures both times it was run (once before, once after the `axiosRef.defaults`-level `sensitiveHeaders` fix). Strategy (b) (the bare-adapter transport, which bypasses `HttpService`'s config normalization): 3 zstd entries also now pass (decompression itself needs no config normalization - `toAxiosLikeResponse` is called directly) and were removed; the `parseReviver`/`sensitiveHeaders` entries correctly stay as harness-limitation (that bare adapter never forwards either field) - both strategies fully re-verified at 0 discrepancies after every code change.
  - Docs: `docs/axios-supported-options.md` - the three known-gap rows PR #32 added are now ✅ (`decompress`, `parseReviver`, `sensitiveHeaders`), plus the default-headers row (`Accept-Encoding`/zstd), the `Content-Encoding` response row, and the `axiosRef.defaults.*` list. Changeset: `minor` (`.changeset/axios-gaps-zstd-reviver-sensitive-headers.md`).
  - Perf (`benchmarks/micro/compare.js`, Node 24, 5 rounds × 3s, paired against `claude/v1.0.0`, proxy env vars unset): get +0.4%, post -4.8%, config +0.5%, error -0.4%, interceptors +2.2% (thresholds 10%/15%) - all comfortably within budget.
  - `npm run lint`/`format:check`/`typecheck`/`typecheck:compat`/`build`/`api:check` (no new exported symbols; `parseReviver`/`sensitiveHeaders` are new fields on the already-exported `AxiosLikeRequestConfig`/`AxiosRefDefaults`/`HttpModuleOptions`, `npm run api:update` committed), `test:jest` (905/905) and `test:examples` all green on Node 24.
- 2026-09-26: README: added a "Tested against the upstream test suites" bullet and an Upstream conformance badge (owner request). No hand-typed pass counts; it links the expected-failure lists instead. `upstream.yml` now also runs on pushes to `main`, so the badge reflects the released code.
- 2026-09-26: PR #37 open (`claude/compat-request`): four request-side phase 2 items. **Numeric-string `timeout`**: `HttpService.executeRequest` normalises a string `timeout` with `parseInt` (axios' own check) right after the existing `isUnparsableTimeout` gate (which still runs against the *original*, unparsed value, so its `ERR_BAD_OPTION_VALUE` behaviour is unchanged), then fixes up `headersTimeout`/`bodyTimeout` (set to the same raw string by `dispatchFastPath`) alongside it - a `typeof` check only, no cost for an already-numeric `timeout`. **Throwing `beforeRedirect`**: `buildRedirectHop` (`redirect.adapter.ts`) now wraps it with a new `createRedirectionFailureError`, porting follow-redirects' own `createErrorType`/`RedirectionError` wrapping exactly (`code: 'ERR_FR_REDIRECTION_FAILURE'`, message `"Redirected request failed: <message>"`, a plain enumerable `.cause`, and even the same `name: "Error [ERR_FR_REDIRECTION_FAILURE]"` axios' own `AxiosError.from` copies onto the final error) - confirmed against real axios' "should support beforeRedirect"/"...with the original URL" tests. **Malformed URL**: a new `malformedHttpProtocolUrl` (`http.service.ts`) ports axios' `assertValidHttpProtocolURL`/`normalizeURLForProtocolCheck` (`buildFullPath.js`) - guarded by the same `isHttpOrHttpsPrefix` fast path `unsupportedProtocol` already uses, so a well-formed URL pays nothing extra; checked directly against real axios' "rejects malformed HTTP URLs before Node URL normalization and preserves config" test (same message, same `config.url` preserved as the *original* un-normalised string). **BREAKING** (a URL that used to be silently "fixed" and dispatched now rejects synchronously) - changeset + migration guide bullet. **CancelToken investigation**: root-caused with a direct repro *and* the real GitHub Actions job log (2026-09-26, run #16) - both give the identical, deterministic `TypeError: Cannot read properties of undefined (reading 'source')` at `axios.CancelToken.source()`, in every environment, not just CI. Cause: strategy (a)'s harness shim (`axiosref-instance.mjs`) substitutes this package's own `axiosRef` for axios' entire default export, and `axiosRef` has no `.CancelToken` static - a legacy top-level class deliberately dropped in an earlier API-trim decision (this package's shape-level `config.cancelToken` support was never touched). Confirmed no real cancellation bug: strategy (b) (real `axios`, only its adapter swapped) has no expected-failure entry for this test at all; a 20-iteration stress repro of the exact scenario (5 concurrent requests, one shared `cancelToken`, cancel synchronously right after they start), via this package's own `AxiosCancelTokenLike` shape, passed every time - pinned permanently as a differential test (`tests/axios-compatibility-matrix.e2e.spec.ts`). `expected-failures.strategy-a.json`'s entry rewritten from "cause not isolated" to this root-caused explanation; kept as an expected failure (harness limitation, nothing to fix in `src/`).
  - Also: `tests/upstream/lib/conformance.mjs` now captures the first line of a failing test's `failureMessages[0]` (both Jest's and Vitest's JSON reporters carry this) and prints it under each *new*-failure entry in the job summary - a future CI-only failure no longer needs a full job-log pull to see what broke.
  - Tests: unit (`http.service.spec.ts`, `redirect.adapter.spec.ts`, `axios-error.spec.ts` - every new/changed case confirmed to fail without its fix via a scoped `git stash` of just the relevant source file), e2e with real servers (`redirects.e2e.spec.ts`, `axios-compatibility-matrix.e2e.spec.ts`), and differential against `@nestjs/axios` (`errors.diff.spec.ts`, 5 new cases). `test:jest`: 923/923 on Node 24.
  - Upstream conformance: 4 entries removed from `expected-failures.strategy-a.json` (numeric-string timeout, both `beforeRedirect`-throws cases, the malformed-URL case).
  - Perf (`benchmarks/micro/compare.js`, Node 24, 5 rounds × 3s, paired against a `claude/v1.0.0` worktree build): get -2.9%, post -2.3%, config +0.2%, error +6.0%, interceptors +1.2% - all within budget (10%/15%).
  - Docs: `docs/axios-supported-options.md` (`timeout`/`beforeRedirect` rows flipped ⚠️→✅, a new malformed-URL row), `docs/migration-guide.md` (numeric-string timeout + `beforeRedirect` bullets, a BREAKING malformed-URL bullet). Changeset: `minor` (`.changeset/request-side-axios-parity.md`).
- 2026-09-26: PR #38, open: `fix: response-side axios parity (compress, decode errors, content-encoding, strict JSON, stream cancellation)` - the 5 remaining phase 2 items PR #32's docs pass found (parallel to `claude/compat-request`/PR #37, which took the other 4).
  - **`Content-Encoding: compress`/`x-compress`**: aliased onto the existing `gzip` decode path (`GZIP_ENCODINGS` in `axios-response-type.adapter.ts`), same as axios (which doesn't implement the old LZW scheme either, just aliases the name onto its own gzip decoder) - covers buffered, `responseType: 'stream'`, `decompress: false`, and the streamed `maxContentLength` check for free (both already generic over the encoding). Default `Accept-Encoding` updated to `gzip, compress, deflate, br`, matching axios' own default exactly (`SUPPORTED_CONTENT_ENCODINGS`).
  - **`Content-Encoding` header deletion after a successful decode**: `toAxiosLikeResponse` now deletes it from `response.headers` under axios' exact conditions (`isDecodableEncoding`, a new predicate kept in lockstep with `decompressBuffer`/`decompressStream`) - unconditionally for `HEAD`/`204`, or when the encoding was actually decoded; never for `decompress: false` or an unrecognized encoding. `content-length` is left untouched, matching axios (no such line in its own `http.js`). **BREAKING**, documented.
  - **Corrupt/truncated compressed body**: `toAxiosLikeResponse`'s catch block now wraps a genuine zlib decode failure (`isZlibErrorCode`, gated on the `Z_*` code prefix so a real network error mid-decompression still reaches `fail()`/`toAxiosError`'s own shaping unwrapped) into a real `AxiosError` - `AxiosError.from(err, undefined, undefined, requestInfo, partialResponse)` for the buffered path, matching axios' own `handleStreamError` shape exactly (`response` attached with `data` never assigned, matching axios; `.request` set, unlike axios' own `null` there for the *strict-JSON* case, which is a different branch entirely - checked against real axios 1.20 line by line). The `responseType: 'stream'` case is a deliberate improvement over axios itself (which leaves a stream's own decompression errors completely unwrapped): a new `wrapStreamCancellation` (see below) also translates a zlib error reaching the already-returned stream, via a `PassThrough` layer (needed because once undici has already called `.destroy(err)` on the source, its `'error'` listeners can't swap the error object for whichever listener - the caller's own - reads it).
  - **Real regression found and fixed during this same PR**: the corrupt-body wrap above initially caught the axios upstream conformance suite's own "should not fail with an empty response (with|without) content-length header (Z_BUF_ERROR)" tests, which used to pass only because the *old*, less precise fallback silently swallowed the same `Z_BUF_ERROR` into an empty string. Root cause: this library's zlib decoders never used axios' own flush-tolerant options (`finishFlush: Z_SYNC_FLUSH`/`BROTLI_OPERATION_FLUSH`/`ZSTD_e_flush` - `lib/adapters/http.js`'s `zlibOptions`/`brotliOptions`/`zstdOptions`), so an empty or merely-truncated (but structurally valid) compressed body threw `Z_BUF_ERROR` instead of resolving with `''`/the partial bytes, exactly like real axios does. Fixed by adding the same flush options to every decoder (`GZIP_FLUSH_OPTIONS`/`BROTLI_FLUSH_OPTIONS`/`ZSTD_FLUSH_OPTIONS`, `axios-response-type.adapter.ts`) - confirmed directly (`gunzipSync`/`createGunzip` with vs. without the flush options, against empty/truncated/genuinely-corrupt inputs) that this changes only the empty/truncated cases, not the genuinely-corrupt one. The "truncated gzip body rejects" tests this PR had written before finding this were themselves testing the wrong expectation (axios doesn't throw for a merely truncated body either, once its own flush options are matched) - rewritten to assert the corrected (non-throwing, partial-data) behaviour instead, and kept as a regression guard.
  - **`transitional.silentJSONParsing: false`**: previously accepted for type compatibility only (the doc comment said outright it described the always-silent default). Now honoured, matching axios' exact condition (`strictJSONParsing = !silentJSONParsing && responseType === 'json'` - `lib/defaults/index.js`): a `JSON.parse` failure throws `ERR_BAD_RESPONSE` with `response.data` the raw text (`STRICT_JSON_RAW_TEXT`, a symbol tagging the original `SyntaxError` so `toAxiosLikeResponse`'s catch block - which never sees the raw text otherwise, local to `readBodyAsResponseType` - can attach it). Every other `responseType`, including the plain default, is provably unaffected (axios' own `JSONRequested` gate requires `responseType === 'json'` too) and stays zero-cost (a single property read). `transitional` added to `AxiosRefDefaults`/`DEFAULTS_PASSTHROUGH_KEYS` (api-extractor updated, an intentional additive change) so it now also works on `axiosRef.defaults.transitional` and at module level with the usual request > defaults > module precedence, not just per-request as before.
  - **`responseType: 'stream'` cancellation, both triggers**: (1) destroying the caller's own upload body stream (`config.data`) mid-request - confirmed directly, with no code of this library's own involved, that undici *itself* already reacts to that by erroring the in-flight response body with `UND_ERR_ABORTED`/`AbortError` (axios' matching upload-side `data.on('close', ...)` watcher exists only to *trigger* the abort Node's own `http` wouldn't otherwise raise; undici already raises it as part of the response body's own lifecycle here, so no matching upload-side code was needed, only the translation below); (2) the caller's own `config.signal` firing *after* the stream was already handed back - `HttpService.executeRequest`'s `onUserAbort` no longer stops calling `abortSignal.abort()` once `settled` (removed a stale guard whose own comment described the exact limitation being fixed here), and the teardown/`SIGNAL_CLEANUP` cleanup that used to run immediately after emission (RxJS auto-unsubscribe) is now deferred - via a new `openStream` tracker - until the stream itself closes, mirroring axios' own `stream.finished(data, onFinished)`. Both triggers land on the same new `wrapStreamCancellation` (`axios-response.adapter.ts`): a `PassThrough` wrapper (needed for the same "can't swap an already-destroyed source's error" reason as the corrupt-body case above) that translates `UND_ERR_ABORTED`/`AbortError` into a real `CanceledError`, applied only when at least one of the two triggers, or an active decompression (for the corrupt-body case), is actually possible for this request (`needsStreamCancellationWrap`) - a plain streamed download with neither pays nothing extra (verified: `response.data` is the literal same undici body object, not a wrapper, in that case).
  - Tests: unit (`axios-response-type.adapter.spec.ts` gained `compress`/`x-compress`, `parseJsonStrict`/`STRICT_JSON_RAW_TEXT`, and `isDecodableEncoding` describe blocks; `axios-response.adapter.spec.ts` gained 4 new describe blocks - header deletion, corrupt-body wrapping, `silentJSONParsing`, and stream cancellation, the last including an explicit zero-cost/reference-equality check and a non-abort-shaped-error guard), e2e with real servers (`tests/compress-decompression.e2e.spec.ts`, `tests/corrupt-compressed-body.e2e.spec.ts`, `tests/strict-json-parsing.e2e.spec.ts`, `tests/stream-cancellation.e2e.spec.ts` - the last mirrors axios' own upstream fixture shape for both triggers, with no fixed sleeps), and differential against real axios/`@nestjs/axios` (`tests/compat/differential/response.diff.spec.ts` gained `compress`, header-deletion, corrupt-body (buffered equal, `responseType: 'stream'` a `knownDifference` - real axios itself never wraps that case) and `silentJSONParsing` cases) - every new/flipped test confirmed to fail without its fix (checked directly against each source file's pre-fix version, including the two zero-cost/precision guards). `test:jest`: 923 → 999 (post-merge; +76 net) on Node 24.
  - Upstream conformance, re-run after merging `claude/v1.0.0` (PR #37, request-side parity) into this branch: strategy (a) 0 new failures, 0 to fix (fully in sync post-merge) - 5 entries removed earlier in this PR's own development (before the merge): the 3 `compress` cases, "should destroy the response stream with an error on request stream destroying", and "should be able to abort the response stream". Strategy (b) also re-run per the coordinator's note (its bare adapter reuses `toAxiosLikeResponse`, so response-side fixes reach it too): 4 entries removed (the 3 `compress` cases, plus "should support gunzip error handling" - the latter's own `error.response.headers` assertion no longer trips a *different*, incidental issue once the corrupt-body path attaches a real `response` object) - 0 new failures, 0 more to fix after the merge.
  - Perf (`benchmarks/micro/compare.js`, Node 24, 5 rounds × 3s, paired against a fresh `claude/v1.0.0` worktree build - post-merge, so it already includes PR #37): get +5.7%, post -0.3%, config -1.1%, error +2.7%, interceptors -4.2% - all within budget (10%/15%).
  - Docs: `docs/axios-supported-options.md` (`signal`, `responseType`, default-headers, `transitional`, `decompress` and the `Content-Encoding` response rows updated; a new `transitional.silentJSONParsing` row), `docs/migration-guide.md` (new bullets: compress decoding, the BREAKING `Content-Encoding`-deletion, corrupt-body shaping, stream cancellation, `silentJSONParsing`). Changeset: `minor` with a BREAKING note (`.changeset/response-side-axios-parity.md`).
  - **PR #38 review round 2, pushed to the same branch/PR**: (1) **HIGH, fixed**: the corrupt-body wrap above only actually caught zlib (`Z_*`-prefixed codes) - brotli's own decode error code (`ERR__ERROR_FORMAT_PADDING_1`-shaped) and zstd's (`ZSTD_error_*`) went unwrapped, both for buffered (silently missing `.response`) and `responseType: 'stream'` (raw, unwrapped error). Replaced the code-shape sniff (`isZlibErrorCode`) with a `DECODE_ERROR` symbol tagged at the actual origin - inside `decompressBuffer`'s catch block, and on `decompressStream`'s decompressor-side `'error'` listener only (a `bodyErroredFirst` flag, set the instant the *source* body errors first, keeps a forwarded network error like `UND_ERR_SOCKET` from being mistagged as a decode error just because it surfaces through the decompressor) - so it now works uniformly for every codec, including a future one, with no per-codec code-shape knowledge at all. Fixed the doc comment's `Z_*` claim to match. Tests: corrupt-brotli/corrupt-zstd cases added alongside the existing corrupt-gzip ones in the unit specs (`axios-response.adapter.spec.ts`, buffered + stream), the e2e spec (`corrupt-compressed-body.e2e.spec.ts`, converted to `it.each` over all 3 codecs), and the differential spec (`response.diff.spec.ts`, new `/corrupt-br`/`/corrupt-zstd` routes, both cases now generated per-codec) - the two new `responseType: 'stream'` differential cases (br/zstd) confirmed to fail against the pre-fix source (pre-fix, those errors passed through unwrapped, coincidentally matching axios' own never-wraps-a-stream-error behaviour, so the "known difference" assertion itself failed); the buffered differential cases don't independently catch this regression (they don't check `.response`), so the unit/e2e tests are what's load-bearing there - both confirmed to fail pre-fix. Also added a negative-control unit test: a genuine `UND_ERR_SOCKET` network error injected mid-decompression still reaches the caller raw, unwrapped, proving the tag doesn't over-fire. (2) **MEDIUM, documented**: the deferred-listener-removal behaviour above (item 5/6's `openStream` tracker) means an abandoned, never-drained, never-destroyed `responseType: 'stream'` response keeps its `abort` listener on the caller's signal forever - intentional axios parity (`stream.finished`'s own lifetime), not a bug, but easy to hit with a shared/long-lived signal. Documented in the `signal` row of `docs/axios-supported-options.md`, including a note that it also delays `HttpService.onModuleDestroy` (see the new Phase 2 item below). Pinned with a new unit test file, `http.service.stream-signal-listener.spec.ts` (5 cases: exactly one listener while the stream is open, removed on drain, removed on `.destroy()`, still present when abandoned, and never attached to begin with for a non-stream `responseType`) - 4 of the 5 confirmed to fail against the pre-`openStream` version of `http.service.ts` (`fad632f`, the commit before this PR started). (3) **Minor, fixed**: `corrupt-compressed-body.e2e.spec.ts`'s truncated-body assertions (`chunks.length >= 0`, always true) now compare the actual decoded bytes against each codec's own sync decompressor with axios' flush options as the reference (`Buffer.compare`), for both the buffered and `responseType: 'stream'` cases, all 3 codecs. (4) **plan.md**: added the new Phase 2 item below (found during this review, not fixed here).
- 2026-09-26: release prep (`claude/release-prep`): deleted `plan/prototypes/`, since every prototype is now maintained under `tests/`, `scripts/` or `benchmarks/` (only comments referred to it; reworded). Added the 1.0 major changeset `.changeset/v1-stable.md`, and repointed three older changesets' migration-guide links whose anchors PR #32's rewrite removed (they end up in the published CHANGELOG).
- 2026-09-26: PR #41 (`claude/shutdown-grace`): resolved the last open Phase 2 decision item, `HttpService.onModuleDestroy` waiting forever for an unconsumed `responseType: 'stream'` response. `close()` races an internal 5s grace period, then force-aborts what's left. Found and worked around a real undici limitation along the way (`Agent#close()` clears its own per-origin registry synchronously, so a later `Agent#destroy()` on the same instance is a no-op) by tracking and aborting each open stream's own per-request signal instead - see the rewritten Phase 2 item above for the full writeup. 4 new e2e tests, each confirmed to fail without the fix; full `test:jest` (1025/1025), lint/format/typecheck/typecheck:compat/build/api:check all green (no public API change); perf within budget on every scenario (none exercise `responseType: 'stream'`, so none should move, and none did beyond normal noise).
- 2026-09-26: PR #43 (`claude/coderabbit-fixes`) open: 4 runtime-code CodeRabbit findings against `claude/v1.0.0`, all confirmed with a repro/failing test first (item 5, `index.html`'s throughput wording, dropped from scope - PR #42/`claude/throughput-chart` is rewriting that copy). See the Phase 6 sub-bullet above for the fixes. Full `test:jest` 1043/1043; lint/format/typecheck/typecheck:compat/build/test:examples all green; `api:check` needed `api:update` for the additive `AxiosRefDefaults` fields (and the now-stale `api-surface.spec.ts` allowlist entries for `maxContentLength`/`maxBodyLength` removed); upstream conformance strategy (a): 0 new failures, 0 fixed (nothing to remove from the expected-failures list); perf within budget on every scenario.
- 2026-09-26: `claude/docs-plain-language` (docs-only): applied the owner's unslop style guide to every docs-site page except README.md/index.html/docsify/sidebar.md (PR #42's) and the generated docs/benchmarks.md, plus tests/upstream/README.md and benchmarks/README.md's prose. No facts, option names, error codes, numbers or links changed; only prose mechanics (dash-as-connector, mid-sentence colons, parenthetical asides, sentence-case headings). Wrote and ran a link/anchor checker over every *.md file outside node_modules/plan//.changeset: all 152 links resolve. `npx prettier --check .` and `npm run test:examples` both green. Merged the latest `origin/claude/v1.0.0` (PR #42) first; it touched only the bench-headline block in benchmarks/README.md and files this PR doesn't own, so the merge was clean with no doc-content conflicts.
- 2026-09-26: `claude/coderabbit-tests-docs` (PR #47): worked the 18-finding CodeRabbit review of the tests/docs/docsify/examples slice (PR #45, `review/1.0-tests-docs`). 12 valid (fixed) and 6 not valid (left as is, with a reason each) - see the Phase 6 sub-bullet above for the breakdown. Replies for every finding written to `.claude/coderabbit-45-replies.json` (not posted to GitHub, per instructions). No changeset (nothing published changes). Full `test:jest` 1043/1043 on Node 24; lint/format:check/typecheck/build/`test:examples` all green; upstream conformance strategy (b) re-run: 0 new failures, 6 fixed and removed from `expected-failures.strategy-b.json`.
