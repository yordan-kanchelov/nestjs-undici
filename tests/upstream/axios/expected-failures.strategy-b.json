{
  "supports http with nodejs DNS should support a custom DNS lookup function": "harness limitation: instruments Node’s own `http.Agent`/socket pooling and error-listener internals directly (`net.Socket`, keep-alive agent internals, MaxListenersExceededWarning bookkeeping, a custom `lookup` function passed straight to Node’s `http`/`https`) - this package is undici-backed and has no equivalent internals to instrument.",
  "supports http with nodejs DNS should support a custom DNS lookup function (async)": "harness limitation: instruments Node’s own `http.Agent`/socket pooling and error-listener internals directly (`net.Socket`, keep-alive agent internals, MaxListenersExceededWarning bookkeeping, a custom `lookup` function passed straight to Node’s `http`/`https`) - this package is undici-backed and has no equivalent internals to instrument.",
  "supports http with nodejs DNS should support a custom DNS lookup function that returns only IP address (async)": "harness limitation: instruments Node’s own `http.Agent`/socket pooling and error-listener internals directly (`net.Socket`, keep-alive agent internals, MaxListenersExceededWarning bookkeeping, a custom `lookup` function passed straight to Node’s `http`/`https`) - this package is undici-backed and has no equivalent internals to instrument.",
  "supports http with nodejs DNS should support a custom DNS lookup function with address entry (async)": "harness limitation: instruments Node’s own `http.Agent`/socket pooling and error-listener internals directly (`net.Socket`, keep-alive agent internals, MaxListenersExceededWarning bookkeeping, a custom `lookup` function passed straight to Node’s `http`/`https`) - this package is undici-backed and has no equivalent internals to instrument.",
  "supports http with nodejs DNS should support a custom DNS lookup function with address entry passing": "harness limitation: instruments Node’s own `http.Agent`/socket pooling and error-listener internals directly (`net.Socket`, keep-alive agent internals, MaxListenersExceededWarning bookkeeping, a custom `lookup` function passed straight to Node’s `http`/`https`) - this package is undici-backed and has no equivalent internals to instrument.",
  "supports http with nodejs Data URL should allow a base64 data URL at the Buffer allocation limit": "plan.md phase 2 \"fix: support data: URLs\": fixed for the real product (HttpService.executeRequest now resolves a data: URL entirely locally - see strategy (a), which passes) - harness limitation for strategy (b) specifically: this bare adapter (tests/upstream/axios/adapters/undici-adapter.cjs) reimplements its own unsupportedProtocol check directly rather than calling HttpService.executeRequest, so it never reaches the new data: URL handling (lib/modules/http/adapters/axios-data-url.adapter.js). Strategy (a) is the one to trust for whether this is a real product gap.",
  "supports http with nodejs Data URL should count ignored input after base64 padding toward the Buffer allocation limit": "plan.md phase 2 \"fix: support data: URLs\": fixed for the real product (HttpService.executeRequest now resolves a data: URL entirely locally - see strategy (a), which passes) - harness limitation for strategy (b) specifically: this bare adapter (tests/upstream/axios/adapters/undici-adapter.cjs) reimplements its own unsupportedProtocol check directly rather than calling HttpService.executeRequest, so it never reaches the new data: URL handling (lib/modules/http/adapters/axios-data-url.adapter.js). Strategy (a) is the one to trust for whether this is a real product gap.",
  "supports http with nodejs Data URL should reject percent-embedded base64 whose Buffer allocation exceeds the limit": "plan.md phase 2 \"fix: support data: URLs\": fixed for the real product (HttpService.executeRequest now resolves a data: URL entirely locally - see strategy (a), which passes) - harness limitation for strategy (b) specifically: this bare adapter (tests/upstream/axios/adapters/undici-adapter.cjs) reimplements its own unsupportedProtocol check directly rather than calling HttpService.executeRequest, so it never reaches the new data: URL handling (lib/modules/http/adapters/axios-data-url.adapter.js). Strategy (a) is the one to trust for whether this is a real product gap.",
  "supports http with nodejs Data URL should support requesting data URL as a Blob (if supported by the environment)": "plan.md phase 2 \"fix: support data: URLs\": fixed for the real product (HttpService.executeRequest now resolves a data: URL entirely locally - see strategy (a), which passes) - harness limitation for strategy (b) specifically: this bare adapter (tests/upstream/axios/adapters/undici-adapter.cjs) reimplements its own unsupportedProtocol check directly rather than calling HttpService.executeRequest, so it never reaches the new data: URL handling (lib/modules/http/adapters/axios-data-url.adapter.js). Strategy (a) is the one to trust for whether this is a real product gap.",
  "supports http with nodejs Data URL should support requesting data URL as a Buffer": "plan.md phase 2 \"fix: support data: URLs\": fixed for the real product (HttpService.executeRequest now resolves a data: URL entirely locally - see strategy (a), which passes) - harness limitation for strategy (b) specifically: this bare adapter (tests/upstream/axios/adapters/undici-adapter.cjs) reimplements its own unsupportedProtocol check directly rather than calling HttpService.executeRequest, so it never reaches the new data: URL handling (lib/modules/http/adapters/axios-data-url.adapter.js). Strategy (a) is the one to trust for whether this is a real product gap.",
  "supports http with nodejs Data URL should support requesting data URL as a Stream": "plan.md phase 2 \"fix: support data: URLs\": fixed for the real product (HttpService.executeRequest now resolves a data: URL entirely locally - see strategy (a), which passes) - harness limitation for strategy (b) specifically: this bare adapter (tests/upstream/axios/adapters/undici-adapter.cjs) reimplements its own unsupportedProtocol check directly rather than calling HttpService.executeRequest, so it never reaches the new data: URL handling (lib/modules/http/adapters/axios-data-url.adapter.js). Strategy (a) is the one to trust for whether this is a real product gap.",
  "supports http with nodejs Data URL should support requesting data URL as a String (text)": "plan.md phase 2 \"fix: support data: URLs\": fixed for the real product (HttpService.executeRequest now resolves a data: URL entirely locally - see strategy (a), which passes) - harness limitation for strategy (b) specifically: this bare adapter (tests/upstream/axios/adapters/undici-adapter.cjs) reimplements its own unsupportedProtocol check directly rather than calling HttpService.executeRequest, so it never reaches the new data: URL handling (lib/modules/http/adapters/axios-data-url.adapter.js). Strategy (a) is the one to trust for whether this is a real product gap.",
  "supports http with nodejs FormData formDataHeaderPolicy ignores undefined getHeaders() results with content-only policy": "harness limitation: strategy (b)'s bare adapter (tests/upstream/axios/adapters/undici-adapter.cjs) bypasses HttpService's config normalization by design - a documented scope boundary, not a new finding. Strategy (a) (axiosRef as the instance, exercising the real production pipeline) is the one to trust for whether this is a real product gap; left here rather than guessed at.",
  "supports http with nodejs FormData formDataHeaderPolicy only copies content headers when formDataHeaderPolicy is content-only": "harness limitation: strategy (b)'s bare adapter (tests/upstream/axios/adapters/undici-adapter.cjs) bypasses HttpService's config normalization by design - a documented scope boundary, not a new finding. Strategy (a) (axiosRef as the instance, exercising the real production pipeline) is the one to trust for whether this is a real product gap; left here rather than guessed at.",
  "supports http with nodejs FormData formDataHeaderPolicy preserves legacy getHeaders() propagation by default": "harness limitation: strategy (b)'s bare adapter (tests/upstream/axios/adapters/undici-adapter.cjs) bypasses HttpService's config normalization by design - a documented scope boundary, not a new finding. Strategy (a) (axiosRef as the instance, exercising the real production pipeline) is the one to trust for whether this is a real product gap; left here rather than guessed at.",
  "supports http with nodejs FormData prototype pollution should not merge prototype-polluted getHeaders into outgoing request": "harness limitation: strategy (b)'s bare adapter (tests/upstream/axios/adapters/undici-adapter.cjs) bypasses HttpService's config normalization by design - a documented scope boundary, not a new finding. Strategy (a) (axiosRef as the instance, exercising the real production pipeline) is the one to trust for whether this is a real product gap; left here rather than guessed at.",
  "supports http with nodejs FormData prototype pollution should not use inherited Symbol.iterator for request or response headers": "harness limitation: strategy (b)'s bare adapter (tests/upstream/axios/adapters/undici-adapter.cjs) bypasses HttpService's config normalization by design - a documented scope boundary, not a new finding. Strategy (a) (axiosRef as the instance, exercising the real production pipeline) is the one to trust for whether this is a real product gap; left here rather than guessed at.",
  "supports http with nodejs HTTP2 response types should support arraybuffer response type": "deliberate difference: `httpVersion`/`http2Options` are module-level only (see docs/axios-supported-options.md), needing a per-service Agent built at `HttpModule.register()` time - this suite configures HTTP/2 per-request, which isn’t read, and this package has no HTTP/2-over-plaintext or per-session tuning support to begin with.",
  "supports http with nodejs HTTP2 response types should support json response type": "deliberate difference: `httpVersion`/`http2Options` are module-level only (see docs/axios-supported-options.md), needing a per-service Agent built at `HttpModule.register()` time - this suite configures HTTP/2 per-request, which isn’t read, and this package has no HTTP/2-over-plaintext or per-session tuning support to begin with.",
  "supports http with nodejs HTTP2 response types should support stream response type": "deliberate difference: `httpVersion`/`http2Options` are module-level only (see docs/axios-supported-options.md), needing a per-service Agent built at `HttpModule.register()` time - this suite configures HTTP/2 per-request, which isn’t read, and this package has no HTTP/2-over-plaintext or per-session tuning support to begin with.",
  "supports http with nodejs HTTP2 response types should support text response type": "deliberate difference: `httpVersion`/`http2Options` are module-level only (see docs/axios-supported-options.md), needing a per-service Agent built at `HttpModule.register()` time - this suite configures HTTP/2 per-request, which isn’t read, and this package has no HTTP/2-over-plaintext or per-session tuning support to begin with.",
  "supports http with nodejs HTTP2 session should reuse session for the target authority": "deliberate difference: `httpVersion`/`http2Options` are module-level only (see docs/axios-supported-options.md), needing a per-service Agent built at `HttpModule.register()` time - this suite configures HTTP/2 per-request, which isn’t read, and this package has no HTTP/2-over-plaintext or per-session tuning support to begin with.",
  "supports http with nodejs HTTP2 session should use different sessions after previous session timeout": "deliberate difference: `httpVersion`/`http2Options` are module-level only (see docs/axios-supported-options.md), needing a per-service Agent built at `HttpModule.register()` time - this suite configures HTTP/2 per-request, which isn’t read, and this package has no HTTP/2-over-plaintext or per-session tuning support to begin with.",
  "supports http with nodejs HTTP2 session should use different sessions for different authorities": "deliberate difference: `httpVersion`/`http2Options` are module-level only (see docs/axios-supported-options.md), needing a per-service Agent built at `HttpModule.register()` time - this suite configures HTTP/2 per-request, which isn’t read, and this package has no HTTP/2-over-plaintext or per-session tuning support to begin with.",
  "supports http with nodejs HTTP2 session should use different sessions for requests with different http2Options set": "deliberate difference: `httpVersion`/`http2Options` are module-level only (see docs/axios-supported-options.md), needing a per-service Agent built at `HttpModule.register()` time - this suite configures HTTP/2 per-request, which isn’t read, and this package has no HTTP/2-over-plaintext or per-session tuning support to begin with.",
  "supports http with nodejs HTTP2 session should use the same session for request with the same resolved http2Options set": "deliberate difference: `httpVersion`/`http2Options` are module-level only (see docs/axios-supported-options.md), needing a per-service Agent built at `HttpModule.register()` time - this suite configures HTTP/2 per-request, which isn’t read, and this package has no HTTP/2-over-plaintext or per-session tuning support to begin with.",
  "supports http with nodejs HTTP2 should connect directly for HTTP/2 when only an environment proxy is configured": "deliberate difference: `httpVersion`/`http2Options` are module-level only (see docs/axios-supported-options.md), needing a per-service Agent built at `HttpModule.register()` time - this suite configures HTTP/2 per-request, which isn’t read, and this package has no HTTP/2-over-plaintext or per-session tuning support to begin with.",
  "supports http with nodejs HTTP2 should connect directly for HTTP/2 when only the selected agent has proxyEnv": "deliberate difference: `httpVersion`/`http2Options` are module-level only (see docs/axios-supported-options.md), needing a per-service Agent built at `HttpModule.register()` time - this suite configures HTTP/2 per-request, which isn’t read, and this package has no HTTP/2-over-plaintext or per-session tuning support to begin with.",
  "supports http with nodejs HTTP2 should enforce maxBodyLength for HTTP/2 streamed uploads": "deliberate difference: `httpVersion`/`http2Options` are module-level only (see docs/axios-supported-options.md), needing a per-service Agent built at `HttpModule.register()` time - this suite configures HTTP/2 per-request, which isn’t read, and this package has no HTTP/2-over-plaintext or per-session tuning support to begin with.",
  "supports http with nodejs HTTP2 should honor proxy false for HTTP/2 with a proxyEnv agent": "deliberate difference: `httpVersion`/`http2Options` are module-level only (see docs/axios-supported-options.md), needing a per-service Agent built at `HttpModule.register()` time - this suite configures HTTP/2 per-request, which isn’t read, and this package has no HTTP/2-over-plaintext or per-session tuning support to begin with.",
  "supports http with nodejs HTTP2 should reject when an HTTP/2 request selects a proxy": "deliberate difference: `httpVersion`/`http2Options` are module-level only (see docs/axios-supported-options.md), needing a per-service Agent built at `HttpModule.register()` time - this suite configures HTTP/2 per-request, which isn’t read, and this package has no HTTP/2-over-plaintext or per-session tuning support to begin with.",
  "supports http with nodejs HTTP2 should reuse an HTTP/2 session with the same custom DNS lookup": "deliberate difference: `httpVersion`/`http2Options` are module-level only (see docs/axios-supported-options.md), needing a per-service Agent built at `HttpModule.register()` time - this suite configures HTTP/2 per-request, which isn’t read, and this package has no HTTP/2-over-plaintext or per-session tuning support to begin with.",
  "supports http with nodejs HTTP2 should support FormData as a payload": "deliberate difference: `httpVersion`/`http2Options` are module-level only (see docs/axios-supported-options.md), needing a per-service Agent built at `HttpModule.register()` time - this suite configures HTTP/2 per-request, which isn’t read, and this package has no HTTP/2-over-plaintext or per-session tuning support to begin with.",
  "supports http with nodejs HTTP2 should support http2 transport": "deliberate difference: `httpVersion`/`http2Options` are module-level only (see docs/axios-supported-options.md), needing a per-service Agent built at `HttpModule.register()` time - this suite configures HTTP/2 per-request, which isn’t read, and this package has no HTTP/2-over-plaintext or per-session tuning support to begin with.",
  "supports http with nodejs HTTP2 should support request cancellation": "deliberate difference: `httpVersion`/`http2Options` are module-level only (see docs/axios-supported-options.md), needing a per-service Agent built at `HttpModule.register()` time - this suite configures HTTP/2 per-request, which isn’t read, and this package has no HTTP/2-over-plaintext or per-session tuning support to begin with.",
  "supports http with nodejs HTTP2 should support request payload": "deliberate difference: `httpVersion`/`http2Options` are module-level only (see docs/axios-supported-options.md), needing a per-service Agent built at `HttpModule.register()` time - this suite configures HTTP/2 per-request, which isn’t read, and this package has no HTTP/2-over-plaintext or per-session tuning support to begin with.",
  "supports http with nodejs HTTP2 should support request timeout": "deliberate difference: `httpVersion`/`http2Options` are module-level only (see docs/axios-supported-options.md), needing a per-service Agent built at `HttpModule.register()` time - this suite configures HTTP/2 per-request, which isn’t read, and this package has no HTTP/2-over-plaintext or per-session tuning support to begin with.",
  "supports http with nodejs HTTP2 should support stream response cancellation": "deliberate difference: `httpVersion`/`http2Options` are module-level only (see docs/axios-supported-options.md), needing a per-service Agent built at `HttpModule.register()` time - this suite configures HTTP/2 per-request, which isn’t read, and this package has no HTTP/2-over-plaintext or per-session tuning support to begin with.",
  "supports http with nodejs HTTP2 should use a custom DNS lookup for HTTP/2 connections": "deliberate difference: `httpVersion`/`http2Options` are module-level only (see docs/axios-supported-options.md), needing a per-service Agent built at `HttpModule.register()` time - this suite configures HTTP/2 per-request, which isn’t read, and this package has no HTTP/2-over-plaintext or per-session tuning support to begin with.",
  "supports http with nodejs JSON should support reviver on JSON.parse": "harness limitation: strategy (b)'s bare adapter (tests/upstream/axios/adapters/undici-adapter.cjs) bypasses HttpService's config normalization by design - a documented scope boundary, not a new finding. Strategy (a) (axiosRef as the instance, exercising the real production pipeline) is the one to trust for whether this is a real product gap; left here rather than guessed at.",
  "supports http with nodejs Proxy-Authorization header leak on redirect does not forward Proxy-Authorization to a redirect target that resolves to no-proxy": "harness limitation: strategy (b)'s bare adapter (tests/upstream/axios/adapters/undici-adapter.cjs) reuses only this package's transport/response/error/redirect code (lib/modules/http/adapters/*, lib/modules/http/errors/*) - by design (see tests/upstream/README.md) it bypasses axiosRef's config-normalization pipeline (buildAxiosConfig/normalizeAxiosRequest) entirely, which is where auth, params serialization, default headers, socketPath and proxy support (HTTP/HTTPS/CONNECT-tunnel/env-var/auth) all live. Strategy (a) (axiosRef as the instance) exercises all of these instead.",
  "supports http with nodejs compression algorithms GZIP decompression should reject when the server aborts mid-stream and maxRedirects is 0": "harness limitation: strategy (b)'s bare adapter (tests/upstream/axios/adapters/undici-adapter.cjs) bypasses HttpService's config normalization by design - a documented scope boundary, not a new finding. Strategy (a) (axiosRef as the instance, exercising the real production pipeline) is the one to trust for whether this is a real product gap; left here rather than guessed at.",
  "supports http with nodejs compression algorithms br decompression should reject when the server aborts mid-stream and maxRedirects is 0": "harness limitation: strategy (b)'s bare adapter (tests/upstream/axios/adapters/undici-adapter.cjs) bypasses HttpService's config normalization by design - a documented scope boundary, not a new finding. Strategy (a) (axiosRef as the instance, exercising the real production pipeline) is the one to trust for whether this is a real product gap; left here rather than guessed at.",
  "supports http with nodejs compression algorithms compress decompression should reject when the server aborts mid-stream and maxRedirects is 0": "harness limitation: strategy (b)'s bare adapter (tests/upstream/axios/adapters/undici-adapter.cjs) bypasses HttpService's config normalization by design - a documented scope boundary, not a new finding. Strategy (a) (axiosRef as the instance, exercising the real production pipeline) is the one to trust for whether this is a real product gap; left here rather than guessed at.",
  "supports http with nodejs compression algorithms deflate decompression should reject when the server aborts mid-stream and maxRedirects is 0": "harness limitation: strategy (b)'s bare adapter (tests/upstream/axios/adapters/undici-adapter.cjs) bypasses HttpService's config normalization by design - a documented scope boundary, not a new finding. Strategy (a) (axiosRef as the instance, exercising the real production pipeline) is the one to trust for whether this is a real product gap; left here rather than guessed at.",
  "supports http with nodejs compression algorithms deflate-raw decompression should reject when the server aborts mid-stream and maxRedirects is 0": "harness limitation: strategy (b)'s bare adapter (tests/upstream/axios/adapters/undici-adapter.cjs) bypasses HttpService's config normalization by design - a documented scope boundary, not a new finding. Strategy (a) (axiosRef as the instance, exercising the real production pipeline) is the one to trust for whether this is a real product gap; left here rather than guessed at.",
  "supports http with nodejs compression algorithms gzip decompression should reject when the server aborts mid-stream and maxRedirects is 0": "harness limitation: strategy (b)'s bare adapter (tests/upstream/axios/adapters/undici-adapter.cjs) bypasses HttpService's config normalization by design - a documented scope boundary, not a new finding. Strategy (a) (axiosRef as the instance, exercising the real production pipeline) is the one to trust for whether this is a real product gap; left here rather than guessed at.",
  "supports http with nodejs compression algorithms zstd decompression should reject when the server aborts mid-stream and maxRedirects is 0": "harness limitation: strategy (b)'s bare adapter (tests/upstream/axios/adapters/undici-adapter.cjs) bypasses HttpService's config normalization by design - a documented scope boundary, not a new finding. Strategy (a) (axiosRef as the instance, exercising the real production pipeline) is the one to trust for whether this is a real product gap; left here rather than guessed at.",
  "supports http with nodejs compression should advertise zstd when enabled through transitional config and supported": "harness limitation: strategy (b)'s bare adapter (tests/upstream/axios/adapters/undici-adapter.cjs) bypasses HttpService's config normalization by design - a documented scope boundary, not a new finding. Strategy (a) (axiosRef as the instance, exercising the real production pipeline) is the one to trust for whether this is a real product gap; left here rather than guessed at.",
  "supports http with nodejs compression should not advertise zstd by default": "harness limitation: strategy (b)'s bare adapter (tests/upstream/axios/adapters/undici-adapter.cjs) bypasses HttpService's config normalization by design - a documented scope boundary, not a new finding. Strategy (a) (axiosRef as the instance, exercising the real production pipeline) is the one to trust for whether this is a real product gap; left here rather than guessed at.",
  "supports http with nodejs keep-alive should install at most one socket error listener across reused keep-alive sockets": "harness limitation: instruments Node’s own `http.Agent`/socket pooling and error-listener internals directly (`net.Socket`, keep-alive agent internals, MaxListenersExceededWarning bookkeeping, a custom `lookup` function passed straight to Node’s `http`/`https`) - this package is undici-backed and has no equivalent internals to instrument.",
  "supports http with nodejs keep-alive should not accumulate socket error listeners when a pooled socket is reassigned before the previous request closes (regression #10780)": "harness limitation: instruments Node’s own `http.Agent`/socket pooling and error-listener internals directly (`net.Socket`, keep-alive agent internals, MaxListenersExceededWarning bookkeeping, a custom `lookup` function passed straight to Node’s `http`/`https`) - this package is undici-backed and has no equivalent internals to instrument.",
  "supports http with nodejs keep-alive should not throw TypeError when a proxy agent stream does not define setKeepAlive (regression #10908)": {
    "reason": "environment-dependent: the test hands axios a mock `transport` (a Node http-specific option this package does not support), so the request falls through to a real GET http://example.com/. It passes whenever the runner has internet access (Actions) and fails offline (this sandbox) - neither outcome says anything about this package. Allowed to pass or fail either way rather than flagged as new/fixed.",
    "environmentDependent": true
  },
  "supports http with nodejs keep-alive should reuse one context-free error listener across different sockets": "harness limitation: instruments Node’s own `http.Agent`/socket pooling and error-listener internals directly (`net.Socket`, keep-alive agent internals, MaxListenersExceededWarning bookkeeping, a custom `lookup` function passed straight to Node’s `http`/`https`) - this package is undici-backed and has no equivalent internals to instrument.",
  "supports http with nodejs keep-alive should route a socket error to the currently-active request after the socket has been reassigned": "harness limitation: instruments Node’s own `http.Agent`/socket pooling and error-listener internals directly (`net.Socket`, keep-alive agent internals, MaxListenersExceededWarning bookkeeping, a custom `lookup` function passed straight to Node’s `http`/`https`) - this package is undici-backed and has no equivalent internals to instrument.",
  "supports http with nodejs keeps malformed URL credentials percent-encoding and does not throw": "harness limitation: strategy (b)'s bare adapter (tests/upstream/axios/adapters/undici-adapter.cjs) reuses only this package's transport/response/error/redirect code (lib/modules/http/adapters/*, lib/modules/http/errors/*) - by design (see tests/upstream/README.md) it bypasses axiosRef's config-normalization pipeline (buildAxiosConfig/normalizeAxiosRequest) entirely, which is where auth, params serialization, default headers, socketPath and proxy support (HTTP/HTTPS/CONNECT-tunnel/env-var/auth) all live. Strategy (a) (axiosRef as the instance) exercises all of these instead.",
  "supports http with nodejs option validation should reject BigInt httpVersion with AxiosError": "harness limitation: instruments Node’s own `http.Agent`/socket pooling and error-listener internals directly (`net.Socket`, keep-alive agent internals, MaxListenersExceededWarning bookkeeping, a custom `lookup` function passed straight to Node’s `http`/`https`) - this package is undici-backed and has no equivalent internals to instrument.",
  "supports http with nodejs option validation should reject Symbol httpVersion with AxiosError": "harness limitation: instruments Node’s own `http.Agent`/socket pooling and error-listener internals directly (`net.Socket`, keep-alive agent internals, MaxListenersExceededWarning bookkeeping, a custom `lookup` function passed straight to Node’s `http`/`https`) - this package is undici-backed and has no equivalent internals to instrument.",
  "supports http with nodejs option validation should reject invalid address with AxiosError": "harness limitation: instruments Node’s own `http.Agent`/socket pooling and error-listener internals directly (`net.Socket`, keep-alive agent internals, MaxListenersExceededWarning bookkeeping, a custom `lookup` function passed straight to Node’s `http`/`https`) - this package is undici-backed and has no equivalent internals to instrument.",
  "supports http with nodejs option validation should reject invalid httpVersion with AxiosError": "harness limitation: instruments Node’s own `http.Agent`/socket pooling and error-listener internals directly (`net.Socket`, keep-alive agent internals, MaxListenersExceededWarning bookkeeping, a custom `lookup` function passed straight to Node’s `http`/`https`) - this package is undici-backed and has no equivalent internals to instrument.",
  "supports http with nodejs option validation should reject object with throwing primitive conversion httpVersion with AxiosError": "harness limitation: instruments Node’s own `http.Agent`/socket pooling and error-listener internals directly (`net.Socket`, keep-alive agent internals, MaxListenersExceededWarning bookkeeping, a custom `lookup` function passed straight to Node’s `http`/`https`) - this package is undici-backed and has no equivalent internals to instrument.",
  "supports http with nodejs option validation should reject unsupported httpVersion with AxiosError": "harness limitation: instruments Node’s own `http.Agent`/socket pooling and error-listener internals directly (`net.Socket`, keep-alive agent internals, MaxListenersExceededWarning bookkeeping, a custom `lookup` function passed straight to Node’s `http`/`https`) - this package is undici-backed and has no equivalent internals to instrument.",
  "supports http with nodejs progress download should flush final download progress before a streamed response closes": "plan.md phase 2: Progress callbacks (`onUploadProgress` / `onDownloadProgress`), `maxRate`, `formSerializer` - not yet implemented (tracked item, not a new finding).",
  "supports http with nodejs progress download should support download progress capturing": "plan.md phase 2: Progress callbacks (`onUploadProgress` / `onDownloadProgress`), `maxRate`, `formSerializer` - not yet implemented (tracked item, not a new finding).",
  "supports http with nodejs progress upload should support upload progress capturing": "plan.md phase 2: Progress callbacks (`onUploadProgress` / `onDownloadProgress`), `maxRate`, `formSerializer` - not yet implemented (tracked item, not a new finding).",
  "supports http with nodejs should CONNECT-tunnel HTTPS targets through an HTTP proxy by default (issue #6320)": "harness limitation: strategy (b)'s bare adapter (tests/upstream/axios/adapters/undici-adapter.cjs) reuses only this package's transport/response/error/redirect code (lib/modules/http/adapters/*, lib/modules/http/errors/*) - by design (see tests/upstream/README.md) it bypasses axiosRef's config-normalization pipeline (buildAxiosConfig/normalizeAxiosRequest) entirely, which is where auth, params serialization, default headers, socketPath and proxy support (HTTP/HTTPS/CONNECT-tunnel/env-var/auth) all live. Strategy (a) (axiosRef as the instance) exercises all of these instead.",
  "supports http with nodejs should allow the Content-Length header to be overridden": "deliberate difference: undici validates Content-Length against the body (request-smuggling protection) and throws RequestContentLengthMismatchError; Node http trusts a caller-supplied value",
  "supports http with nodejs should apply httpsAgent TLS options to CONNECT-tunneled origins (issue #10953)": "harness limitation: strategy (b)'s bare adapter (tests/upstream/axios/adapters/undici-adapter.cjs) reuses only this package's transport/response/error/redirect code (lib/modules/http/adapters/*, lib/modules/http/errors/*) - by design (see tests/upstream/README.md) it bypasses axiosRef's config-normalization pipeline (buildAxiosConfig/normalizeAxiosRequest) entirely, which is where auth, params serialization, default headers, socketPath and proxy support (HTTP/HTTPS/CONNECT-tunnel/env-var/auth) all live. Strategy (a) (axiosRef as the instance) exercises all of these instead.",
  "supports http with nodejs should decode basic auth credentials from the request URL": "harness limitation: strategy (b)'s bare adapter (tests/upstream/axios/adapters/undici-adapter.cjs) reuses only this package's transport/response/error/redirect code (lib/modules/http/adapters/*, lib/modules/http/errors/*) - by design (see tests/upstream/README.md) it bypasses axiosRef's config-normalization pipeline (buildAxiosConfig/normalizeAxiosRequest) entirely, which is where auth, params serialization, default headers, socketPath and proxy support (HTTP/HTTPS/CONNECT-tunnel/env-var/auth) all live. Strategy (a) (axiosRef as the instance) exercises all of these instead.",
  "supports http with nodejs should display error while parsing params": "harness limitation: strategy (b)'s bare adapter (tests/upstream/axios/adapters/undici-adapter.cjs) reuses only this package's transport/response/error/redirect code (lib/modules/http/adapters/*, lib/modules/http/errors/*) - by design (see tests/upstream/README.md) it bypasses axiosRef's config-normalization pipeline (buildAxiosConfig/normalizeAxiosRequest) entirely, which is where auth, params serialization, default headers, socketPath and proxy support (HTTP/HTTPS/CONNECT-tunnel/env-var/auth) all live. Strategy (a) (axiosRef as the instance) exercises all of these instead.",
  "supports http with nodejs should enforce maxBodyLength for streamed uploads with maxRedirects: 0": "harness limitation: same config-normalization gap as above - streamed maxContentLength/maxBodyLength enforcement is wired up in HttpService.executeRequest, not in the response/error code this bare adapter reuses.",
  "supports http with nodejs should ignore inherited nested request option fields in http adapter": "harness limitation: strategy (b)'s bare adapter (tests/upstream/axios/adapters/undici-adapter.cjs) reuses only this package's transport/response/error/redirect code (lib/modules/http/adapters/*, lib/modules/http/errors/*) - by design (see tests/upstream/README.md) it bypasses axiosRef's config-normalization pipeline (buildAxiosConfig/normalizeAxiosRequest) entirely, which is where auth, params serialization, default headers, socketPath and proxy support (HTTP/HTTPS/CONNECT-tunnel/env-var/auth) all live. Strategy (a) (axiosRef as the instance) exercises all of these instead.",
  "supports http with nodejs should preserve basic auth across multi-hop same-origin redirects": "harness limitation: strategy (b)'s bare adapter (tests/upstream/axios/adapters/undici-adapter.cjs) reuses only this package's transport/response/error/redirect code (lib/modules/http/adapters/*, lib/modules/http/errors/*) - by design (see tests/upstream/README.md) it bypasses axiosRef's config-normalization pipeline (buildAxiosConfig/normalizeAxiosRequest) entirely, which is where auth, params serialization, default headers, socketPath and proxy support (HTTP/HTTPS/CONNECT-tunnel/env-var/auth) all live. Strategy (a) (axiosRef as the instance) exercises all of these instead.",
  "supports http with nodejs should preserve basic auth across same-origin 303 POST - GET redirect": "harness limitation: strategy (b)'s bare adapter (tests/upstream/axios/adapters/undici-adapter.cjs) reuses only this package's transport/response/error/redirect code (lib/modules/http/adapters/*, lib/modules/http/errors/*) - by design (see tests/upstream/README.md) it bypasses axiosRef's config-normalization pipeline (buildAxiosConfig/normalizeAxiosRequest) entirely, which is where auth, params serialization, default headers, socketPath and proxy support (HTTP/HTTPS/CONNECT-tunnel/env-var/auth) all live. Strategy (a) (axiosRef as the instance) exercises all of these instead.",
  "supports http with nodejs should provides a default User-Agent header": "harness limitation: strategy (b)'s bare adapter (tests/upstream/axios/adapters/undici-adapter.cjs) reuses only this package's transport/response/error/redirect code (lib/modules/http/adapters/*, lib/modules/http/errors/*) - by design (see tests/upstream/README.md) it bypasses axiosRef's config-normalization pipeline (buildAxiosConfig/normalizeAxiosRequest) entirely, which is where auth, params serialization, default headers, socketPath and proxy support (HTTP/HTTPS/CONNECT-tunnel/env-var/auth) all live. Strategy (a) (axiosRef as the instance) exercises all of these instead.",
  "supports http with nodejs should re-evaluate proxy on redirect when proxy set via env var": "harness limitation: strategy (b)'s bare adapter (tests/upstream/axios/adapters/undici-adapter.cjs) reuses only this package's transport/response/error/redirect code (lib/modules/http/adapters/*, lib/modules/http/errors/*) - by design (see tests/upstream/README.md) it bypasses axiosRef's config-normalization pipeline (buildAxiosConfig/normalizeAxiosRequest) entirely, which is where auth, params serialization, default headers, socketPath and proxy support (HTTP/HTTPS/CONNECT-tunnel/env-var/auth) all live. Strategy (a) (axiosRef as the instance) exercises all of these instead.",
  "supports http with nodejs should reject invalid sensitiveHeaders config": "harness limitation: strategy (b)'s bare adapter (tests/upstream/axios/adapters/undici-adapter.cjs) bypasses HttpService's config normalization by design - a documented scope boundary, not a new finding. Strategy (a) (axiosRef as the instance, exercising the real production pipeline) is the one to trust for whether this is a real product gap; left here rather than guessed at.",
  "supports http with nodejs should reject when only the request socket emits an error": "harness limitation: instruments Node’s own `http.Agent`/socket pooling and error-listener internals directly (`net.Socket`, keep-alive agent internals, MaxListenersExceededWarning bookkeeping, a custom `lookup` function passed straight to Node’s `http`/`https`) - this package is undici-backed and has no equivalent internals to instrument.",
  "supports http with nodejs should respect the timeout property during TCP connect with maxRedirects set to 0": {
    "reason": "environment-dependent: same DNS race as in strategy (a) - the hanging custom `httpAgent` is ignored, so the request really resolves `connect-timeout.test`, and whether the 100ms `timeout` or an ENOTFOUND wins depends on the runner's resolver (seen both ways on Actions). Allowed to pass or fail either way.",
    "environmentDependent": true
  },
  "supports http with nodejs should sanitize unencoded Unicode request headers before passing them to Node": "harness limitation: strategy (b)'s bare adapter (tests/upstream/axios/adapters/undici-adapter.cjs) reuses only this package's transport/response/error/redirect code (lib/modules/http/adapters/*, lib/modules/http/errors/*) - by design (see tests/upstream/README.md) it bypasses axiosRef's config-normalization pipeline (buildAxiosConfig/normalizeAxiosRequest) entirely, which is where auth, params serialization, default headers, socketPath and proxy support (HTTP/HTTPS/CONNECT-tunnel/env-var/auth) all live. Strategy (a) (axiosRef as the instance) exercises all of these instead.",
  "supports http with nodejs should strip sensitiveHeaders case-insensitively on cross-origin redirect": "harness limitation: strategy (b)'s bare adapter (tests/upstream/axios/adapters/undici-adapter.cjs) bypasses HttpService's config normalization by design - a documented scope boundary, not a new finding. Strategy (a) (axiosRef as the instance, exercising the real production pipeline) is the one to trust for whether this is a real product gap; left here rather than guessed at.",
  "supports http with nodejs should strip sensitiveHeaders configured on an instance": "harness limitation: strategy (b)'s bare adapter (tests/upstream/axios/adapters/undici-adapter.cjs) bypasses HttpService's config normalization by design - a documented scope boundary, not a new finding. Strategy (a) (axiosRef as the instance, exercising the real production pipeline) is the one to trust for whether this is a real product gap; left here rather than guessed at.",
  "supports http with nodejs should strip sensitiveHeaders on cross-origin redirect": "harness limitation: strategy (b)'s bare adapter (tests/upstream/axios/adapters/undici-adapter.cjs) bypasses HttpService's config normalization by design - a documented scope boundary, not a new finding. Strategy (a) (axiosRef as the instance, exercising the real production pipeline) is the one to trust for whether this is a real product gap; left here rather than guessed at.",
  "supports http with nodejs should supply a user-agent if one is not specified": "harness limitation: strategy (b)'s bare adapter (tests/upstream/axios/adapters/undici-adapter.cjs) bypasses HttpService's config normalization by design - a documented scope boundary, not a new finding. Strategy (a) (axiosRef as the instance, exercising the real production pipeline) is the one to trust for whether this is a real product gap; left here rather than guessed at.",
  "supports http with nodejs should support HTTP protocol": "harness limitation: Node http-specific / sandbox environment artifact, not this package - see the specific note below. IPv6/protocol-family specific tests can fail with ECONNREFUSED/EAFNOSUPPORT/ENOTFOUND depending on this sandbox’s network stack, independent of this package.",
  "supports http with nodejs should support HTTP proxies": "harness limitation: strategy (b)'s bare adapter (tests/upstream/axios/adapters/undici-adapter.cjs) reuses only this package's transport/response/error/redirect code (lib/modules/http/adapters/*, lib/modules/http/errors/*) - by design (see tests/upstream/README.md) it bypasses axiosRef's config-normalization pipeline (buildAxiosConfig/normalizeAxiosRequest) entirely, which is where auth, params serialization, default headers, socketPath and proxy support (HTTP/HTTPS/CONNECT-tunnel/env-var/auth) all live. Strategy (a) (axiosRef as the instance) exercises all of these instead.",
  "supports http with nodejs should support HTTP proxy auth": "harness limitation: strategy (b)'s bare adapter (tests/upstream/axios/adapters/undici-adapter.cjs) reuses only this package's transport/response/error/redirect code (lib/modules/http/adapters/*, lib/modules/http/errors/*) - by design (see tests/upstream/README.md) it bypasses axiosRef's config-normalization pipeline (buildAxiosConfig/normalizeAxiosRequest) entirely, which is where auth, params serialization, default headers, socketPath and proxy support (HTTP/HTTPS/CONNECT-tunnel/env-var/auth) all live. Strategy (a) (axiosRef as the instance) exercises all of these instead.",
  "supports http with nodejs should support HTTPS protocol": "harness limitation: Node http-specific / sandbox environment artifact, not this package - see the specific note below. IPv6/protocol-family specific tests can fail with ECONNREFUSED/EAFNOSUPPORT/ENOTFOUND depending on this sandbox’s network stack, independent of this package.",
  "supports http with nodejs should support HTTPS proxies": "harness limitation: strategy (b)'s bare adapter (tests/upstream/axios/adapters/undici-adapter.cjs) reuses only this package's transport/response/error/redirect code (lib/modules/http/adapters/*, lib/modules/http/errors/*) - by design (see tests/upstream/README.md) it bypasses axiosRef's config-normalization pipeline (buildAxiosConfig/normalizeAxiosRequest) entirely, which is where auth, params serialization, default headers, socketPath and proxy support (HTTP/HTTPS/CONNECT-tunnel/env-var/auth) all live. Strategy (a) (axiosRef as the instance) exercises all of these instead.",
  "supports http with nodejs should support HTTPS proxy set via env var": "harness limitation: strategy (b)'s bare adapter (tests/upstream/axios/adapters/undici-adapter.cjs) reuses only this package's transport/response/error/redirect code (lib/modules/http/adapters/*, lib/modules/http/errors/*) - by design (see tests/upstream/README.md) it bypasses axiosRef's config-normalization pipeline (buildAxiosConfig/normalizeAxiosRequest) entirely, which is where auth, params serialization, default headers, socketPath and proxy support (HTTP/HTTPS/CONNECT-tunnel/env-var/auth) all live. Strategy (a) (axiosRef as the instance) exercises all of these instead.",
  "supports http with nodejs should support IPv6 literal strings": {
    "reason": "environment-dependent: needs IPv6 support/routing on the runner (an ::1 bind + connect); confirmed to fail in this sandbox (no IPv6) and pass on a real, dedicated Actions runner. Allowed to pass or fail either way rather than flagged as new/fixed.",
    "environmentDependent": true
  },
  "supports http with nodejs should support basic auth": "harness limitation: strategy (b)'s bare adapter (tests/upstream/axios/adapters/undici-adapter.cjs) reuses only this package's transport/response/error/redirect code (lib/modules/http/adapters/*, lib/modules/http/errors/*) - by design (see tests/upstream/README.md) it bypasses axiosRef's config-normalization pipeline (buildAxiosConfig/normalizeAxiosRequest) entirely, which is where auth, params serialization, default headers, socketPath and proxy support (HTTP/HTTPS/CONNECT-tunnel/env-var/auth) all live. Strategy (a) (axiosRef as the instance) exercises all of these instead.",
  "supports http with nodejs should support basic auth with a header": "harness limitation: strategy (b)'s bare adapter (tests/upstream/axios/adapters/undici-adapter.cjs) reuses only this package's transport/response/error/redirect code (lib/modules/http/adapters/*, lib/modules/http/errors/*) - by design (see tests/upstream/README.md) it bypasses axiosRef's config-normalization pipeline (buildAxiosConfig/normalizeAxiosRequest) entirely, which is where auth, params serialization, default headers, socketPath and proxy support (HTTP/HTTPS/CONNECT-tunnel/env-var/auth) all live. Strategy (a) (axiosRef as the instance) exercises all of these instead.",
  "supports http with nodejs should support beforeRedirect and proxy with redirect": "harness limitation: strategy (b)'s bare adapter (tests/upstream/axios/adapters/undici-adapter.cjs) reuses only this package's transport/response/error/redirect code (lib/modules/http/adapters/*, lib/modules/http/errors/*) - by design (see tests/upstream/README.md) it bypasses axiosRef's config-normalization pipeline (buildAxiosConfig/normalizeAxiosRequest) entirely, which is where auth, params serialization, default headers, socketPath and proxy support (HTTP/HTTPS/CONNECT-tunnel/env-var/auth) all live. Strategy (a) (axiosRef as the instance) exercises all of these instead.",
  "supports http with nodejs should support function as paramsSerializer value": "harness limitation: strategy (b)'s bare adapter (tests/upstream/axios/adapters/undici-adapter.cjs) bypasses HttpService's config normalization by design - a documented scope boundary, not a new finding. Strategy (a) (axiosRef as the instance, exercising the real production pipeline) is the one to trust for whether this is a real product gap; left here rather than guessed at.",
  "supports http with nodejs should support password-only basic auth credentials from the request URL": "harness limitation: strategy (b)'s bare adapter (tests/upstream/axios/adapters/undici-adapter.cjs) reuses only this package's transport/response/error/redirect code (lib/modules/http/adapters/*, lib/modules/http/errors/*) - by design (see tests/upstream/README.md) it bypasses axiosRef's config-normalization pipeline (buildAxiosConfig/normalizeAxiosRequest) entirely, which is where auth, params serialization, default headers, socketPath and proxy support (HTTP/HTTPS/CONNECT-tunnel/env-var/auth) all live. Strategy (a) (axiosRef as the instance) exercises all of these instead.",
  "supports http with nodejs should support proxy auth from env": "harness limitation: strategy (b)'s bare adapter (tests/upstream/axios/adapters/undici-adapter.cjs) reuses only this package's transport/response/error/redirect code (lib/modules/http/adapters/*, lib/modules/http/errors/*) - by design (see tests/upstream/README.md) it bypasses axiosRef's config-normalization pipeline (buildAxiosConfig/normalizeAxiosRequest) entirely, which is where auth, params serialization, default headers, socketPath and proxy support (HTTP/HTTPS/CONNECT-tunnel/env-var/auth) all live. Strategy (a) (axiosRef as the instance) exercises all of these instead.",
  "supports http with nodejs should support proxy set via env var": "harness limitation: strategy (b)'s bare adapter (tests/upstream/axios/adapters/undici-adapter.cjs) reuses only this package's transport/response/error/redirect code (lib/modules/http/adapters/*, lib/modules/http/errors/*) - by design (see tests/upstream/README.md) it bypasses axiosRef's config-normalization pipeline (buildAxiosConfig/normalizeAxiosRequest) entirely, which is where auth, params serialization, default headers, socketPath and proxy support (HTTP/HTTPS/CONNECT-tunnel/env-var/auth) all live. Strategy (a) (axiosRef as the instance) exercises all of these instead.",
  "supports http with nodejs should support sockets": "harness limitation: strategy (b)'s bare adapter (tests/upstream/axios/adapters/undici-adapter.cjs) reuses only this package's transport/response/error/redirect code (lib/modules/http/adapters/*, lib/modules/http/errors/*) - by design (see tests/upstream/README.md) it bypasses axiosRef's config-normalization pipeline (buildAxiosConfig/normalizeAxiosRequest) entirely, which is where auth, params serialization, default headers, socketPath and proxy support (HTTP/HTTPS/CONNECT-tunnel/env-var/auth) all live. Strategy (a) (axiosRef as the instance) exercises all of these instead.",
  "supports http with nodejs should surface a CONNECT 407 from the proxy as an AxiosError (issue #6320)": "harness limitation: strategy (b)'s bare adapter (tests/upstream/axios/adapters/undici-adapter.cjs) reuses only this package's transport/response/error/redirect code (lib/modules/http/adapters/*, lib/modules/http/errors/*) - by design (see tests/upstream/README.md) it bypasses axiosRef's config-normalization pipeline (buildAxiosConfig/normalizeAxiosRequest) entirely, which is where auth, params serialization, default headers, socketPath and proxy support (HTTP/HTTPS/CONNECT-tunnel/env-var/auth) all live. Strategy (a) (axiosRef as the instance) exercises all of these instead.",
  "supports http with nodejs should throw an error if http server that aborts a chunked request": "harness limitation: strategy (b)'s bare adapter (tests/upstream/axios/adapters/undici-adapter.cjs) bypasses HttpService's config normalization by design - a documented scope boundary, not a new finding. Strategy (a) (axiosRef as the instance, exercising the real production pipeline) is the one to trust for whether this is a real product gap; left here rather than guessed at.",
  "supports http with nodejs should use proxy for domains not in no_proxy": "harness limitation: strategy (b)'s bare adapter (tests/upstream/axios/adapters/undici-adapter.cjs) reuses only this package's transport/response/error/redirect code (lib/modules/http/adapters/*, lib/modules/http/errors/*) - by design (see tests/upstream/README.md) it bypasses axiosRef's config-normalization pipeline (buildAxiosConfig/normalizeAxiosRequest) entirely, which is where auth, params serialization, default headers, socketPath and proxy support (HTTP/HTTPS/CONNECT-tunnel/env-var/auth) all live. Strategy (a) (axiosRef as the instance) exercises all of these instead.",
  "supports http with nodejs socketPath security accepts a path-only url when socketPath is set (regression #6611)": "deliberate difference: `allowedSocketPaths` (an axios security feature restricting which `socketPath` values are accepted) is not implemented by this package - `socketPath` itself is supported (see docs/axios-supported-options.md) but unconditionally, with no allowlist.",
  "supports http with nodejs socketPath security accepts a path-only url when socketPath matches allowedSocketPaths": "deliberate difference: `allowedSocketPaths` (an axios security feature restricting which `socketPath` values are accepted) is not implemented by this package - `socketPath` itself is supported (see docs/axios-supported-options.md) but unconditionally, with no allowlist.",
  "supports http with nodejs socketPath security allows socketPath when it matches an allowedSocketPaths string": "deliberate difference: `allowedSocketPaths` (an axios security feature restricting which `socketPath` values are accepted) is not implemented by this package - `socketPath` itself is supported (see docs/axios-supported-options.md) but unconditionally, with no allowlist.",
  "supports http with nodejs socketPath security allows socketPath when it matches an entry in allowedSocketPaths array": "deliberate difference: `allowedSocketPaths` (an axios security feature restricting which `socketPath` values are accepted) is not implemented by this package - `socketPath` itself is supported (see docs/axios-supported-options.md) but unconditionally, with no allowlist.",
  "supports http with nodejs socketPath security allows socketPath when no allowedSocketPaths is set (backwards compatible)": "deliberate difference: `allowedSocketPaths` (an axios security feature restricting which `socketPath` values are accepted) is not implemented by this package - `socketPath` itself is supported (see docs/axios-supported-options.md) but unconditionally, with no allowlist.",
  "supports http with nodejs socketPath security empty allowedSocketPaths array blocks all socketPath values": "deliberate difference: `allowedSocketPaths` (an axios security feature restricting which `socketPath` values are accepted) is not implemented by this package - `socketPath` itself is supported (see docs/axios-supported-options.md) but unconditionally, with no allowlist.",
  "supports http with nodejs socketPath security rejects non-string socketPath": "deliberate difference: `allowedSocketPaths` (an axios security feature restricting which `socketPath` values are accepted) is not implemented by this package - `socketPath` itself is supported (see docs/axios-supported-options.md) but unconditionally, with no allowlist.",
  "supports http with nodejs socketPath security rejects socketPath attempting path traversal that escapes allowlist": "deliberate difference: `allowedSocketPaths` (an axios security feature restricting which `socketPath` values are accepted) is not implemented by this package - `socketPath` itself is supported (see docs/axios-supported-options.md) but unconditionally, with no allowlist.",
  "supports http with nodejs socketPath security rejects socketPath not in allowedSocketPaths": "deliberate difference: `allowedSocketPaths` (an axios security feature restricting which `socketPath` values are accepted) is not implemented by this package - `socketPath` itself is supported (see docs/axios-supported-options.md) but unconditionally, with no allowlist.",
  "supports http with nodejs socketPath security treats relative and absolute allowedSocketPaths entries equivalently": "deliberate difference: `allowedSocketPaths` (an axios security feature restricting which `socketPath` values are accepted) is not implemented by this package - `socketPath` itself is supported (see docs/axios-supported-options.md) but unconditionally, with no allowlist.",
  "supports http with nodejs toFormData helper should only match explicit routes for express 5 form handlers": "harness limitation: strategy (b)'s bare adapter (tests/upstream/axios/adapters/undici-adapter.cjs) bypasses HttpService's config normalization by design - a documented scope boundary, not a new finding. Strategy (a) (axiosRef as the instance, exercising the real production pipeline) is the one to trust for whether this is a real product gap; left here rather than guessed at.",
  "supports http with nodejs toFormData helper should properly serialize nested objects for parsing with multer.js (express.js)": "harness limitation: strategy (b)'s bare adapter (tests/upstream/axios/adapters/undici-adapter.cjs) bypasses HttpService's config normalization by design - a documented scope boundary, not a new finding. Strategy (a) (axiosRef as the instance, exercising the real production pipeline) is the one to trust for whether this is a real product gap; left here rather than guessed at.",
  "supports http with nodejs when invalid proxy options are provided should throw error": "harness limitation: strategy (b)'s bare adapter (tests/upstream/axios/adapters/undici-adapter.cjs) reuses only this package's transport/response/error/redirect code (lib/modules/http/adapters/*, lib/modules/http/errors/*) - by design (see tests/upstream/README.md) it bypasses axiosRef's config-normalization pipeline (buildAxiosConfig/normalizeAxiosRequest) entirely, which is where auth, params serialization, default headers, socketPath and proxy support (HTTP/HTTPS/CONNECT-tunnel/env-var/auth) all live. Strategy (a) (axiosRef as the instance) exercises all of these instead."
}
